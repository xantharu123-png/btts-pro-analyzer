# Football live ORIGINAL integration — storage and causal publication

Spec: `docs/superpowers/specs/2026-09-07-kontextmodell-design.md`.
Execution: approved isolated worktree, subagent-driven development. Correctness and storage work for an existing approved model contract, not activation of a new numerical effect.

## Global Constraints

- No new model fit, score matrix or calibrator invocation. Preserve exact floats, order, existing ORIGINAL-v1 logical bytes, source clocks and legacy forecasts.
- Price never changes the model pool or sporting provenance. Cricket and monetary state are untouched.
- Missing native links remain explicitly unresolved, never invented. Corruption is not missing data. Later receipts cannot qualify an earlier decision.
- New original storage is transport-only until its owning source/replay/training contract is integrated; no empirical or injury-effect approval is implied.
- No whole-history copy per timestamp. Reuse equal immutable chunks and bodies. No old artifact pruning, migration or rewriting.
- Readers are read-only; freeze physically related bytes in one transaction and release it before CPU decoding. Writers publish complete atomic units with collision/readback checks.

### Task 1: Bounded, lossless, content-addressed ORIGINAL storage

Implement `context_models/football_original_storage.py` and `tests/test_football_original_storage.py`; narrowly factor shared atomic insertion in `model_artifacts.py` if needed, with its tests. This storage prerequisite must be complete before live scanning is enabled. Do not change scanner or model calculations in this task.

1. Read `football_original.py` and existing original capture/parity tests. Accept the owning `FootballOriginal` packet, not arbitrary user JSON. Logical v1 data must stay unchanged, including ordered/duplicate arrays and float precision. Test round-trip canonical bytes and logical digest exactly, not rounded equality.
2. Fixed-path pack only these arrays: `league_history`; non-null `team_history`; `goal_provenance.history_refs`; and `goal_provenance.reference_weights.heads.{home,away}.components.{venue_attack,venue_defense,form_attack,form_defense}.{goals,xg}.prior_refs` when the existing provenance has its supported shape and the metric is not null. An explicitly unavailable legacy provenance has no such paths and must remain unchanged, not guessed.
3. Each equal array is one existing-A1 artifact kind `football-original-json-chunk-v1`, payload `{schema: 1, values: array}`. Typed closed references replace only these fixed paths. Stable body kind `football-original-body-v1` contains the exact remaining packet with `captured_at` removed. Identical bodies reuse one artifact. A compact `football-original-storage-manifest-v1` links body, original capture clock and the logical ORIGINAL digest. Physical manifest identity is distinct from logical expanded identity; never place compact bytes under a digest of expanded bytes. No new database tables or generic recursive tree format.
4. Provide an owning storage/load API and a preparation API suitable for a later outer live publication, so that the later bridge can include decision/code/B1 bindings in the same atomic publication rather than fabricate those now. Validate closed versions/kinds/reference shapes, canonical timestamps, actual stored creation times and reference availability. Bounded fixed-depth reconstruction; references cannot recurse. Resolve only required raw rows in a consistent read image, then decode after release. Missing/corrupt chunks/body/manifest, wrong kinds/types and broken hashes fail explicitly.
5. Plan all unique encoded objects before mutation. Initial safety bounds: expanded ORIGINAL at most 4 MiB; compact manifest at most 8 KiB. The caller must supply a finite nonnegative integer budget of newly inserted payload bytes for this publication; do not introduce a silent unlimited default or arbitrary lifetime limit. Charge only new unique objects under the write transaction so concurrent writers cannot overrun a shared per-publication insertion budget. This is a payload-byte bound, not a disk-byte guarantee. A dedicated storage-budget exception leaves no partial insertions or orphan chunks. On repeated same content, existing creation timestamps remain untouched.
6. One writer transaction/savepoint publishes all missing chunks/body/manifest, insert-if-absent plus actual readback. Reuse/refactor the owning A1 insertion validation instead of duplicating a weaker writer. Preserve public `put_artifact` behavior. No process-global initialized/schema shortcut, journal-mode or timeout change.
7. TDD RED first. Tests: real owning ORIGINAL fixture round-trip; unavailable provenance; repeated identical calculation with changed actual capture clock adds only manifest and zero chunks/body; equal prior arrays shared across 16 uses; same-league cross-event reuse; changed score/xG/order/recipe produces the correct changed objects; duplicates/order retained; malformed, oversized and recursive references rejected; canonical type/clock/digest failures; transaction rollback on size/budget/collision/readback failure; existing timestamps preserved; concurrent real connections and retry idempotence. Do not substitute mock writer invocation for stored contents.
8. Include a deterministic 380-row season-volume synthetic fixture using existing model factories (not real-source evidence). Measure expanded bytes, unique payload plan and SQLite file growth on initial and unchanged repeated saves. Prior read-only probe observed ~830 KB expanded and ~233 KB unique payload before full headers; report actual results rather than asserting those exact measurements. Test a meaningful storage-reuse ratio and bounded incremental manifest payload. No network, production DB or source qualification.
9. Run focused RED/GREEN, then all original capture/parity/native tests and model-artifact tests plus new storage tests. Preserve original AST parent pins and model behavior; this task changes no engine seams. Self-review, commit only owned source/tests, no push/deploy. Report exact commands, results, sizes, API and remaining limitations to its SDD task report.

### Task 2: Same-call final-scan publication with causal B1 links

Pending detailed task brief after storage review. Required integration: actual pre-model decision clock; one already-persisted B1 snapshot per owning worker; validate receipt hashes/index/body before filtering; resolve only engine-selected baseline records; forward optional direct native provenance through both engine wrappers; capture only final discovery and actual model-recompute refresh calls, never probes or price/context-only paths. Publish immutable compact ORIGINAL plus code identity and exact causal B1 binding, with no new model fits or changed forecasts. Receipt absence is partial evidence, not a market exclusion. No pending in-memory response is authoritative before exact B1 persistence/readback. Per-publication and per-run budget must be derived from measured task-1 storage before production enablement. Preserve old candidate serialization; references belong in closed optional admin metadata. Full wrapper/worker parity, temporal-correction, source-integrity, quote-independence and no-extra-provider-call regressions required. Existing verifier unknown-artifact transport acceptance is not an owning mathematical replay proof.

### Task 3: Integration and release evidence

After task-2's concrete reviewed implementation, run broad suite and independent branch review. Measure realistic isolated scan/refresh storage and CPU, confirm source timestamps and dedup, and verify protected-backup/restore compatibility without relaxing gate contracts. Enable production only with a finite measured operational budget and sufficient unchanged updater reserve. Report software, native coverage, empirical status and activation separately. Remaining learned football effect/training/consumer adapter and untouched holdout evaluation are not completed by ORIGINAL publication.
