# D3 closed context transport — 9 September 2026

This packet is a pure CPU transport and read projection, **not completed D3**.
No production worker/domain/UI or provider is wired by this packet. It is
frozen for a separate independent review; that review is not yet accepted.

## Contract and limits

- Full original Event/Base/FeatureVector, all inspected observation and
  preprocessing refs, actual resolved effect/approval envelopes and exact B3
  result are closed inputs. No caller comparison, price or `verified` flag.
- Existing B3 key additionally binds a typed input-descriptor digest, explicitly
  not a source receipt, separating reference roles and old payload versions.
- Only B5 roster-v2, B7 winner/serve load-v2 and C2 rotation-v1 laws are connected.
  Whole owning Event/Base feature references are checked even without an effect.
- Absent/inapplicable/unapproved effects preserve exact baseline. A numerical
  failure is separate from corrupt identity, which is rejected.
- Full numerical replay is worker/audit work. Card reads verify the immutable
  key-and-payload reference and exact selected distribution without re-solving
  the C2 Ridge reference or any context comparison.
- Embedded canonical envelopes prove transport identity, not real source/fit/
  empirical truth. Actual B1/A1/D2 resolution and D4 replay remain mandatory.

## Owning RED/GREEN evidence

Permanent tests include real B5/B7/C2 pure comparisons, exact synthetic approval
scope, no-effect full-event binding, malformed approval/payload types, altered
key/params/markets/deltas and typed reference-role changes. Rehashed comparison
forgery fails numerical replay. Synthetic approvals are explicitly transport
fixtures, never a real worker or D2 decision.

- Original transport implementation: 32 tests passed; reference-role collision
  then reproduced RED and was fixed using the explicit descriptor.
- Owning C2 dispatch/full-event/bad-approval seam: 8 RED / 8 GREEN before fixes,
  then 49 transport tests passed.
- Real C2 reader spy reproduced an unintended Ridge replay: 1 RED, then 265
  transport/copy/B3 tests passed after separating light reads from full audit.
- Malformed consumer payloads: 10 true RED cases. One additional first-run
  fixture error used a nonexistent runtime constant and was corrected before
  production code; the actual B3 stored-roundtrip then passed unchanged.
- After typed read-boundary fixes: **276 passed, 4.31 seconds** across 61
  transport, 33 copy and 182 B3 cases. Actual SQLite roundtrip proves one B3
  CPU callback and detached two-consumer reads, not upstream provider once-only.

Final source SHA-256:
`2fa1c380e30eece0ff8da6db2ec28b0743a717ef217b23f2a3d6a5236662f205`.
Permanent-test SHA-256:
`805fb2b31004dc5bdf671e77a80efc6b893039027d8071f4586ee7cb5db79e88`.

The earlier full 4323/18/97 run preceded the final read-path refinements and
does not certify these final bytes. The new full run on these frozen bytes,
`context-transport-final-full-20260909-01`, completed with **4351 passed,
18 expected platform skips and 97 subtests passed in 251.70 seconds**.
Independent review remains required.
Actual both-tab integration, legacy calibrated baseline capture, C1/C3/C4
adapters, native Tennis identity, browser, production and empirical acceptance
remain separate outstanding work.
