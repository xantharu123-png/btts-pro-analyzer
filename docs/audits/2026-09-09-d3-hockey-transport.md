# D3: Hockey law through the full stored context envelope

2026-09-09. Root baseline `0964dd1c1ea43bc19f922fedf339abfeb3f54c31`.
This adds the reviewed C3 family to the existing pure D3 transport; it is
not a native NHL acquisition path, a real D2 approval, the complete automatic
worker connection or UI deployment.

The explicit pair `ice_hockey:regulation_goals` /
`hockey-observed-exposure-load-v1` binds the existing owning full Event/Base
reference with no preprocessing contract. Comparison calls only the actual
`apply_hockey_effect`; B3 retains its independent whole-lineup conditional
restriction. No repeated distribution formula or caller comparison callback.
Actual source/FV/effect/approval resolution remains the worker's obligation.

## Actual RED and correction

The first full-envelope test discovered an owning export mismatch before
the missing family dispatcher: `_legacy_json(fitted)` left NumPy float64
objects inside `fit.coefficients`. The generic B3 finite-JSON contract
correctly rejected these internal objects. Old bare result tests did not
transport the full original reference.

The correction is confined to the known optimizer coefficient list in the
owning export. It now emits identical Python floats. Canonical JSON bytes
and exact binary float values are explicitly compared with the previous
projection and actual unchanged optimizer. The generic finite-JSON
validator, old fitting algorithm, probabilities and OT computation are
unchanged. No broad JSON coercion or tolerance is introduced.

Original nine new tests: **6 RED / 3 GREEN**, 3.24 s, all failures from the
actual NumPy payload boundary. The family connection and owning scalar fix
then passed the 156-test focus. Two more explicit tests verify the unchanged
optimizer/canonical bytes and real B3 SQLite reuse for two market readers.

Final focused integration: **550 passed, one expected Windows symlink skip,
39.04 s**, `.pytest_tmp/transport-hockey-final-20260909-01.xml`. Includes
all C3 tests, all D3 transport tests, unchanged prematch and B3 tests.
No whole-project post-change result is claimed by this focus.

Eleven new cases cover unavailable/experimental/applied CPU roles, actual
conditional lineup staying experimental, fixed original OT, full identity
and preprocessing binding, source object immutability, no Poisson refit in
market projection, true B3 once-only storage and owning replay rejection of
a rehashed promoted conditional result. Synthetic approval envelopes are
test fixtures, never actual D2 verification.

## Frozen source/test bytes

```text
153aadec4306ae726fd1283249ca3cd9589ed87001b475212d0979aa654e1816  context_transport.py
5b27b2fc84c354e87e7842e5191658f9aea9f0213f4a9e5e2136270c4f1280d4  context_models/ice_hockey.py
cc28f733b8370ca363b73ae7a534895e5b4fd0abef55140249334b35d31f0a37  tests/test_context_transport_hockey.py
```

Read-only card projection proves exact identity against its stored reference,
not a new source or empirical calculation. Full owning replay is necessary
for numeric/conditional audit. Upstream acquisition and joint feature
preparation are not covered by the SQLite CPU-once claim.

Independent follow-up review and post-merge full regression remain separate.
No price/ranking, Cricket, money, 15K, ticket, historical prediction, provider
request, main publication or VPS mutation belongs to this packet.
