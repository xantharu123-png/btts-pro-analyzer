# BetBoy N1Bet Importer

1. Open `chrome://extensions` or `edge://extensions`.
2. Enable developer mode.
3. Choose `Load unpacked` and select this directory.

The extension reads visible sportsbook odds only. It does not read credentials,
does not bypass blocked pages and never submits a bet.
