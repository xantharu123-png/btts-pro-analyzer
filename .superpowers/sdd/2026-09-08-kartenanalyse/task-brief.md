# Erklaerbare Wettfinder-Karten (user request 2026-09-08)

## User intent

The user explicitly rejects the displayed internal checklist (H2H checked/no veto, injuries list/impact not modeled, weather checked, lineup confirmation pending). They want a short explanation of why this specific selection is considered, with a useful counterargument. Main reasoning must be visible without opening an expander. This is an additive scoped UI/data-projection repair, not the entire twenty-task context-model project.

## Scope and authority

Base8c3df7978bf074cfe6ce3d9a794f6f60139327ea; own worktree C:/Projekt/BetBoy/betboy-app/.worktrees/erklaerbare-karten-20260908, branch codex/erklaerbare-karten-20260908. Separate from unfinished A4 in sibling worktree. User authorized implementation, commits/push/deploy; controller owns review, push, main merge, VPS and real browser verification. Do not access providers, VPS, accounts, or runtime databases. Do not dispatch agents.

Implement using TDD in focused modules: new small pure `forecast_analysis.py` (if useful), `wettfinder_surface.py`, minimal `app.py` rendering/style seam, `ev_signal_sources.py` transport, `wettfinder_automation.py` price-free evidence projection, and focused tests. Do not modify challenge_engine.py, riskobet behavior, Cricket, numerical model/selection/validation/price/account/ticket/settlement policies. No generation API/LLM calls or extra scans on UI visits. No reconstruction based on bookmaker prices, names, or unsupported human-sounding stories.

## Concrete findings / real sample

Controller read current source and a bounded exact Porto row from the VPS artifact at2026-09-08T15:48:05.182186Z (artifact generated15:37:17Z). Fixture1635654/candidate1635654:RESULT_HOME, FC Porto vs Manchester City, kickoff2026-09-08T19:00:00Z, probability0.46361, model_scope cross_competition_provisional_forecast. Canonical discovery_candidate in the same document has expected_home_goals1.527, expected_away_goals1.133, venue_samples[12,12], form_samples[6,6]. The projected model_candidate loses these numeric explanatory fields. Context was observed14:37:34Z: reported home_missing8/away_missing2, impact_assessment_completeFalse, probability_integration.appliedFalse, lineups pending, h2h matches0, weather passed. No injury effect or weather advantage is proved. This is source output, not independent verification that the fixture or model is correct. Use anonymized fixtures for broad tests; an exact numeric regression is appropriate.

Current defect: app._render_wettfinder_card_actions creates expander Analyse anzeigen and writes card.context_label; that is only signal.context_summary from challenge_engine.candidate_context_summary. _top_card_markup also renders the technical context line; compact rows need an immediately visible concise analysis too. ModelSignal has context_evidence, but both automated builders currently discard football context/evidence. A backward-compatible explicit analysis evidence field can be added with strict narrow projection. Keep internal summaries intact for diagnostics but stop rendering them as user analysis.

## Acceptance

1. A deterministic, escaped, price-independent 2-4 sentence short analysis visible on both featured and additional cards (not behind Analyse anzeigen). Headline e.g. Warum diese Auswahl? with one compact caution. No copied raw provider reason/detail/status jargon. Do not duplicate the same text below a card and in its markup.
2. Specific numerical model basis where recorded: home/away goal expectation, relevant total/team rate or separate corner/card expected_unit, exact market contract and sample context. Do not call Poisson expected goals observed xG or predicted final score. Do not transfer goal reasoning to corners/cards or winner reasoning to totals. Both binary sides and home/away reversal handled correctly; unsupported/missing data gets a short honest fallback, not a hidden row. No blanket market bans.
3. A probability below50% is not an overall likely win; don't claim strongest of three without the other outcomes. A RESULT_HOME0.46361 has53.639% for draw/away combined. State that counter-risk succinctly. A domestic cross-competition model must retain a plain-language limitation about league-strength transfer; no claim of directly comparable leagues or validated UEFA superiority.
4. Explain observed injury/lineup uncertainty only from a narrow typed projection with actual context clock; unchecked/missing/stale/partial must not become healthy or an advantage. Observed absent-player counts are not medical diagnoses or learned effect sizes. When integration.appliedFalse, do not attribute the win probability to injuries, weather or lineup. Do not display raw player-name lists or the old 10-unmodeled jargon. Keep one useful short caution, not four checklists. New context-effect enrichment remains the separate B/D project.
5. Persist price-free explanatory facts during normal candidate projection, retain original modeled_at/input_cutoff_at. For older artifacts without those facts, a safe exact-bound join to the same document's raw discovery candidate may be used only if candidate/fixture/native teams/market/kickoff/probability match unambiguously (no names-only or first-match join; conflicting duplicate candidates decline enrichment). Never borrow another revision/event/market's model basis. Alternatively leave a honest fallback until the next ordinary refresh; state that limitation. No additional source requests.
6. Tests: original technical checklist no longer reaches public card markup; exact Porto numeric case; probabilities/ordering/price states unchanged with missing/changed odds; other markets/sides; misbound/duplicate/NaN/bool/negative evidence; HTML hostile text escaped; stale context not used as current; missing evidence truthful; no extra scans; featured+compact render integration. Adapt only tests that encode the deliberately replaced UX contract, preserve semantic checks.
7. Focused TDD while iterating, full suite once on stable code before commit. Interpreter C:/Projekt/BetBoy/betboy-app/.codex_test_venv/quality/Scripts/python.exe; -p no:cacheprovider and unique .pytest_tmp child. New worktree pinned stage_runtime_databases.py may be Git-marked modified due raw LF normalization but has zero content diff, SHA2561441158c542e97a19b193fa0cd091b645ec6442d6d8157f1d4fceabbba72b026: never stage/rewrite it.
8. Self-review and scoped commit (no blanket git add). Detailed report at .superpowers/sdd/2026-09-08-kartenanalyse/report.md: exact changes, RED/GREEN commands/output, full-suite status, files, any unresolved proof. Controller review and rendered browser evidence required before production claim. Do not assert profitable bets or complete context-model integration.

Raise concrete schema/identity ambiguities before broadening. Preserve inherited evidence/release boundaries. The old superpowers package is unavailable on this account; follow this already approved task/review workflow without reinstalling it.
