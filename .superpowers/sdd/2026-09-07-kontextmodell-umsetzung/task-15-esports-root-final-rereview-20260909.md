# C4 Root final scope rereview and integration — 9 September 2026

PASS for the owning C4 source/model mechanics after `3eb87ef51d19e0a67ec2359f0c9f5f5fa20b04d3`.
This is not native data coverage, a D1/D2 empirical result, live context use,
rendered acceptance or production deployment.

Root read the complete narrow R1c diff, all fifteen new permanent cases,
complete new audit and earlier second independent finding, with surrounding
whole-event selection, scope filtering, observed counts and recovery assembly.
The patch reuses the existing canonical event/scope identity, excluding only
status as before; the separate native lifecycle checks stay intact. A full
new-scope fact can still retire its old scope. A partial replacement cannot
turn missing activity into a known empty window or an older exact rest time.
No source field, old receipt, arbitrary healthy value or empirical rule changed.

Independent Root command in the frozen C4 worktree: quality Python `-B -m
pytest -q -p no:cacheprovider`, six owning C4 files plus all five unchanged
independent probe files. Basetemp `c4-scope-root-rereview-20260909-01`.
**305 passed, no skips, 143.54s**: 197 permanent cases plus the original
67, subsequent10 and final31 independent probes. No assertion/source edit
occurred during this rereview. The owner's4220/18/97 full result is prior
evidence, not a new full Root run.

Accepted C4 was then merged into Root as `3c14e3d`. Conflicts occurred only
where C2/C3/C4 added closed family routing in `contracts.py` and
`context_snapshots.py`. The resolution retains all three owning branches,
all original C2/C3 validations and C4's exact complementary series law; it
introduces no common fallback and removes none of the predecessor checks.
The resulting diff against Root contains exactly C4's original 26/14-line
contract/snapshot additions plus the already reviewed owning files.

Root post-merge focused regression across contracts, B3/transport, all C2/C3/C4
tests: **1034 passed, 1 expected Windows symlink skip, 184.50s**. JUnit
`.pytest_tmp/c4-root-merge-integration-20260909-02.xml`, SHA256
`a8618fb621382f72a6971ec5587bc642b386f9cc752d4dc776c609aa010e320a`.
The first attempted selection named two nonexistent test files and collected
nothing; this is a disclosed command setup error, not a successful test run.
The actual existing filenames were selected using `rg --files` and the entire
corrected selection ran on a fresh basetemp. No numerical test was skipped.

All three prior reports remain byte-identical in this SDD directory, including
their original change-request dispositions. They were not rewritten as green:

- original: `201130cf5ba5a009c98c08df0f59aa79759b1c9605b47035d9ee7017a1f3f722`;
- first rereview: `5eba9ee2120724513b64ff998a3acc2c6a12b4577c53aad0e579a9edcd6ca686`;
- second rereview: `8a96fb0f86c2645e01bbf86eb157b1da14f34d6dde8f7b954257620304e8e269`.

Protected stage helper, old model outputs, Cricket, price/ranking policy,
15K/tickets/ledger and production are untouched. C4 D3 transport, actual
worker sources, D1/D2 population and release still require their separate work.
