# P5b-A0 — final FootballOriginal callback, narrow prerequisite

Controller ruling,10 September2026. Existing approved context spec, B/D plan,
Variant1 and isolated-worktree consent apply; Cricket remains excluded. This
is a bounded prerequisite within P5b, not a claim to complete B3, D3 or the
user's injury/load effects. Start only after the other implementation writer
has frozen its source/commit. Use a fresh LF worktree from accepted Root code.

Read the preserved full `task-18-football-live-worker-preflight-20260910.md`,
the actual P5a `FootballOriginal`/builder, both actual Finder workers, and the
relevant approved B3/B4/B5 clauses. Original probe bytes are preserved under
Root `.pytest_tmp/p5b-preflight-20260910/`; do not overwrite or weaken them.

## Chosen interface and limits

- Add optional keyword-only `original_capture=None` to `scan_daily_challenge`,
  `_default_football_scan` and `_run_market_scan_worker` (retain existing
  positional compatibility). Only an explicitly supplied callable enables it;
  invalid callback input fails before source/model work. None retains the old
  lower-level call without new kwargs, clocks or provenance work.
- Forward to the single final `build_fixture_candidates` location, not UEFA
  feasibility/probe builds. The callback receives the existing immutable
  `FootballOriginal`, once per successfully executed final fixture calculation.
  Preserve the actual existing fixture deduplication behavior; no second fit,
  predict, calibration evaluation, history selection or GET to produce a copy.
- Both normal Finder worker wrappers may forward the supplied local sink.
  The existing scan and worker return dictionaries stay unchanged. No new JSON
  fields, DTO, global cache or retained mutable callback state. Do not place
  large original/history payloads in user, automatic, admin or audit JSON.
- No default capture without a receiving owner: silently collecting then
  throwing away objects is not useful persistence. A0 is opt-in plumbing for
  the next owning worker, not automatic production publication. Callback errors
  stay explicitly observable through the existing failure path; do not swallow
  them or relabel a partial run as completed. Missing source metadata in an
  original is not a new model/market exclusion.
- Existing 15K callers/IDs/tickets/probabilities/rounding/ranking/price and
  cross-competition rules remain exact. Do not alter its immutable model
  signature or introduce mandatory candidate fields.

## Required RED/GREEN and acceptance

Use the owning real scanner and both wrapper paths with synthetic responses
through existing tested native/capture machinery; no live provider requests.
Count final model invocations, calibration calls, source GETs and captures.
Assert all final unrounded/calibrated markets and actual capture/receipt clock
ordering, no second model and exact parent results with/without the observer.
Include UEFA probes, no-model fixture, invalid callback, callback error,
default mocks/call signature, source incompleteness and JSON non-leakage.
Independent tests must examine the callback's real object, not a reconstructed
object from rounded candidates or a stubbed claim of the intended value.

The original preflight default-worker missing-capture/B3 tests remain their
original witnesses: an opt-in seam alone does not turn default production
capture or B3 publication into a completed feature. Record that limitation.
No original failure may be silently reclassified as green by changing its
setup to supply the new sink. New opt-in tests have their own RED/GREEN record.

Allowed source scope: three named scanner/worker files, narrowly needed
imports, new focused test file and one owning audit. Do not modify the pure
P5a capture, formulas, generic B1/A1/B3/D4 schemas, RiskStore, deployment,
source budgets, keys or protected stage helper. No full suite/push/VPS.
Commit a frozen focused packet, supply precise evidence, then independent
review. Root remains owner of any integration and subsequent deployment.

### Narrow parity-test accommodation, 10 September 2026

The existing whole-module AST comparison in
`tests/test_football_original_parity.py` predates this approved scanner seam.
It may be extended only to recognize and assert the exact authorized scanner
parameter, early callable validation and final-builder forwarding additions,
then strip those known additions for the existing parent/closure comparison.
Do not skip the whole scanner, discard unknown nodes, repoint the old parent,
or weaken the engine/closure assertions. New A0 tests must independently
compare all three actual worker functions against their `887cb47` parent
blobs and reject an unrelated change. Preserve the original RED witness and
document the exact test accommodation in the owning audit.

## Explicit next boundary (not authorized implicitly by A0)

Durable live-original publication needs a separately closed calibrated-live
original type, exact native event/schedule/source binding, distinct original
and context clocks, preserved calibration/legacy-marginal semantics, and its
own D4 read/restore support. Then B4 features, D1/D2 scope and once-only B3
both-reader publication can be wired. A raw zero-effect B5 distribution must
not replace a calibrated legacy marginal or be called an injury contribution.
CANC/1H native capture, historical exposure, real fit/200-event evidence and
actual injury/load application remain open. This brief fabricates none of them.
