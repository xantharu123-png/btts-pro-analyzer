# Independent P6a review — bounded mechanics PASS, restore boundary retained

10 September 2026. Frozen worktree
`C:/Projekt/BetBoy/betboy-app/.worktrees/kontext-p6a-esports-original-20260910`,
HEAD `c28ae1ef70fbec51171cc3f3ae370a89db7a91ee`, parent
`6d03ca00c487295a9a336e5917c314762e8ec637`.

## Disposition

**PASS for the scoped same-call capture and Shadow logger correction.** The
two unchanged original functional REDs now pass. No new qualified correctness
finding was reproduced in the bounded independent checks. This is not an
unqualified deployment or old-C4-runtime compatibility approval.

The documented C4 whole-file-code identity boundary is **real and independently
reproduced**, not assumed resolved by unchanged mathematical outputs. An old
native reference still renders its stored markets exactly, but its full Base
and D3 numerical replay under the new source fails the strict old code hash.
The existing D4 reader honestly reports transport-only/incomplete for this
unsupported family; it does not certify a successful owning replay. Root must
retain that integration/restore limitation rather than whitelist hashes or
rewrite an older immutable forecast. No production C4 inventory was inspected.

Own runs: **382 focused tests / 26 subtests passed**, the **two unchanged
functional original probes passed**, and **32 newly authored independent cases
passed**. Zero skips/deselections. One explicit restore case was rerun with
visible output for its exact identities; it is not counted as another unique
case. No complete repository suite was run or borrowed from the author/Root.

Only new ignored tests/report, unique XML/temp databases and isolated exact-parent
source fixtures were created. No existing source, owning test, original witness,
Git ref, provider, production data or VPS was changed. All six packet files,
the original preflight/probe/RED XML and frozen HEAD were rechecked unchanged.

## Personally read contracts and source

The complete approved context-model specification, complete C/D implementation
plans, Task 15/18 briefs and their controller rulings were read, along with the
full P6a preflight and unchanged 13-case original probe file. The original D3
E-Sport review was read for the known light-read/full-replay/source distinction.
The full 191-line P6a audit, complete new 305-line transport, full 371-line new
tests, changed Shadow test and exact source/test diff were read. Existing Elo,
actual candidate/selector/logger, C4 export/reference validation, D3 read/full
paths and the actual D4 capability branch were inspected directly.

The broad goal remains learned, causally qualified context, but this authorized
packet is deliberately earlier: capture actual existing unrounded calculation
values once. It must not claim a native source/roster/patch/empirical effect,
replace the legacy post-IID value, change its heuristics, reinterpret BO7 as
qualified C4, or rewrite old financial/forecast history.

## Actual once-only worker and numerical parity

- The original `test_required_once_actual_log_predictions_has_no_second_elo_calculation`
  and `test_required_once_real_scan_runner_reuses_candidate_elo` were executed
  directly at their unchanged Root absolute path. Both reach actual logger/
  runner/SQLite. Their original-code-object profiler counts across imported
  aliases; replacing one alias or merely comparing rounded output cannot satisfy
  the assertions. The scanner double supplies only local synthetic responses.
- The original historical pin/current-two-call witnesses were not changed or
  misreported as present-day passing tests. Only the two named functional
  requirements are rerun against the correction; all original source/XML bytes
  remain preserved.
- The replacing permanent Shadow test now observes the actual Elo code object,
  selected 20-row histories, actual raw indices and unrounded returned Elo bits.
  The old alias-only test remains in the original Git blob and preflight.
- A new independent multi-record batch uses two different valid events, one
  revised duplicate ID, one insufficient-history event, live input and null.
  The actual new worker makes four candidate/selection calls, three Elo calls
  and **one batch clock read**, with two inserted first observations. Its entire
  real SQLite row set equals the separately executed actual Git-parent logger.
  A duplicate is still calculated before `INSERT OR IGNORE`, as in the old
  contract; this packet does not claim a new cross-batch calculation cache.
- Twenty independent primitive cases compare actual parent/default law with
  capture for ID/score/stats/history-BO/history-time values represented as
  strings, floats, booleans and opaque Decimal/datetime objects. Computable
  originals retain their candidates; existing unavailable cases remain so.
  Nontransportable data is explicitly marked and not treated as replayable.
- Five independent offset cases preserve the actual raw begin/end strings,
  original lexical ordering and canonical actual decision. Replaying the
  complete projection matches the actual captured values. The owning suite
  additionally covers equal-object/twice-same-object indices, duplicate series,
  exact microsecond cutoff boundaries and prematch/live BO1/3/5/7 parity.
- All original numerical helpers, unchanged two carried Elo passes, post-IID
  roundtrip, heuristic 150-point uncertainty and five-percentage-point margin
  retain their owning meanings. They are not new measured roster/fatigue
  effects or a proven individual probability lower bound.

## Transport and integrity boundaries

The new immutable object owns canonical JSON bytes and returns detached dicts.
Raw event fields, stats and every raw history position are projected through
closed allowlists. Missing/explicit null/primitive types remain distinct where
captured; unsupported data is a typed marker, not arbitrary repr/price/provider
JSON. The selection is produced by the same stable indexed history pass, not
an equality search or second selector. Logger reads only the original Elo values;
it does not publish the full new object into a shared A1/B3 consumer snapshot.

Pure validation binds canonical shapes, hashes, raw indices, selected-side
percentage arithmetic and explicit unresolved source status. It intentionally
does not certify fresh Elo fitting or native truth. Five additional independent
mutations of Elo, consumed wins, direct series probability and raw indices are
rejected by explicit offline replay or earlier owning form guards. The owning
suite separately demonstrates that a self-consistent public-rehash Elo edit
may pass form validation but cannot pass the actual offline recipe replay.
Neither offline replay nor C4 native reconstruction runs in capture/logger.

The independently rerun owning tests retain closed types/unknown keys,
noncanonical Unicode/NaN/infinity/large integer cells, canonical immutable
wrapper, unavailable source markers, price-invariance, no-IO capture/form
projection, SQL first-observation/settlement/racing rules and frozen Cricket
parity. No new model/price/ranking/financial gate was added by this review.

## Explicit actual C4 restore witness

The independent test obtains **actual `git show` parent bytes** for
`esports_elo.py`, `multi_sport_recommendations.py` and `context_models/esports.py`.
It executes these modules in an isolated temporary runtime, with normal imports
temporarily resolving to those exact modules. C4 reads its real byte hashes
from those fixture files. There is no patched `_CODE_HASHES`, free `verified`
flag or fabricated old reference.

Under that real old runtime, the existing synthetic native fixture builds actual
B1 SQLite records, the old source-bound original Base, features and a no-effect
D3 payload. Actual `compute_once` stores it in the same temporary DB. Old full
Base/D3 replay passes before leaving that runtime. Then the current unchanged
validator, projection and actual `verify_context_database` inspect the same
objects/database under the new code:

1. Current Base validation and full D3 replay reject with the real
   `original Elo code/version/constants differ` condition.
2. Both historical winner-market projections remain bit-exact using their
   original consumer reference. No fit or original probability is substituted.
3. D4 returns `verification_level='transport_only'`, limitation
   `d3-owning-family-replay-unavailable`, no empirical approval and no active
   manifest. Actual inventory: **81 contents / 81 observations / one snapshot /
   zero artifacts / zero manifests / zero rollbacks**. The unsupported C4
   capability is not silently upgraded to source/numerical verification.
4. The complete database bytes and stored original code-hash dict stay unchanged.

Actual hashes in the witness:

```text
old multi_sport_recommendations.py 80c545cf82a577bd4d1540bbd2619a12171662781373161d6db4c80ba38615c5
new multi_sport_recommendations.py 7430075ff384a13af34fdfa44a796daae41f40016aecf1517ed21cb2837239b1
old and new esports_elo.py bd431e06926a319a68d5803749771595bbf72bb8561d39da2b8ee841d2af7140
```

This isolates the additive multi-sport-file hash change from unrelated CRLF/LF
differences. The old Root preflight had a different physical Elo hash; no
cross-platform or same-Git-text byte equality is asserted. Any eventual full
old-recipe support requires its own reviewed version-bound implementation or
explicit unavailable result, not retroactive hash replacement. Existing
production inventory, trusted deployment/backup preflight and activation
disposition are Root's separate tasks.

## Exact independent run ledger

Existing quality Python, `-B -m pytest -q -p no:cacheprovider`, explicit Python
paths, unique basetemp/XML and actual `$LASTEXITCODE` propagation throughout.

| Run | Actual result | Scope |
| --- | --- | --- |
| `p6a-independent-original-01` | 2 passed, 1.71s, exit 0 | Only the two unchanged original functional REDs by exact test name. |
| `p6a-independent-focus-02` | 382 passed / 26 subtests, 108.17s, exit 0 | All twelve files selected by the bounded owner-disclosed glob inventory. |
| `p6a-independent-attacks-03` | 32 passed, 5.45s, exit 0 | New independent primitive, actual batch, clocks, rehash and real parent-C4 restore cases. |
| `p6a-independent-restore-04` | 1 passed, 3.64s, exit 0 | Same unchanged restore test repeated with visible exact code/D4 output. |

There were no pytest setup failures, assertion edits, new REDs, xfails,
deselections or Windows skips in this independent P6a task. A mistaken plan
filename during reading was resolved from the actual file inventory; it was
not a test or source defect. Truncated combined reads were completed with
smaller source/document ranges.

The twelve focused files are `test_riskobet_candidates.py`,
`test_multi_sport_recommendations.py`, `test_ev_signal_sources.py`,
`test_esports_shadow.py`, `test_esports_live_original.py`,
`test_esports_context_scope_revisions.py`, `test_esports_context_revisions.py`,
`test_esports_context_retractions.py`, `test_esports_context_model.py`,
`test_esports_context_corrections.py`, `test_esports_context.py` and
`test_context_transport_esports.py`. These include C4 lifecycle/math/B3/D3
regressions, but no fresh empirical or provider evidence.

## Frozen raw SHA256

```text
context_models/esports_live.py 6fa58ed4fc31ff3a0ea85403a997468f8cdec888dc89654f0d3f8673d624ac93
multi_sport_recommendations.py 7430075ff384a13af34fdfa44a796daae41f40016aecf1517ed21cb2837239b1
esports_shadow.py f911bb17cc0bff0970d113e04521a45295083d1f487e7ccc20bdfa1be7279bf9
tests/test_esports_live_original.py 2258403afebc07e86f023cb3e41b6fcb4fae2a9f3734d20dceff32f148c900aa
tests/test_esports_shadow.py cb11305dc30f8ba462c33ab2f616816e2093a123bcef98bb7bfc1f2e0ae6b4ec
docs/audits/2026-09-10-p6a-esports-original.md dfa939188f1e6b27312fba3c959280d3a3c6d9dbdbe28c0e36acd073c2b0ed17
esports_elo.py bd431e06926a319a68d5803749771595bbf72bb8561d39da2b8ee841d2af7140
```

Unchanged Elo Git blob: `ca390bc51976a5ce598c9a2a952568cd25439ca2`.

New independent evidence, relative to this worktree:

```text
.pytest_tmp/p6a-independent-20260910/test_independent_original.py 40a4e215a978c5345ab462675c168791060924f111f8e6cd82112fe74321add2
.pytest_tmp/p6a-independent-original-01.xml d2d19d41146f164f5bceeb090b9e276020070f7697c1a32ab5ec2b975fa3e130
.pytest_tmp/p6a-independent-focus-02.xml 71a0aee74c8814fd3cc4b79833d3cc611a2ddfd189d7a7ff0b5cd41973c3aa74
.pytest_tmp/p6a-independent-attacks-03.xml 12097723476252a0fd7db25bb45973b0e1a80dcbc37f4499a27687d1be3ba684
.pytest_tmp/p6a-independent-restore-04.xml 7a7e0a4628901eaa01afdd99c25e5a460ec947fa1dc9e8e07071b716d08210e4
```

Unchanged original Root QA evidence:

```text
PREFLIGHT.md 5634964adcc6472d76439d04fc0855329c2faa30b7713dd9729177b61c20092b
test_same_call_preflight.py c0ccceda30499dd48a9283514a60c2ab7397934ea362c8ec1dd531d4cb69d7eb
red-01.xml a07fd5b2fbf3360090b6a4e638ddf644e56604becf0830e8e8c30e4d86fca9f0
```

Report hash is delivered separately. Frozen source and probe bytes are not
normalized or subsequently altered. Shared durable consumer/worker publication,
native whole-status capture, qualified B1/C4 binding, real D1/D2, full D4 and
live release remain separate. This bounded PASS does not complete P6 or the
user's overall injury/context and betting-quality request.
