# C4 implementation rulings — controller, 9 September 2026

The controller read the complete approved Task15, C2 rulings and actual-source
preflight `ad94e352cac1a46a0b8238efaa8b1666158daa184e1d79171c1feff54955afa7`.
This closes the bounded CPU implementation decisions, not source availability,
empirical activation, provider budget or production acceptance.

- Own family `esports:series:winner`, parameter `p_a`, one B2 logit `winner`
  head; exactly `series_winner_a` and `series_winner_b`. Initial formats are
  `series_best_of_1`, `series_best_of_3`, `series_best_of_5`. Require explicit
  native prematch 0:0, normal unhandicapped/non-forfeit rules, title, native
  competition, season and participants. No new Event fields or default BO3.
  A winner effect does not certify maps, exact scores, handicaps or live states.
- Additive original-base export at the actual unrounded calculation point:
  retain the existing post-IID-roundtrip winner bytes, not a substituted direct
  Elo value. Include the exact native receipt inventory, two ordered team
  windows, deduplicated subgraph, unchanged Elo constants, carried second pass,
  code version/bytes and exact replay. Native causal title/season evidence is
  required for the new reference; the legacy default remains unchanged when
  it is missing. A rounded Shadow card is not the original base.
- The roster reference is explicitly **observed equal-series participation**
  over each team's unique actual contributing native series in that replayed
  subgraph. Count each series once, not once per Elo pass or both input lists.
  This is a measured participation reference, NOT analytical Elo influence
  weights or a decomposition of Elo. Bind that definition/version to the base.
  Current-minus-reference participation, then signed A-minus-B, uses the exact
  ordered title/season/player vocabulary. Its residual effect is learned by B2.
  No healthy hypothetical roster, guessed negative-weight normalization or
  implicit zero for missing players/lineups/history. A source-complete measured
  lineup is distinct from a current explicit scenario; a team roster is neither.
- Use a closed INTERNAL native transport, clearly not a PandaScore adapter.
  Own whole event/participant/title/season revisions; preserve participant
  lineage through real B1 selection and do not stitch partial rosters. Separate
  observed historical lineups, explicit current candidate/confirmed lineups,
  series completions, individually identified map completions and reported
  patch/veto state. Unknown patch remains unknown; no guessed latest patch,
  50-percent stand-in mixture, physical injury or medical fatigue claim.
- Observed series/maps and rest use 1/3/7-day half-open UTC windows with actual
  completed end times. Best-of and series score are not individual map records.
  Real terminal receipts may prove a strict upper bound on end, never an exact
  end or active map duration. Retain conflicts and old participant proof;
  bound-irrelevant histories may be ignored only strictly before the particular
  window/latest known end, with proof refs. Incomplete coverage stays explicit.
- `esports-context-reference-v1` binds the complete original Event/Base and
  sorted actual preprocessing refs; effect application has keyword-only Event.
  Use existing strict B2 offset/parameter arithmetic and actual fitted synthetic
  binomial residuals, one row per series, not a new constant/estimator.
  A neutral effect preserves all original parameter and market bytes.
- A narrow esports-only B3 comparison envelope may bind original Event/Base,
  FV and actual externally resolved A1 effect, following the strict owning
  C2 shape. No generic callback/verified flag, new database or approval bypass.
  Without matching D2 evidence, used parameters/markets remain original.
- The old flat training-row sketch is superseded by owning D1 case/outcome/
  identity assembly. The initial packet proves actual fitting/application on
  synthetic cases only. Native source-qualified D1/D2, scorer and real frozen
  cohorts remain explicit follow-up integrations, not a second training path.
- No provider request, budget/tariff change, live worker/UI wiring, old forecast,
  money/15K/settlement, Cricket or deployment-helper change in this packet.
  Preserve exact old default outputs, including frozen Cricket parity before
  any optional export change. Record missing real roster/patch/map-time evidence.

Require real RED/GREEN, actual B1 SQLite through B3, whole-revision and causal
boundary negatives, signed fitted effect/mirror/extreme arithmetic checks,
scope/format/source mutations, old-default parity, full regression, an exact-hash
audit and independent review before merge. Software acceptance is not D2 approval.
