# D3 byte-copy fix — independent re-review, 9 September 2026

**PASS for the bounded D3-R1 correction. No new finding.** This is not complete
D3 integration, source/empirical approval, general Root-WIP acceptance or VPS
release permission.

Exact target: clean worktree
`C:/Projekt/BetBoy/betboy-app/.worktrees/kontext-d3-bytefix-20260909`,
commit `1a9fffa43f3315c5616254d2cb7101310c10078b`.
Only the three intended committed files changed. The reviewer did not edit
target source/tests, Git state, providers, runtime databases, workers or VPS.
Own copied probes/report are confined to this ignored review directory.

Read the complete new correction audit, all 20 new permanent cases and the
full source diff against the already-reviewed transport. The correction binds
canonical copies of original result inputs, the role-selected used parameters
and markets, and the complete recomputed delta mapping. It neither changes
generic B3 tolerances nor adds a fit to the lightweight read. Original JSON
numeric representations remain original; only contradictory copies fail.

## Genuine unchanged regression evidence

Both independent original probe files were copied byte-for-byte into the same
relative `.pytest_tmp/d3-transport-independent-20260909` depth in the FIX
worktree. SHA256 was checked against the original review before execution:

- `test_independent_transport.py`:
  `7720e8fc0206d0288451bafe3790d78e2be7a101ba3a2b592745e6f76f36700c`.
- `test_transport_controls.py`:
  `cd794a7b6ff2b8fd05890a2041bdd2678a2b08cac4549db334820e3679120530`.

The old 6 RED cases are all green, with no test assertion adjustment. All 59
original independent cases plus the old 61 transport, 20 new byte-copy, 33 copy
and 182 B3 cases produced **355 passed in 6.38 seconds**, no skips. Evidence:
`fix-01.xml`, SHA256
`7b80a4f1fe705b7e7d1fd6a80ecb7770502cfbab6275c8b86a7e65592d1999a1`.

Five additional independent controls passed in **0.61 seconds**. They assert
that the actually imported transport is physically in this FIX worktree, not
the original Root; equivalent dictionary order remains equivalent canonical
bytes; and the light card projection cannot call the full Base validator.
`test_fix_scope.py` SHA256:
`ad1815c343bb648c3c93cfce8e9b7bfc50dd90d9de927efaffeaab5739e63742`.

The unchanged probe packet also rechecks actual B3 SQLite storage failures,
typed endpoints/None/nonfinite values, four concurrent threads/eight calls to
one CPU comparison, old consumer reference binding, full-law rejection of a
rehashed comparison, foreign effects and the explicit C2 no-fit/no-source read
boundary. The combined independent evidence is **360 passing tests in two
explicit runs**, not a new whole-suite claim.

## Frozen target identities

| Target | SHA256 |
| --- | --- |
| context_transport.py | feff952c7f8a7d769d7a20e25b9d164f9dd0d9470446b9d647e9aebb52e187ce |
| tests/test_context_transport_bytes.py | 1481cb2b7f190bfe9ffa2501363029c7e973ed49bb70ebd9034aa0a1a668e00c |
| tests/test_context_transport.py, unchanged | 805fb2b31004dc5bdf671e77a80efc6b893039027d8071f4586ee7cb5db79e88 |
| docs/audits/2026-09-09-d3-transport-byte-correction.md | 72bd8025c783a4453b2f5ca1f472fb0f836e68b2e159925b74d12ac9b93197f4 |

These target hashes and clean Git state were rechecked after the tests.
All source payloads/effects/approvals used by this pure-transport review remain
synthetic mechanism fixtures, not real A1/B1/D2 source resolution. Rehashed
arbitrary but well-formed comparisons still require full numerical replay;
the light reader is deliberately not empirical/source authority. That boundary
is preserved, not newly relaxed by this fix. Production integration, native
source/fit replay, actual approval resolution, D4 and browser acceptance remain
separate controller-owned work.
