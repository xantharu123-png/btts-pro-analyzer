# SDD ledger — plan: docs/superpowers/plans/2026-09-07-kontextmodell-umsetzung.md

## Manual input F1 accepted — 10 September 2026, approximately 11:26 CEST

Root74d714a integrates frozen C0c0a83f1 and F1acf6b8e. Independent rereview
addresses both actual UI findings, no new Important source regression. Its one
Important provenance correction is applied: C0 two-card evidence was AppTest,
not CUA. The new Root F1 browser report independently proves two cached manual
decisions, edit/clear/invalid-input isolation, visible initial100.00 and no
model/order change. Actual320px popover readable; both views1440→320 fit; all
six synthetic sport filters and separate Risk price-only overlay confirmed.
Author195bounded+18unchanged controls green; no full-suite claim for this target.

P4b3(a) resumed only after F1 writer froze; still separate unmerged work.
GitHub main/VPS lastverified2ba3931, no installed bridgeA or B deployment yet.
Protected helper/raw inherited WIP preserved. Next freeze an LF integrated QA
target, run full suite once, qualify the release delta, then installed A→B.

## Account continuation — 10 September 2026, late morning CEST

Existing approvals/spec/20-task plan are retained. Fresh Git checks confirm
Root/remote feature898652b; main and actual VPS remain2ba3931. No context
deployment, source reset or replacement of inherited WIP. Seven timers planned,
app/local/public health good; today's original combined Tennis job failed.

- Task19 follow-up: C0c0a83f1 original manual quote staleness is fixed in the
  actual completed browser rerun. Independent review reports two Important
  findings: fresh bankroll visibly blank, and invalid explicit checks omit
  checked-input identity. Original author owns narrow fix round1; C0 preserved.
  Both original review and Root browser report remain separate evidence.
  Ruling: replace only this bankroll number widget with a normally editable
  text input initialized to100.00; preserve finite numeric/min1 validation,
  explicit-empty=None, raw checked-input identity and existing evaluator/save
  rules. Streamlit1.59.2 couples the numeric default to clear behavior; the
  one-shot100/defaultNone workaround is false in the real browser. Cost:
  this field loses its increment/decrement stepper; no finance/model rule changes.
- Finder/Risk geometry and navigation checked at1440/1024/761/760/390/320;
  no document horizontal overflow. Synthetic six-sport filter roundtrips exact.
  Actual quote edits did not alter five model-card texts/order. Not real bets,
  empirical quality or production UI acceptance. See task19 Root browser report.
- Root integration of P4b2/P6a before account stop:526passed/26subtests,23.97s;
  XMLc6a62188 verified on resume. Fullf2 results do not cover C0/later packets.
- P4b3(a) remains isolated WIP; four authorized visibility/clock original probes
  now green, both durable-B3-ref probes correctly still red. Author edits paused
  during UI correction; self-review noted deduplication/closed-batch obligations.
  Not merged or included in the technical release.
- P5b native Football preflight completed and copied byte-identically:
  cafcac20,2 real missing-worker REDs/10 controls, unchanged16sourcehashes.
  Original capture and B3 final-worker connection absent; CANC/1H stored status
  lineage also absent while legacy direct-detail reconciliation still works.
  Calibrated legacy marginals must not be relabelled as raw B5 probability;
  zero-rate-change substitution demonstrably changes probability spuriously.

Next: source/protocol/browser correction -> independent scoped rereview ->
integrated immutable target/full suite -> approved installed A-to-B route.
Then continue P4b3/P5b and remaining actual source/empirical dependencies.
No new user approval request is needed for existing scope; no new coefficient,
historical receipt, source budget or empirical qualification is inferred.

## Accepted original/binding packets and browser release hold — 10 September 2026

Root codef058517 integrates P5a8691022+34b4a5a, P4b2ba73dc9+5a3d0c1, P6ac28ae1e.
Frozen technical B remainsf2f7611, separate clean tracked worktree. Its actual
full run is complete6255/20WindowsSkips/97subtests1354.13s, XML3217c046.
It does not cover the later P4b2/P6a/UI changes. Main/VPS lastverified2ba3931;
no new main push, installed A or context B. Existing user authority continues.

- P5a Root105/0 accepts all four unchanged original metadata REDs. Exact same
  scalar projection, no probability/calibration/fit or integer-limit changes.
- P4b2/F1 independently541focus+71qualified attacks pass; one previously proven
  invalid SQL-column harness case remains explicitly deselected and preserved.
  No new finding, no source/empirical/worker approval inferred.
- P6a independently382focus/26subtests +2original functional witnesses +32new
  controls. Actual logger/runner once-only Elo, exact parent SQL/float history.
  Actual old-C4 SQLite restore probe keeps historical cards readable but full
  old-code replay unsupported; D4 says transport_only, not full qualification.
- A655e7a6 exact updater-only bridge; B shares its ancestry. Independent updater
  contract277/0 passes. Original A CRLF pin-test failures remain, separate LF
  checkout83/7 passes without normalizing any protected file. Actual A→B not run.
- Real local B browser shows one Porto choice from two opposing raw directions,
  immediately visible analyses, stable empty-filter roundtrip, no card/order
  changes from confirmed1.12 or4.00. But changing the form quote before submit
  leaves the previous price message visible: qualified UI release hold. A new
  separate manual-price UI fix is authorized; no frozen B source alteration.
- BB/Hockey P4b3 preflight has real6RED/8controls: baseline/Risk calculation
  exists but normal catalog/B1/B3 connection and post-fetch clock are absent.
  Football P5b actual final-calibration/source/shared-worker preflight active.

New full/Root rereview and four independently authored original reports are
preserved under task-18/task-20 names. Technical work, usable data, D1/D2 effect
qualification and actual production activation remain distinct. Cricket,
money, old forecasts and protected helper1441158 remain unchanged.

## Real Linux DAC and complete Root regression — 10 September 2026, 00:20 CEST

Root/feature remote6d03ca0; latest GitHub main21:56UTC2ba3931, no context deploy.
Existing user authorization remains in force. Cricket remains excluded.

- Immutable Root full6d03:6168passed/20WindowsSkips/97subtests1206.62s, exit0.
  XML07cf9cd6, detailed report task-20-root-full-6d03ca0-20260910.md. This does
  not cover later P4b2/P5a/P6a. Every skip checked; no result inferred from WIP.
- Actual Linux root/betboy DAC PASS in new synthetic fixture5zxtb2ij. Real
  closed CLI0/2/1 results,12 denied operations with real positive controls,
  unchanged timeout/output bounds and full pre/post identities. Root read
  complete RESULT015e0300; raw large integers preserved byte-exactly.
  Detailed report task-20-linux-dac-root-run-20260910.md. No production/secret/
  unit/pin mutation. Installed trusted BridgeA→B is still a separate open test.
- P4b2ba73dc9 frozen with823 owning focused tests; worker agent now runs the
  independent prepared review. No integration/final approval yet.
- P5a8691022 Root original review4RED/71GREEN: nonrepresentable unused owning
  calibration sample metadata aborts optional capture, not the legacy model.
  P3 fix assigned to another agent in separate WT; original evidence frozen.
- P6a original logger/real scanner each execute Elo twice, actual code frames
  counted. Two functional REDs preserved. Narrow same-call original+logger
  fix explicitly authorized in separate6d03 WT; old Offline base_request
  replay and historical SQL/settlement/IDs/Cricket remain unchanged.

Remaining real worker/consumer/native-source connections and D1/D2 empirical
effect proof are not hidden by these technical approvals. Final browser,
main push and trusted two-commit deployment remain open. Preserve protected
stagehelper1441158 and all inherited WIP/output files.

## Accepted consumers/native captures and actual Linux run — 9 September 2026, 21:53 UTC

Root code `4f49a8c86e98d56127f5419d0b63b629997e0712` integrates consumerfix
`5a865e0`, P4a/F1 `e284e7a`, P4b1 `b2b7d34`, Linux fixtures `2346846`.
GitHub main/VPS freshly checked21:08UTC remain `2ba3931`; app/Caddy/localhealth
healthy, old Tennis combined-refresh HTTPError still not reset or repaired live.
Seven timers are present; NEXT=- during an active worker alone is not a defect.

- Shared Tennis consumer independently accepted: all67 original witnesses plus
  98 owning cases pass (165/0/0), then46 independent boundaries pass. Known
  ATP/WTA header contradiction, false-valued malformed-context fallback and
  generic untyped shape errors are corrected. Root broader680/1/26. Original
  report08724ef and independent rereview622ce38 preserved byte-identically.
- Actual BB/Hockey originals accepted after known-ID validation-order correction.
  Root242/0/0 in8.86s includes all eight original negatives. Original review
  988dd9c and new Root rereview retained. This still marks source links unresolved;
  a valid same-call original is not native/empirical source approval.
- Native existing-response capture P4b1 independently accepted:265/0/0 plus46
  qualified independent cases. One mistaken reviewer WAL-lifetime setup remains
  preserved and explicitly deselected; two actual WAL controls pass. Report
  0077a6a unchanged. Warm legacy cache is not converted into a new receipt.
- Actual unprivileged Linux test matrix on pinned archive plus only two positive
  fixture corrections:785passed, zero skips/errors/failures,55.61s, exit0.
  Ordinary umask0002, all source hashes unchanged. Original14RED/771GREEN preserved.
  JUnit7c77e9d; full Root report `task-20-linux-fixture-root-rerun-20260909.md`.
  This is not yet real root/app DAC or the installed trusted A-to-B transition.
- Active authorized subagents: P4b2 pure native-alias/raw-input resolver;
  P5a actual Football same-call original capture; local-only synthetic root/app
  DAC harness design pending Root source review before any privileged execution.

Remaining: real shared worker/normal consumers BB/Hockey, Football/C4 owning
original/consumer integration, external-source gaps and D1/D2 learned-effect
proof, final integrated suite/browser and safe main/updater/VPS release.
Cricket excluded; no historical forecast/ticket/account rewriting. Installed
stage-helper pin1441158 stays unchanged; unrelated outputs/WIP preserved.

## Accepted actual originals, D4 and updater correction — 9 September 2026, 21:01 UTC

Root code3dccf6b integrates accepted Domain53c9a88, Reader4011392, P3ccefe85,
Tennis actual32ac1d8+clockb566da3/9a9da36, D4live5d5bab6 and Hook7ba4c97.
Main/VPS last actual15:57UTC2ba3931; no context release. The known Tennis job
HTTPError remains a production issue, not cosmetically reset.

- Domain Root88/0/0 rerun accepts eleven original real failures; original and
  rereview reports already preserved68aff47. Reader233/1 integration completed;
  prior entry's session12818 is no longer active.
- Actual Tennis clock Root184/0/0 in8.79s. Corrected owning full5274/19/97
  in862.27s. New Root report preserves the first SQL-column harness correction
  and explains why the original main-test no-file assertion was overbroad:
  received native status rows remain while invalid predictions/revisions do not.
- D4 actualTennis5d5bab6 reviewed independently:375/3 in37.52s,57 qualified
  probes and two actual mixed D2/live stores in52.53s. Full original report
  SHA bc9c550bc1a49eaa6c2f405ccd874ad27a81478204d13bf6cca5e4257ad1c927
  copied unchanged. No native alias/court/empirical capability inferred.
- Hook7ba4c97 Root software rereview367/0 in38.58s, one deliberately historical
  sourcepin deselected. Original12 functional REDs plus6 controls unchanged and
  green. Owning full4789/18/97 in1004.62s; its immutable Full26 addendum copied.
  Actual Linux DAC/restore/installed A-to-B chain is still required.
- Shared Tennis consumer is local WIP on5d5bab6 in separate consumer worktree;
  242passed/26subtests14.27s plus400passed/1WindowsSkip29.20s. Both views consume
  full-precision winner and same immutable reference. Set simulation and all
  legacy absent-reference JSON/IDs stay separate; price inputs do not change p.
  Freeze/independent review and integrated merge not yet done at this checkpoint.
- P4a team-sport original builder and P4b1 same-response native envelope capture
  are active in separate agent worktrees. Actual shared BB/Hockey worker/normal
  consumer and Football/C4 original/consumer integration remain open.

Preserved new reports: task-18-tennis-publication-clock-root-rereview,
task-18-team-sports-live-preflight, task-20-updater-original-independent-review,
task-20-updater-full26-owner, task-20-updater-unicode-root-rereview,
task-20-tennis-d4-independent-review (all suffix20260909.md).
No source entitlement or >=200 real-event approval fabricated; no Cricket,
money, old forecasts, tickets, pinned helper or unrelated output mutations.

## Closed full run / accepted reader correction — 9 September 2026, 16:24 UTC

Root code `ef1aea8ae01d51eb1b134aa06265f3f2fe4c471b`; main/VPS latestactual
15:57UTC `2ba3931`, no new context deployment; actual combined Tennis rebuild
HTTPError remains a production issue, not reset or hidden.

- Frozen d2809fc full COMPLETE:5202passed/19skipped/97subtests1196.76s.
  `kontext-integrated-qa-20260909/.pytest_tmp/context-integrated-full-20260909-01.xml`
  SHA256c368aa7c7b4e8eb6ab867f485cb7696910bec2b6d6d247d95f9c18ec24a151f0.
  Does not cover later Domain, corrected Reader, actual Producer or Updater.
- Readerfix4011392 fullownerreport/source/tests read byRoot; independent74/1,
  7.48s includes actual two unchanged originalfindings, both qualifiedWALcontrols,
  tenRootprobes and61 permanentcases. Firstcombinedcollection had same-name
  helpercollision, zero tests; correctedviaexplicit targethelperpreimports only.
  Rootrereview preserved alongside originalnegative, mergedef1aea8 exacthashes.
  Rootintegration session12818 active; not a new completed fullsuite.
- New ccefe85 P3 same-call original seam: 19 APIRED then727/1 broad128.16s,
  22postLFchecks; frozen-source/defaultAST parity replaces obsolete prefix check,
  no oldfixture/pin alteration. RealCricket normal/missing predictor+RiskIDs
  before/afterSHA17769c31bbee92d89991ed2582255cebe713a17379b661a91e4b972426227083.
  Owning live B3/Basebuilders and shared normal consumer still required.
- Domain9de independent review active in worker agent after Readerfreeze.
  P3ccefe85 independent review queued; no acceptance inferred yet.
- ActualTennis32ac1d8 frozen with758/0 focus including154 new cases/48 legacy
  parity. Fullrunning byowner; Root starts independentreview. TypedOriginkind
  requires Root exact original/lightvalidation, actual shared normal/Riskreaders
  and D4publication+nativeB1+state reference binding before context release.
- Updaterhook still in owner regression on separate worktree. Fixed12-code
  limitedexit2 rule unchanged; actual opaqueD2schemas correctly reject.

All old negative reports/handoffs preserved. No D2>=200 empirical approval,
no invented five-sport source capabilities, no Cricket/tickets/accounts changes.

## Current actual producer/reader checkpoint — 9 September 2026, 15:59 UTC

Root code `9de9b52ab096c8c08852460dfe8226854014e789`; last verified remote feature
`7201302a7b145c7dd94de30bd41d1b5b8a498f9f`. Fresh GitHub main AND readonly VPS
verification at15:57UTC: `2ba3931dd8cb35f31d2475ae5797d75b44be268e`. Tracked VPS
clean, App/Caddy active/localhealthok, seven timers scheduled. No context release.

Important changed production status: tennis service is failed, last07:17:02–
07:18:34CEST/exit1. Journal proves combined rebuild HTTPError/rc1 after64s;
existing state retained, subsequent day scan rc0/22 new predictions. No source
rerun or reset-failed was performed. Do not repeat older 'no failed units'.
Existing reviewed A1–A4 independent tour refresh is still feature-only.

- Original-event binding da0 independently PASS831/0. Original negative and
  accepted rereview plus actual producer/updater preflights preserved7fae762.
- D4 F1 independently13/13,86.57s; exact old13 probes unchanged. JUnit SHA256
  `adbbd85bd872ecd85d1cafc568893c89513caff01aea5d02364a702fe1c87e85`.
  Owning1daf72d merged `d2809fc67d4e0ebc548cdd1ded89034d4e2d1761`; original and
  rereview preserved7201302. Explicit new C3/C4/Tennis-v3 source replay is not
  silently certified by the older closed numeric-capability table.
- Fresh whole integration run is ACTIVE in separate clean
  `kontext-integrated-qa-20260909` on exactd2809fc, session70501,
  `.pytest_tmp/context-integrated-full-20260909-01.xml`. This includes accepted
  sport/capture/D4 merges and initial Reader97cb672, not later domain/Readerfix/
  actual Tennis/updater work. Current run has not completed; report no total yet.
- New reader97cb672 independent REQUEST_CHANGES: F1 table_info hides generated
  extra columns; F2 preexisting SHM hardlink overwrites a separate temp sentinel.
  ReportSHA `85874edf9da050ff77ccb30b36b92b29c1df162ef9f88da77a8b199b35a23529` in
  `kontext-reader-review-20260909/.pytest_tmp/consumer-reader-independent-20260909`.
  Original413GREEN/4literalRED/1Windows-symlinkskip: two realfindings, one
  explicitly overbroad no-WAL-sidecars assertion and one unpinned-WAL harness.
  New actual WAL controls prove current uncheckpointed rows/consistent readtxn.
  Fix active in `kontext-reader-fix-20260909` on7201302: closed table_xinfo,
  narrow shared dataset._reader main/companiontrust beforeSQLiteopen/after,
  no immutable=1/raw-copy/stale fallback. Root rereview pending. Keep originals.
- Domain-link packet9de9b52 (8exactfiles) adds frozen ContextReference,
  optional ModelSignal/EventModelSnapshot.ref and normal JSON readback.
  Legacy IDs/absent field remain exact; existing event/model/input UNIQUE
  remains strict, new actual context must have its proper new input hash.
  Latest298/0/26subtests5.95s, XML SHA256
  `f970df164f355db1882da53db52f07d4874994f04f322cac68a9630a470bfafb`.
  Independent review pending. No real producer attaches this field yet.
- Actual Tennis producer active in `kontext-tennis-live-20260909` on da0.
  Agent owns predict optional same-call capture, daily two-phase ordering,
  revision-atomic shadow context_json.context_model, new
  context_models/tennis_live.py + tennis/live_context.py and additive contract.
  Base version tennis-live-calibrated-winner-v1; model_hash actual prior A1
  tourstate. Original publication is later/separate. Native current ESPN IDs
  are retained, historical state alias remains unavailable; Surface/Indoor
  unknown in native status-v1 stay None while actual modelinputs remain in
  the owning original. No made-up serve format/duration/approval. Root will
  add exact new original validation in transport and actual consumers after
  independent freeze; no blanket reclassification of D1 or old B7 comparison.
- Updaterhook implementation active `kontext-updater-hook-20260909` on1daf72d.
  Full Root-read preflight/rulings held. New hooks beforedowntime/afterbackup
  beforeapply; only TARGET_PAYLOAD verifier asbetboy, exact runtime mapping,
  isolated root-owned single DB/real seal/no app ZIP or keys. Fixed12 positive
  capabilitylimits only for typed exit2, never unknownschemas/errors. Missing
  only proved pre-context legacy (previous complete payload no contextpath
  contract + exact live/backupabsence); Root can release hook-only bridgeA then
  real contextB. No newRootinstaller/stagehelper/Unitpin/migrationpolicy change.
  VPSpreflight actual runtime overrideabsent/contextfileabsent; updaterroot0755
  SHAa07ad24c92f207c9d1124acee37fb4b0e443dae39876dad6c1948b55bb725cbb,
  stagehelperroot0755 unchanged1441158…; venv3.12.3/SQLite3.45.1/deserializeTrue.

No real empirical approval, complete five-sport D3 UI or new main/VPS activation.
Cricket excluded; preserve prior forecasts/tickets/accounts/untracked/protected
helper. Continue the approved work without repeating spec/worktree permissions.

## Current owning integration checkpoint — 9 September 2026, 15:28 UTC

This section supersedes older next-task statements. Root source is
`da0ae34613fb030280aac3e8abc504d18ebca47e`; last verified remote feature is
`caa230f73705f5bf8aadcc25394cd136963e858d`. Main/GitHub main remain
`2ba3931dd8cb35f31d2475ae5797d75b44be268e`; VPS was verified there earlier in
this same continuation, not refreshed by this checkpoint. No new context
main/VPS deployment. Existing spec/worktree/Variant 1 authority stands.

- C3 Hockey and D3 transport independently accepted. Owner and original review
  reports are preserved in `e7911e0`; independent transport573/1 platform skip.
- C4 owning native-scope correction independently accepted, merged `3c14e3d`.
  Root integration1034/1 Windows symlink skip184.50s; XML SHA256
  `a8618fb621382f72a6971ec5587bc642b386f9cc752d4dc776c609aa010e320a`.
  Original negative, correction and final reports preserved; no live roster,
  patch/map-time source, D1/D2 corpus or empirical effect acceptance.
- Tennis-v3 real existing-response status/workload packet `f45c7eb` independently
  accepted978/0 and merged `d4312dd`; Root integration523/0,42.16s. Existing
  source/v2/predictor bytes remain unchanged. Receipt publication happens on
  capture scope exit, after `_run_daily`; native player-to-tour-state binding,
  actual worker/consumer integration and real empirical corpus remain open.
- Football FT watch capture final `af7375b` independently accepted510/0,15.42s,
  merged `ffb4005`. Original F1: filtered SQL inventory hid a corrupted existing
  base receipt. All rows now decode before source/kind selection or writes.
  Positive original damage witnesses correctly fail before the bad state; do
  not count those expected diagnostic failures as ordinary passing tests.
  Tennis and original/after-fix Football reports preserved in `b6a70c0`.
- D3 C4 adapter `82f1e2d` original review REQUEST_CHANGES, one P2 (9 RED/41 GREEN
  new probes). Existing fallback could relabel a known original event before
  numerical comparison. Root reproduced the same C2 and C3 light-read class:
 18 permanent tests14 RED/4 GREEN; fixed `da0ae34` with exact owning original
  reference-kind/event binding before calculation, replay and card projection.
  New combined134/0,20.58s; XML SHA256
  `db5de3f69c4c33c1ea0e7dda512829a516d426e0bea0d868bf9a22add89fc3f9`.
  All50 byte-identical original C4 probes now green12.06s; XML SHA256
  `33b0a6c7a91cacf10de1a9ca5fd0f7e1c434378b04dd6361a55ca9e2cc2e8a6d`.
  Independent rereview active in `kontext-original-event-review-20260909` on
  exact `da0ae34`. Preserve the original negative report before merge/release.
- D4 semantic owner frozen `b56da33`, NOT merged; owner full4533/18 platform
  skips/97 subtests1080.69s. Root original13 probes produced2 RED/11 GREEN:
  orphan known cases skipped physical/time validation of explicit FV.refs.
  Narrow F1 fix active; original probe SHA256
  `c2972259269e8e119e7021ce7307ced0e5bc110fede03c609705bd8c22ddb0bf`
  and negative REVIEW.md must remain unchanged. No final body may be opened
  to solve a missing configuration/capability, and no old Linux proof certifies
  these new semantic branches.
- Read-only D4 updater preflight complete:
  `.pytest_tmp/d4-updater-preflight-20260909/REPORT.md`, SHA256
  `0a747558167dd55aeba0fcab8bbdf27bf7a877096f0abf99f73ebc7ef3a928b3`.
  Proposed exact seam: verified preupdate backup before apply_trusted_payload;
  private root-owned sealed single-DB copy, target payload verifier only as
  betboy, never App/venv modules as root or backup-key account. No implemented
  hook. Actual runtime mapping, typed limited exit policy, Linux permissions,
  WAL seal/restore and installed two-commit trusted transition still need tests.
- Actual D3 producers are being traced read-only. Basketball/Hockey historical
  winner models exist in Riskobet but are not consumed by normal Wettfinder
  (`live_only_no_prematch_model`). Manual totals are distinct model families.
  Football legacy market calibration cannot be relabelled D1 raw Poisson;
  Tennis loses unrounded original winner/hold/native IDs before Shadow storage.
  These are concrete remaining software seams, separate from unavailable
  player/forecast sources. No generic helper is a live integration proof.

Latest whole Root4449/18/97 predates these integrations; fresh stable whole
regression remains required. D3 actual shared assembly/domain/store/both-tab
cards, D4 production hook/Linux restore, D5 browser/release and actual>=200
event empirical approvals remain open. Baseline-safe software may ship without
activating an unapproved effect; never claim all sport effects are complete.
Cricket, prices/ranking, old predictions/tickets/ledger and production unchanged.
Protected stage helper raw SHA256 remains
`1441158c542e97a19b193fa0cd091b645ec6442d6d8157f1d4fceabbba72b026`.
Preserve unrelated untracked archives/probes; force-add ignored SDD only by
exact report path. Timers calculate, never pull or deploy code.

## Current integration and source-status checkpoint — 9 September 2026

This section supersedes the older next-task statements below. Current Root
source is `c871ba1203b66f8ba69adb5fed056e251d84c59b`. Last verified remote
feature is `908667ec0d682b192f5ac33c20c915f79b3259d4`; this checkpoint and
the subsequent scoped feature push must be verified separately. Main/GitHub
main remain `2ba3931dd8cb35f31d2475ae5797d75b44be268e`; VPS was verified
at that release earlier in this same continuation, not by this note. No new
context main/VPS deployment. Existing spec/worktree/Variant 1 authority stands.

- Latest completed whole Root suite: 4449 passed, 18 Windows/POSIX platform
  skips, 97 subtests, 715.38s; `context-d2-capture-full-20260909-01.xml`, SHA256
  `649d6d74d9234aa225de8a46739de84b35d38c368ae87b29e5d969171c96f459`.
  This predates all following integrations: do not call it their full regression.
- D3 exact transport byte fix `1a9fffa` independently accepted and merged
  `0840c35`. Football native NS request-scope correction independently accepted,
  Root215 tests, committed `53c2ae9`. Original and completed review reports are
  preserved byte-identically (or clearly identified as Root rereview) in `0964dd1`.
- C3 Hockey owning correction `39cbc3e` independently accepted, Root295 passed
  with 1 Windows symlink skip, merged `a55d1a7`. Real player/TOI capture and
  native D1/D2 corpus remain unavailable; no empirical or live acceptance.
- Subsequent Root D3 Hockey transport `c871ba1` is frozen pending independent
  review. Its owning coefficient export converts NumPy floats to Python floats
  without changing float.hex or canonical JSON. Actual B3 two-consumer CPU
  reuse, regulation and overtime parity: latest550 passed/1 platform skip.
  This does not establish live-worker assembly or full source-semantic replay.
- C4 remains isolated, NOT merged. Original and second reviews exposed terminal
  status, derived-delta and incomplete native-scope correction failures. Root
  R1b `be45dab` passed4205/18/97 full, but the next independent31 cases found
  R1c (5 RED). Owner is correcting only native whole-identity retention in
  `kontext-c4-esports-20260909`; latest1012 focused passed167.10s, full running.
  All old adversarial probes and pinned Elo/source bytes must remain unchanged.
- Tennis actual existing-response status/workload capture is active in
  `kontext-tennis-capture-20260909`, base `ecf9200`. Explicit status/source/FV-v3
  preserves native correction lineage, including incomplete terminal revisions;
  old B6/v2 bytes stay unchanged. Latest85 new tests green; broad run/review
  pending. No new GETs, inferred native state IDs or invented end/duration times.
- D4 actual semantic verification is active in `kontext-d4-semantic-20260909`,
  base `0840c35`. It must use existing evaluator functions on the same in-memory
  read-only snapshot, preserve opaque legacy transport-only boundaries, and
  never open frozen final labels before their legitimate recorded opening.
  Exact source bytes/indexes remain checked; missing owning contexts cannot
  become full semantic approval. Known invalid artifacts fail closed.
- D2 owning evaluator is accepted but no actual >=200-event successful approval
  exists. D3 actual A1/B1/D2 worker assembly, missing sport adapters, signal/domain/
  store/both-tab integration and rendered evidence remain open. D4 production
  backup wiring and D5 release remain open. Do not mislabel helpers as live work.

Cricket, prices/ranking, old forecasts/tickets/money and production state remain
unchanged. Protected stage helper still has raw SHA256
`1441158c542e97a19b193fa0cd091b645ec6442d6d8157f1d4fceabbba72b026`.
Preserve unrelated untracked archives/probes. Never blanket-stage ignored SDD;
force-add only exact new report paths. Timers calculate and never deploy code.

## Latest D2, capture and independent-review checkpoint — 9 September 2026

Current Root source `ecf92007f46c5e8b85cb24b6f7208cb71240cac4`. Main/GitHub
main last verified `2ba3931dd8cb35f31d2475ae5797d75b44be268e`; VPS verified
earlier in this same continuation at that release, not refreshed by this note.
Last independently verified remote feature was `4d33224`; newer Root commits
below still need their feature push. No new context main/VPS deployment.

- D2 actual source dataset/evaluator/approval/CLI `84c101b` independently PASS
  (70 owning +15 new integration attacks). Merged `187dfa0`; byte-identical
  review retained `526d988`. Root independently reran all70 tests:471.59s.
  No >=200-event successful actual-source approval; Tennis native-state alias,
  other sports' D1/D2 and preregistered detailed cohorts remain missing.
- D3 public copy independently accepted `5fb41de` (178 tests). Pure transport
  frozen `4d33224` passed4351/18/97 full but independent review found one P2
  exact numeric-copy identity issue (6 RED). Root narrow fix is independently
  PASS in separate clean `kontext-d3-bytefix-20260909`, `1a9fffa`:17 new REDs
  closed,296 owning /360 independent in two runs. Not yet merged while Root's
  existing full run retains frozen bytes. Original review/probes unchanged.
- Football actual existing-response capture committed `ecf9200`:28 new tests,
  397 focused /32 subtests green13.95s; no new GETs, no retroactive receipt
  clocks, only worker-owned native batches. Automatic scan/context refresh and
  manual scan wrappers preserve baseline selection, with closed internal
  capture metadata separate from ordinary provider errors/eligibility.
  Independent review by b3 is active. Root full `context-d2-capture-full-20260909-01`
  is still running; never substitute the earlier4351 count as its result.
- C3 Hockey clean `6b2473b`, owner4442/19/97 full and1001/1 focus. Independent
  review assigned worker_failures; no merge or actual NHL input/D1/D2 acceptance.
- C4 original review found two P2s: terminal->started correction retained old
  load/rest; absolute tolerance admitted a changed derived participation delta.
  Owner fix clean `79763a1` with35 new tests,235 focus/4189 full/18 skips/97
  subtests. Actual native-status Subject-v2 retains intervening corrections;
  only a genuinely consistent later terminal series restores its end.
  Original probes/audit/Elo bytes unchanged. b6 independent re-review active.
- Root prepared clean `kontext-tennis-capture-20260909` on `ecf9200` for actual
  already-fetched ESPN receipt preservation; NO new source edits there yet.
  Preflight identified that nonterminal corrections must also be preserved;
  the currently terminal-only B6 source must not silently retain obsolete load.
  Pending-refresh remains no-network; native state identity/end/duration gaps
  are not filled with names or scheduled/receipt times.
- D3 full owning A1/B1/D2 assembly, sport adapters, domain/store/both-tab UI and
  browser evidence remain open. D4 needs real semantic evaluator/transport
  verification and production backup wiring. D5/empirical activation remain open.

Preserve all unrelated output archives/probes and the phantom changed staging
helper: raw SHA256 `1441158c542e97a19b193fa0cd091b645ec6442d6d8157f1d4fceabbba72b026`
still matches. Cricket, prices/ranking, prior predictions/tickets/ledger and
production state are unchanged. Existing spec/worktree/Variant1 authority is
already granted; no repeated approval question. Timers calculate, never deploy.

## Latest C1/C2/D1 integration checkpoint — 9 September 2026

This is newer than the older checkpoint below. Do not restart completed work or
ask again for specification, isolated worktrees or Variant 1 delegation approval.
Root feature source is `a1f4969f9aab6f7253951eafaa0d0582e0156be9`; main/VPS
remain the separately verified `2ba3931dd8cb35f31d2475ae5797d75b44be268e`.
No new context code has been published to main or deployed in this checkpoint.

- D1 owning replay/case/all-five-alpha fit mechanics `f4649c3` independently
  accepted and merged; original reports preserved byte-identically in `70d6ce1`.
  Identity proof retains original valid receipt history while numerical replay
  uses actual latest native values. Real corpus and Tennis native-state resolver
  remain missing; synthetic optimization is not empirical acceptance.
- D2 exact distribution scorers and complete-registry paired comparisons are
  independently accepted in `9e8e22f`, with the real owning dataset/evaluator
  rulings. Final-label opening and all-family BH inventory stay immutable.
  The new owning dataset/evaluator/activation/CLI is now isolated under
  `kontext-d2-evaluator-20260909`, on base `70d6ce1`, not yet independently accepted.
  New actual SQLite controls bind terminal-result revisions at the original
  train/tune boundaries and at final evaluated_at, plus label-free outer-index
  integrity before opening. These are mechanics, not a >=200-event real result.
- C2 reviewed branch `23aa21c` merged as `b908712`, with all three original
  review reports preserved in `13b4dac`. Original F1-F3 and later F4-F5 closed
  in the OWNING path: full causal participant/term lineage, exact-end rest,
  strict half-open load and old-conflict bounds. Independent final 319 + 44
  positive tests in two runs; two unchanged wrong-generic-reader diagnostics
  remain negative integration evidence, NOT green. Owner full3795/15/97.
  D3 must use basketball_observations_as_of, never the wrong generic selector.
- C1 exact-cutoff rest fix independently accepted and committed `a1f4969`:
  final reviewer283 passed, genuine original Git-code reproduction and unchanged
  Tennis source. Historical total/window count remains strict; exact rest may
  include an end known exactly at decision. No source or fatigue coefficient added.
- Root stable full integration before subsequent D3 transport-only refinements:
  `context-c2-transport-integration-full-20260909-01`, **4323 passed / 18 expected
  platform skips / 97 subtests**, 272.30s. It includes then-current unreviewed
  copy/transport WIP; it is not independent D3 acceptance or a live result.
- D3 public copy is frozen for independent review: context_copy.py and
  tests/test_context_copy.py (33 cases). The standalone closed CPU transport is
  still Root WIP, with 50 tests; latest copy/transport/B3 focus265 green4.16s.
  It binds full original Event/Base/FV, typed reference lists, resolved artifact
  identities and exact result/consumer market. Consumer reads do not refit
  Ridge through a validator. Real A1/B1/D2 assembly, workers, signal/domain/store,
  both UIs and browser proof are NOT implemented by these helper tests.
- C4 internal E-Sport mechanics frozen `26e891096ecee78ac962b5090a8294deefd7d395`
  in `kontext-c4-esports-20260909`, clean, owner full4154/18/97. Root independent
  review remains required. No native Roster/Patch/Maptime live feed or D1/D2 proof.
- C3 Hockey implementation is assigned in clean `kontext-c3-hockey-20260909`,
  base `13b4dac`, after full task14 ruling/preflight. Measured exposure is not
  nonlinear optimizer influence; hypothetical starters cannot silently become
  the central applied forecast. Existing observed-fact-only paths are not banned.
- D4 structural/read-only/isolated Linux restore proof remains accepted, while
  D2 semantic verification, D3 input/source replay and production deploy wiring
  remain open. D5 real worker/browser/release and empirical proof remain open.

Cricket, odds/ranking policy, old forecasts/tickets/ledger and protected helper
bytes remain unchanged. Untracked output archives/probes remain user work. The
seven timers calculate data and do not auto-pull or deploy source. Commit/push,
main/VPS code, actual worker execution, source freshness and empirical activation
must continue to be evidenced separately.

## Current continuation checkpoint — 9 September 2026

This section supersedes earlier next-task statements, not historical evidence.
The full new attachment was read; prior spec/worktree/delegation authority
remains in force. Cricket remains excluded. No model-price or money policy was
expanded, no prior prediction/ledger was rewritten.

- Current main/GitHub/VPS are exactly `2ba3931dd8cb35f31d2475ae5797d75b44be268e`.
  This separately reviewed native Tennis schedule-settlement fix passed2022
  tests/11 expected skips/97 subtests in main plus independent173 focused tests.
  The trusted updater verified pre/post backups with87 databases each, exact
  deployed source hash, healthy app/Caddy/local/public health and7 enabled timers.
  The first subsequent scheduled Wettfinder run10:07:22-10:11:09 CEST completed
  with exit0,122 model candidates and0 fully confirmed tips. It does not prove
  quality or fix the external WTA503. Context merged this release in7962322.
- Context D2 math commit `9b46c27df02a5037b9d729f958daa44b0bdcaf94` is pushed and
  exactly verified through ls-remote. Current reviewed D4 merge is
  `dfcc6d357c536e4390322c1267bc26957663ddbd`; registry and Linux fixture evidence
  below are the next scoped commits. Root full3865/18/97 includes final reviewed
  D2 math/registry, D4 and corrected private Linux fixtures. It is not a D2 empirical
  approval or a main/VPS context release. Unrelated outputs and pinned helper
  bytes remain untouched.
- B3 software was independently source-reviewed by Root and rerun:557 passed,
  4 expected POSIX skips. Approved scoped commit
  `f876df0588aaed82db4a752e5f23977c6ceb5dc1` was fast-forward integrated.
  Its 182 new tests include real A1 roundtrips,8 threads,3 processes, corrupt
  payload/key bindings, stale/future/mismatched evidence and legacy certification.
  Shared production consumers and complete D4 restore remain future tasks.
- B4 provenance6726818 and genuine3-process test harness9ddb45f independently
  read/retested by Root:275 tests green, including UI-before-snapshot order.
  Integrated in c318224. The harness retains a common start barrier, exactly
  one callback, all child exits and exact stored/result equality; no skip.
- Root B4 source/roster/feature/provider/native-receipt mechanics is reviewed.
  Real sanitized fixture probe:2 governed BACKGROUND GETs,46 player
  rows/32 known minutes, no historical prematch or complete absence claim.
  First3 independent revision findings fixed. The further same-membership
  mixed-content revision attack was reproduced and corrected with whole-team
  content binding. Empty-collection withdrawal, HTTP3xx/206 admission and
  direct45min normalizer scope were reproduced and corrected. Final independent
  review:118 focus/39 additional attacks/459 integration passed,10 source/test
  SHAs unchanged; Root independently repeated94 including all39 new attacks.
  Full Root3281/15/97; task-8-final-review-20260909.md preserves the review.
  Training/empirical/runtime boundaries remain open, not inferred from this.
- B6 corrected implementationad8f85d passed Root271 tests, including unchanged
  independent participant-revision attacks, and is integrated in dd6fd38.
- B5 mathematical-only49e6fae plus independent-review fixes219b6c1 are integrated
  in ce7f070 after Root source review/390-test recheck. Known target team IDs
  bind even with unresolved history; B2 stored integer coefficients/scales must
  be exactly float64-representable. Full original Event/base v2 reference and
  strict B4 feature version prevent old-feature reuse after schedule changes.
  Real football training rows/fit/empirical approval/activation remain open.
- B7 mathematical-onlybf758206 passed final independent rereview:266 plus18
  new boundary attacks, no findings; original attacks unchanged. Integrated in
  3e9ce2c, Root674 focused and full3281/15/97. V2 binds full Event/base; strict
  serve preserves the existing approximation and exact legacy outputs.
  Neutral effects preserve original market bytes; a sufficiently old terminal
  receipt may exclude an unknown end from recent windows, using its own exact
  bounded/partial coverage identity. task-11-final-review-20260909.md preserved.
- C1 corrected f14f75e is independently approved and merged in0d000f6:
  original37 attacks +163 permanent+29 new independent cases pass, reviewer
  full3170/15/97; Root439 integrated checks. task-12-final-review-20260909.md
  preserves evidence. Real weather/source, fitted C1 effects and runtime stay open.
- D4 corrections3df419d independently approved and merged indfcc6d. Source
  reads now use only an in-memory SQLite image;
  original source/sidecars remain unmodified, controlled input cap64MiB.
  Actual isolated Linux QA Python3.12.3/SQLite3.45.1/pytest9.1.1, non-root ubuntu
  and ordinary umask0002: original5failed/429passed from permissive test fixtures,
  then434passed/0skips on unchanged product bytes and explicit private fixtures.
  Independent fixture review150passed/3Windows-skips, no findings; both exact
  source/helper hashes unchanged. Full proof in2026-09-09-d4-linux-restore.md.
  D2 semantic/D3 input/deploy integration remain separate.
- C1-C4,D1-D3,D5 and actual numerical injury/fatigue activation remain open;
  B4/B5/B7/C1/D4 have the limited software/review statuses stated above.
  D1 split reviewed47 incl13 independent, config ea511ba/ea511a2 reviewed445,
  native outcome/global map3322470 with Serve consistency2fd9c72 reviewed by
  Root;124 identity/outcome/config/registry checks pass. Actual replay/case/fit
  is frozen b74e2ab in kontext-d1-training-20260909 for independent review,
  implementer full3463/15/97; no real causal Tennis or participation fit claimed.
  D2 math finally reviewed246 incl107 unchanged independent attacks and committed
  9b46c27. Registry had two P2 findings/six REDs, corrected and independently
  approved136 incl76 independent probes; Root136 rerun also green. Complete
  task-17-registry-final-review-20260909.md is preserved byte-identically.
  C2 basketball implementation follows approved preflight/rulings under b6;
  separate D3 read-only preflight is with worker_failures. No C2 source
  request or production edit. C3/C4/D3/D5 are not completed by these mechanics.
  No wrapper/synthetic test count is an empirical200-event/three-block result.

## Identity and authority

- User approved written specification on 2026-09-07 and selected execution option 1 (subagents with task review).
- Binding spec: docs/superpowers/specs/2026-09-07-kontextmodell-design.md.
- Starting main commit: 54314cad6d33b728dfb7e00da08b6ad6218e0491.
- Execution order: A1-A4, B1-B7, C1-C4, D1-D5, with scoped evidence/backup dependencies below.
- Worktree explicitly approved by user reply `ja`. Active implementation checkout: C:/Projekt/BetBoy/betboy-app/.worktrees/kontextmodell-20260907 on codex/kontextmodell-20260907; branch base 8c3df7978bf074cfe6ce3d9a794f6f60139327ea. Parent main differs from the previous starting commit only by the committed .worktrees/ ignore rule.
- Expected untracked artifacts preserved: .playwright-cli/, two AUDIT_BERICHT files, UMSETZUNGSPLAN_MARKTBENCHMARK_2026-09-02.md, output/.
- Full baseline command: .codex_test_venv/quality/Scripts/python.exe -m pytest -q -p no:cacheprovider --basetemp=.pytest_tmp/context-baseline-20260907-01
- Baseline result: 1724 passed, 11 skipped, 97 subtests passed in 70.49s; exit 0. No production code changed.
- SDD helper requires Git Bash --login on this Windows host so basename/awk are discoverable. First two non-login attempts failed without creating task artifacts; --login succeeded.
- Mechanical execution view created at 2026-09-07-kontextmodell-umsetzung.md inside this workspace. source-map.json records all five source SHA-256 hashes and the exact twenty-task A1-D5 mapping; all were rechecked successfully against unchanged source files.
- Task 1 brief was generated by the supplied helper, read completely and amended only with scoped controller clarification. It was regenerated in the approved worktree before dispatch. Use the task-state table and execution continuation below for current progress, not the parent checkout's historical preflight state.
- Each implementer must use absolute worktree paths for apply_patch: a shell workdir does not change the patch tool's CWD. A1 removed only its own accidental C:/Projekt/BetBoy/model_artifacts.py creation; controller verified this path and parent-checkout model_artifacts.py are absent after the report.

## Preflight rulings

Ruling2026-09-08 real-data A3 repair: Controller read the complete diagnosis and actual serve update, serializer and calibration traversal. One real US Open row creates exact0 return games/4 breaks;225 qualifying source rows have inconsistent zero denominators, not rounding. Permit atomic bilateral serve-row admission for NEW separated-tour training and its calibration_only traversal: validate both sides before any accumulator mutation, require finite nonnegative genuine numeric counts and positive service/return denominators for a completed serve observation, reject breaks beyond corresponding games; skip only that row's serve contribution while retaining its valid Elo result. No clipping, tolerance relaxation, invented denominators or event/market bans. Use one common owning validation helper; legacy/default combined build and ordinary backtest retain their existing call behavior, codec/accumulator invariants stay strict. Keep decay, priors, Elo and calibration-year/loss rules unchanged. Record actual admitted/skipped counts, stable reasons and year/event coverage in the existing build/CLI diagnostics, not a new artifact/manifest schema or unreviewed provider. Both real build and calibration must use the same admission policy. If this changes training population, disclose it and require fresh real build, focused/default-parity regression, independent review and later empirical acceptance; software success alone is not a quality claim. No production activation before the separate restore/release checks.

Ruling: A4's legacy bridge is operator opt-in, not a constant allow_legacy=True in regular scans. Normal scan_fixtures/refresh_pending_predictions default allow_legacy_model=False; initial CLI may accept explicit --allow-legacy-model. The unchanged Wettfinder caller stays separated-only. Controller checked spec section8 and the migration-only brief after the implementer reported a hard-wired True; implementer confirmed the mismatch and is correcting it with default-negative and explicit-bridge TDD. Keep top-level errors compatible with the unchanged automatic consumer, healthy tour continuation and old forecasts/identities. If wrong, an unbuilt isolated tour may remain unavailable until an explicit bridge/build; leaving automatic fallback would instead conceal incomplete migration and perpetuate mixed-model use. No quote, market, account or settlement rule changes.

Ruling: Use a mechanically normalized execution view with Task 1-20 headings for the supplied SDD helpers; preserve A1-D5 labels and original text as the authority — the helper only recognizes Task N, whereas the approved parts use A1-D5 — if mapped incorrectly, an agent could receive the wrong task; verify all twenty labels and source hashes before dispatch.

Ruling: Task B5 needs an integration RED test that calls apply_football_effect; its offset-only numerical example is not sufficient after B2 exists — the plan itself notes this dependency, and TDD must protect the new consumer behavior — if missed, a green mathematical helper test could hide missing integration.

Ruling: TrainingRow must include explicit family and head fields, with allowed head names from EffectArtifact, in addition to the listed B contract fields — the two football/hockey/serve heads otherwise have no reliable row routing — if wrong, schema migration costs rework, but silent home/away label swaps are prevented.

Ruling: Eligibility-set intersections in D2 are within the same registered comparable population/market-outcome cohort, never across unrelated sports or formats — each sport has different events, and the spec requires paired comparisons on the same events within each family — if wrong, the comparison population changes; record every exclusion and keep all registered hypotheses in BH.

Ruling: A4 software can be reviewed before the full D4 context-backup scenario exists, but no A release occurs without a tour-artifact backup/restore proof; the complete observations/snapshot restoration stays required after B3 — D4 references future B3, while the index permits independent tour repair — if wrong, an incomplete release could omit artifacts, so release remains explicitly gated.

Ruling: Scope status is four-dimensional (software, real data, empirical validation, production activation); do not mark a whole task complete when its required source/evidence work is still open — the spec explicitly forbids counting a wrapper or synthetic proof as finished integration — if wrong, progress may be conservative, but no absent capability is represented as delivered.

## Self-consistency scan

Ruling: A3 keeps as_of as logical training cutoff and built_at as actual completion time; refresh_tours adds publication_clock (default actual aware UTC now), consulted after each build and again for CAS publication. It requires training cutoff <= built_at <= publication time, validates coverage against training cutoff and codec eligibility against publication time. Tests inject coherent future clocks; production never backdates built_at — a real build finishes after run start, so treating run start as publication time would reject valid refreshes — if wrong, model availability could be misdated; cover elapsed builds, clock reversal and future input/state explicitly.

Ruling: A3 stores a strict outer payload {schema:1, training_cutoff: canonical aware UTC ISO, state: exact A2 payload}, artifact kind tennis-tour-state and slots tennis:ATP/tennis:WTA; ModelState adds optional training_cutoff metadata attached from that wrapper. A2's strict envelope and legacy bytes remain unchanged — the spec requires a training cutoff distinct from build time and coverage — if wrong, wrapper compatibility could break readers/restore; A3 load, A4 evidence and D4 verification must validate both layers and bind their shared artifact hash.

Ruling: A3 may extend tennis/backtest.py plus focused cutoff/calibration tests with an explicit end cutoff and calibration_only mode for separated tour builds. This mode copies a fixed sport/result allowlist before calibration, never filters sample eligibility by quotes and never invents quote placeholders. Legacy backtest default remains unchanged; calendar-year policy, sample thresholds, raw-probability rounding, calibration math/orientation and all other eligibility rules stay fixed. Unused WTA serve data are not loaded at zero serve weight — the spec's price-free calibration contract outranks preserving an implicit historical price-availability filter — if wrong, the changed calibration population shifts probabilities; verify missing/invalid/changed prices have no effect in new mode, default-path parity, exact cutoff filtering and disclose population change without claiming empirical improvement.

Ruling: Do not add data_loader.as_of solely to make future-year fixtures pass. Keep source validation on its real clock and pass the existing explicit current_year where appropriate; synthetic 2027 tests freeze/inject the existing source and completion clocks coherently. A future logical build cutoff must be rejected before production source I/O unless the test's real clock is explicitly advanced — this avoids weakening current-source future-date protections for a test convenience — if wrong, unusual offline callers need an explicit clock adapter later; production cannot accept future records by supplying a future validation date.

Ruling: A2 decode_state adds an optional keyword-only decision_cutoff; it performs structural decoding without consulting the wallclock when omitted. A3 publication and A4 forecast selection must explicitly validate against their actual as_of cutoff, with future-state regressions — this preserves reproducible synthetic/offline artifacts while enforcing decision-time causality — if a consumer omits the cutoff, a future model could leak into an earlier forecast; review A3/A4 call paths explicitly.


| Task | Own test / implementation / files check |
| --- | --- |
| A1 | Canonical artifact API, CAS and corruption tests align; duplicate-key tests must use encoded JSON, since a Python dict cannot express duplicate keys. |
| A2 | Fixed codec fields, new default metadata and parity/cross-tour tests align; artifact identity excluded from its own serialized hash. |
| A3 | Fresh-retained and published statuses both count healthy; independently retained-error metadata must not become fresh. |
| A4 | Tour-specific selection and cross-tour rejection align; legacy hash must bind the bytes actually trusted/read. Operational D4 dependency ruled above. |
| B1 | Separate individual fact vs complete collection coverage; repeated recheck gets a new receipt but does not rewrite first observed time. |
| B2 | All three links use frozen offsets; scale/coefficients must be JSON lists outside numerical functions; no untested live clipping. |
| B3 | CPU-only compute transaction and approval identity align; corrupt existing snapshot must not be replaced by a new calculation. |
| B4 | Component-specific roster reference, replacements and doubtful-player scenarios align; no regulation exposure inferred from aggregate extra-time minutes. |
| B5 | Helper test alone insufficient; integration RED requirement ruled above. Training rows need explicit head routing. |
| B6 | Result observation gives lower recovery bound, not exact end; missing workload remains missing, not zero. |
| B7 | Winner-only and serve-identified variants remain separate; new serve variant uses identity joint calibration and its own validation. |
| C1 | Forecast issue/valid/receipt times remain distinct; archive terms and real coverage checked before claiming data. |
| C2 | Margin-only distribution does not invent totals; Cricket parity remains mandatory. |
| C3 | Regulation score and inclusive winner remain distinct; uncertain goalie not assumed fifty percent. |
| C4 | Winner-only series model does not invent independent maps; roster and illness are separate facts. |
| D1 | Temporal splits and frozen config align; physical reconstruction time stays distinct from logical training cutoff. Head fields ruled above. |
| D2 | Paired event Brier/HAC/BH align; comparable-population intersection ruled above; no test-fitted calibration. |
| D3 | Used versus experimental probability and factor_states/roles align; selected_market is a projection, not an event mutation. |
| D4 | New manifest rollback preserves later data; full cross-artifact restore occurs before activation. |
| D5 | Full tests, empirical report, push, backup, exact VPS revision, real cycle and rendered evidence remain separate required checks. |

## Task state

Ruling: A4 may extend the native observed-status producer and tennis/shadow.py's transactional write guard to reject an observed cancellation during computation, with actual receipt time and no downgrade by older observations. The schema currently has no independent cancellation observation; a read outside the write transaction would race. Unknown legacy status stays unknown, existing forecasts/revisions and settlement/money policy remain unchanged, and no new fetch loop is authorized. If wrong, a status update could suppress a valid new revision or overwrite history; native-ID, stale-observation and in-computation cancellation tests plus task review are required.

Ruling: D1/D2 and B3 use validation-contract-decisions.md. It resolves seven binding seams: preserve the plan's train-only selected fit (reject the suggested train+tune refit); freeze failed/unsupported hypotheses too; canonicalize native event identities before splitting; keep one chronologically ordered point per event; score logloss on the same final cohort; expose exact approval/report/effect kinds, digest transport and linked provenance; retain opened-test history across changed experiment hashes. Main read the complete six-finding preflight and actual D/B/spec clauses; the extra test-reuse check follows the explicit untouched-test rule. Costs if wrong are recorded per decision: weaker use of training data, multiplicity/identity/weighting bias, mismatched logloss population, approval over-transfer, or repeated-test leakage. No numerical acceptance threshold is changed. Downstream B3/D1/D2 briefs point to the document; later D3/D4 must consume it too.

Ruling: A4 adds optional aware decision_cutoff validation to the existing manifest/tour reader seam: the selected hash-checked manifest must have published_at no later than the forecast cutoff, checked in the read transaction; omitted cutoff remains structural. Root inspected A1's timestamp-hashed manifest and A3's structural loader: built_at alone cannot prove pre-decision publication. No historical manifest traversal is introduced; a later current manifest declines that fixed-cutoff refresh while retaining earlier forecasts. If wrong, a concurrent publication may conservatively defer one refresh; without this guard a post-decision model could appear historically available. Test earlier build/later publication, current eligible reads, naive cutoffs and independent-tour behavior; no rating math or source clocks change.

Ruling: A4 may minimally extend tennis/model_state.py:load_state to hash the exact bytes read through the existing trusted handle, preserving all trust/type checks and old file bytes; readers must not duplicate the legacy loader or reopen a path for hashing. The task demands a true legacy-byte identity but its Files line omitted the owning loader. If wrong, legacy call-site compatibility or identity could shift; focused byte-identity/legacy-label tests and the task review must cover this seam.

Ruling: Repair only the pre-existing sub-50ms wallclock-dependent progress-extension test in tests/test_scan_jobs.py using a controlled monotonic clock and event/barrier synchronization, in a separate commit from A3; leave ScanJob production logic and inactivity thresholds unchanged. Evidence: A3 full run 1 failed/1850 passed/15 skipped/97 subtests; unchanged ScanJob module15 passed; isolated affected unittest failed once then19 passed without A3 execution. A host scheduling delay consumes its mere15ms slack. If the deterministic harness is wrong it could stop detecting lost heartbeat extension, so it must inspect a running intermediate state beyond the initial deadline, preserve the no-reset failure condition and receive independent review plus a fresh full run. No further statistical repetition loop is needed.

Ruling: B1-B3 use the exact clarifications in context-contract-decisions.md: separate receipt/content digests and UTC microseconds; an empty-by-default trusted archive resolver registry; typed source/roster references that keep unresolved CSV baselines visible; fixed feature ordering/ddof0 scale; complete FeatureVector snapshot hashing; explicit event/features/effect-envelope bindings and closed population sets. This resolves six concrete pre-review gaps without price features or relaxed activation rules. Wrong choices could misdate evidence, misjoin roster history, transfer approval or alias snapshots; each is bound to mismatch/round-trip/causality regressions and an independent task review. The document's Costs section records those risks; downstream B/C/D consumers must use the same contracts.

- Read-only /root/b_contract_prereview (gpt-5.6-sol high) completed bounded B1-B3 interface review and one schema clarification; controller read both reports fully and checked relevant A1/base code. No code, tests, source calls or Git changes by that agent. The resulting controller decisions are requirements for the upcoming tasks, not implementation or source acceptance.

- [x] Task 1 (A1): completed at e41458d4f8a3127a3687678ec454fc9bfbdba6fe. BASE 8c3df7978bf074cfe6ce3d9a794f6f60139327ea; initial bb0b084; R1 d292c21 resolved recursive JSON keys and ancestor trust, R2 e41458d fixed actual Linux private-directory creation and fixture-owner defects. Scoped re-review /root/a1_registry_r2 confirms both findings addressed, no new Critical/Important breakage and no out-of-scope observations. Windows focused 34 passed/4 POSIX-skipped; controller exact-archive Linux GREEN 29 passed/zero skipped in0.55s. A1 pushed and verified on origin/codex/kontextmodell-20260907; main remains8c3df79 and no activation occurred. Initial A1 full suite was 1744 passed/12 skipped/97 subtests before R1/R2; the next full suite must include those fixes.
- [x] Task 2 (A2): complete (commits e41458d..e08b48c, review clean). HEAD e08b48cae799c6cc22fdf6921c6c336c1f0a57df; implementer /root/a2_tennis_codec, reviewer /root/a2_codec_review, both gpt-5.6-sol high. Spec compliant and quality Approved; no findings or unresolved cross-task checks. Required A2 regression107 passed; full1798 passed/15 skipped/97 subtests in64.76s, no warnings. All five planned files committed, no other content changes. Exact e08b48c pushed and verified on origin/codex/kontextmodell-20260907; main remains8c3df79 and VPS unchanged. A3/A4 explicit-cutoff consumers remain separately required.
- [x] Task 3 (A3): complete (commits e08b48c..16d9c4e, review clean). Independent /root/a3_tour_review found one Important eligible-surface overflow bypass; /root/a3_tour_refresh fixed it in 16d9c4e88435017c5841e613b9cd91670313813a. Task 3: fix round 1/5 (1 addressed, 0 open; correction bf4bff8..16d9c4e, scoped package d4ece59..16d9c4e). /root/a3_surface_rereview (gpt-5.6-sol high) confirms ADDRESSED, no new Critical/Important breakage or out-of-scope observations. Controller read full original report and complete appended fix evidence: RED8 failed, GREEN8 passed, focused202 passed/3 skipped in6.33s, no warnings. Previous full1851 passed/15 skipped/97 subtests in65.07s is tied to d4ece59, not the later fix; next task full suite includes it. Exact16d9c4e pushed and verified on origin/codex/kontextmodell-20260907; main remains8c3df79, VPS unchanged. A3 commits include original8f960fc, authorized deterministic ScanJob-test d4ece59, and separate controller-only source-audit bf4bff8. A4 decision-time consumption, real source refresh, empirical quality and D4/D5 restore/release remain downstream requirements, not claims established by A3 tests.
- [ ] Task 4 (A4): software APPROVED on c8935c22af0d4a5e5e03a575be8eb1b5d3f1dc40; R1 real append clock, R2 pending snapshot CAS and R3 strict canonical receipt retry closed. Full1887passed/15skipped, independent final132passed/0skipped plus original races and integrity/parity cases. Subsequent A3 real-data corrections fa98a3e4d107a44746391349b20225263b15e49c and 2436dd411987ba0b543fd73212da6cbb72d65e99 independently approved, full1904passed/15skipped/97subtests. Actual July-seed ATP/WTA builds now both succeed on Windows and isolated Linux; real static Linux backup/restore proves identical states, registry and forecasts (tour-restore-proof-20260908.md). Current source freshness, full linked D4, empirical/release work and production activation remain open. No context code deployed.
- [x] Task 5 (B1): software complete and independently APPROVED on22164fdf577190fa17f9aeddebd1b810ca4c59a1; all three original findings closed under explicit audit refs versus usable_refs contract. Full2079/15/97 and independent rereview281/4. Reports preserved and separate branch pushed as250c8c0bfc3c63e231a40e5f3356a4054040558d. Integrated with A4 R3, A3 real-data followup and released UI in0c50c5ab2a30c1ffd1f35a62be9c4dda863306b3: controller full2169/15/97, independent45exactblob comparisons plus9integrationtests, no findings. Controller read all reports. Original evidence Git blobs retained; raw working CRLF differences are documented. New adapted repro received only terminal-empty-line cleanup and all18cases were rerun successfully. No source certification, learned numeric effect or production activation implied; Cricket/price/money unchanged.
- [x] Task 6 (B2): software complete at a71d0955a74884a152c90fddd9bfae2e20602b34; independent review by /root/card_analysis_review has no findings. Exact four-file implementation, three prescribed learned losses/gradients, shared fit validation, training-only scaling, zero-reference and stable inverse links. Root's two valid large-JSON-integer errors and NumPy-alpha boundary were reproduced RED and fixed without caps/clipping. Implementer full 2324 passed / 15 skipped / 97 subtests (73.36s); independent 262 passed plus separate gradient, closed-form/score-root, Decimal, invalid-input and B1-compatibility proofs. Root read complete source, tests and both reports and verified all five frozen source/test/helper raw hashes. Evidence preserved in b1bafce79731c516b640b6f07e6ea9f7dfb50892, conflict-free merge d7294de65fcb71bbf011378a808316405929d185. Root compared all 361 non-SDD/non-handoff tracked blobs/modes to the reviewed B2 tree and ran final full regression: 2324 passed / 15 Windows skips / 97 subtests in 88.56s. No source, real effect artifact, empirical or live activation implied; price/ranking/Cricket/money unchanged.
- [ ] Task 7 (B3): next approved task, not started. Follow task-7-brief.md plus the now-tracked context-contract-decisions.md and validation-contract-decisions.md. Bind complete feature identity and exact effect/approval/scope to the common revision; typed model error or unavailable context must retain the valid base. No repeated spec/worktree/execution approval required.
- [ ] Task 8 (B4): pending
- [ ] Task 9 (B5): pending
- [ ] Task 10 (B6): pending
- [ ] Task 11 (B7): pending
- [ ] Task 12 (C1): pending
- [ ] Task 13 (C2): pending
- [ ] Task 14 (C3): pending
- [ ] Task 15 (C4): pending
- [ ] Task 16 (D1): pending
- [ ] Task 17 (D2): pending
- [ ] Task 18 (D3): pending
- [ ] Task 19 (D4): pending
- [ ] Task 20 (D5): pending

## Cross-task file and interface scan

| Tasks | Produced/consumed shared boundary | Finding / implementation constraint |
| --- | --- | --- |
| A1 / A3 | artifact registry; manifest CAS | Use A1 storage/hash/CAS primitives and one DB; immutable references required for backup/rollback. |
| A1 / A4 | artifact registry | Use A1 storage/hash/CAS primitives and one DB; immutable references required for backup/rollback. |
| A1 / B1 | artifact registry | Use A1 storage/hash/CAS primitives and one DB; immutable references required for backup/rollback. |
| A1 / B3 | artifact registry; manifest CAS | Use A1 storage/hash/CAS primitives and one DB; immutable references required for backup/rollback. |
| A1 / D1 | artifact registry | Use A1 storage/hash/CAS primitives and one DB; immutable references required for backup/rollback. |
| A1 / D2 | artifact registry; manifest CAS | Use A1 storage/hash/CAS primitives and one DB; immutable references required for backup/rollback. |
| A1 / D4 | model_artifacts.py; artifact registry; manifest CAS | Use A1 storage/hash/CAS primitives and one DB; immutable references required for backup/rollback. |
| A1 / D5 | artifact registry; manifest CAS | Use A1 storage/hash/CAS primitives and one DB; immutable references required for backup/rollback. |
| A2 / A3 | tennis/model_state.py; ModelState; state codec | Tour namespace and legacy identity explicit; no name-based tour split or freshness inheritance. |
| A2 / A4 | ModelState | Tour namespace and legacy identity explicit; no name-based tour split or freshness inheritance. |
| A2 / B7 | ModelState | Tour namespace and legacy identity explicit; no name-based tour split or freshness inheritance. |
| A2 / D1 | ModelState | Tour namespace and legacy identity explicit; no name-based tour split or freshness inheritance. |
| A2 / D4 | state codec | Same named contract; preserve exact type, scope and identity between producers/consumers; no code copies. |
| A3 / A4 | ModelState; artifact registry; tour loader | Tour namespace and legacy identity explicit; no name-based tour split or freshness inheritance. |
| A3 / B1 | artifact registry | Use A1 storage/hash/CAS primitives and one DB; immutable references required for backup/rollback. |
| A3 / B3 | artifact registry; manifest CAS | Use A1 storage/hash/CAS primitives and one DB; immutable references required for backup/rollback. |
| A3 / B7 | ModelState | Tour namespace and legacy identity explicit; no name-based tour split or freshness inheritance. |
| A3 / D1 | ModelState; artifact registry | Tour namespace and legacy identity explicit; no name-based tour split or freshness inheritance. |
| A3 / D2 | artifact registry; manifest CAS | Use A1 storage/hash/CAS primitives and one DB; immutable references required for backup/rollback. |
| A3 / D4 | state codec; artifact registry; manifest CAS | Use A1 storage/hash/CAS primitives and one DB; immutable references required for backup/rollback. |
| A3 / D5 | artifact registry; manifest CAS | Use A1 storage/hash/CAS primitives and one DB; immutable references required for backup/rollback. |
| A4 / B1 | artifact registry | Use A1 storage/hash/CAS primitives and one DB; immutable references required for backup/rollback. |
| A4 / B3 | artifact registry | Use A1 storage/hash/CAS primitives and one DB; immutable references required for backup/rollback. |
| A4 / B7 | tennis/predict.py; ModelState | Tour namespace and legacy identity explicit; no name-based tour split or freshness inheritance. |
| A4 / D1 | ModelState; artifact registry | Tour namespace and legacy identity explicit; no name-based tour split or freshness inheritance. |
| A4 / D2 | artifact registry | Use A1 storage/hash/CAS primitives and one DB; immutable references required for backup/rollback. |
| A4 / D4 | artifact registry | Use A1 storage/hash/CAS primitives and one DB; immutable references required for backup/rollback. |
| A4 / D5 | artifact registry | Use A1 storage/hash/CAS primitives and one DB; immutable references required for backup/rollback. |
| B1 / B2 | context_models/contracts.py; FeatureVector; EffectArtifact | Same named contract; preserve exact type, scope and identity between producers/consumers; no code copies. |
| B1 / B3 | context_models/contracts.py; artifact registry; FeatureVector; BaseDistribution; EffectArtifact; ContextResult | Use A1 storage/hash/CAS primitives and one DB; immutable references required for backup/rollback. |
| B1 / B4 | Observation; FeatureVector; BaseDistribution | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| B1 / B5 | Observation; FeatureVector; BaseDistribution; EffectArtifact; TrainingRow | Explicit family/head routing required; keep one event per split and retain evidence times. |
| B1 / B6 | Observation; FeatureVector; BaseDistribution | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| B1 / B7 | Observation; FeatureVector; BaseDistribution; EffectArtifact; TrainingRow | Explicit family/head routing required; keep one event per split and retain evidence times. |
| B1 / C1 | Observation; FeatureVector; BaseDistribution; EffectArtifact | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| B1 / C2 | Observation; FeatureVector; BaseDistribution; EffectArtifact; TrainingRow | Explicit family/head routing required; keep one event per split and retain evidence times. |
| B1 / C3 | Observation; FeatureVector; BaseDistribution; EffectArtifact; TrainingRow | Explicit family/head routing required; keep one event per split and retain evidence times. |
| B1 / C4 | Observation; FeatureVector; BaseDistribution; EffectArtifact; TrainingRow | Explicit family/head routing required; keep one event per split and retain evidence times. |
| B1 / D1 | artifact registry; Observation; FeatureVector; BaseDistribution; EffectArtifact; TrainingRow | Explicit family/head routing required; keep one event per split and retain evidence times. |
| B1 / D2 | artifact registry; FeatureVector; BaseDistribution; EffectArtifact; TrainingRow | Explicit family/head routing required; keep one event per split and retain evidence times. |
| B1 / D3 | Observation; FeatureVector; BaseDistribution; EffectArtifact; ContextResult | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| B1 / D4 | artifact registry; Observation; EffectArtifact; ContextResult | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| B1 / D5 | artifact registry | Use A1 storage/hash/CAS primitives and one DB; immutable references required for backup/rollback. |
| B2 / B3 | context_models/contracts.py; FeatureVector; EffectArtifact | Same named contract; preserve exact type, scope and identity between producers/consumers; no code copies. |
| B2 / B4 | FeatureVector; offset fit | Each sport uses its declared link/head and training-only scale; no coefficient or coverage transfer. |
| B2 / B5 | FeatureVector; EffectArtifact; offset fit | Each sport uses its declared link/head and training-only scale; no coefficient or coverage transfer. |
| B2 / B6 | FeatureVector | Same named contract; preserve exact type, scope and identity between producers/consumers; no code copies. |
| B2 / B7 | FeatureVector; EffectArtifact; offset fit | Each sport uses its declared link/head and training-only scale; no coefficient or coverage transfer. |
| B2 / C1 | FeatureVector; EffectArtifact; offset fit | Each sport uses its declared link/head and training-only scale; no coefficient or coverage transfer. |
| B2 / C2 | FeatureVector; EffectArtifact; offset fit | Each sport uses its declared link/head and training-only scale; no coefficient or coverage transfer. |
| B2 / C3 | FeatureVector; EffectArtifact; offset fit | Each sport uses its declared link/head and training-only scale; no coefficient or coverage transfer. |
| B2 / C4 | FeatureVector; EffectArtifact; offset fit | Each sport uses its declared link/head and training-only scale; no coefficient or coverage transfer. |
| B2 / D1 | FeatureVector; EffectArtifact; offset fit | Each sport uses its declared link/head and training-only scale; no coefficient or coverage transfer. |
| B2 / D2 | FeatureVector; EffectArtifact | Same named contract; preserve exact type, scope and identity between producers/consumers; no code copies. |
| B2 / D3 | FeatureVector; EffectArtifact | Same named contract; preserve exact type, scope and identity between producers/consumers; no code copies. |
| B2 / D4 | EffectArtifact | Same named contract; preserve exact type, scope and identity between producers/consumers; no code copies. |
| B3 / B4 | FeatureVector; BaseDistribution | Same named contract; preserve exact type, scope and identity between producers/consumers; no code copies. |
| B3 / B5 | FeatureVector; BaseDistribution; EffectArtifact; snapshot API | One immutable revision; pure computation; later reads/price changes do not recompute. |
| B3 / B6 | FeatureVector; BaseDistribution | Same named contract; preserve exact type, scope and identity between producers/consumers; no code copies. |
| B3 / B7 | FeatureVector; BaseDistribution; EffectArtifact; snapshot API | One immutable revision; pure computation; later reads/price changes do not recompute. |
| B3 / C1 | FeatureVector; BaseDistribution; EffectArtifact | Same named contract; preserve exact type, scope and identity between producers/consumers; no code copies. |
| B3 / C2 | FeatureVector; BaseDistribution; EffectArtifact | Same named contract; preserve exact type, scope and identity between producers/consumers; no code copies. |
| B3 / C3 | FeatureVector; BaseDistribution; EffectArtifact | Same named contract; preserve exact type, scope and identity between producers/consumers; no code copies. |
| B3 / C4 | FeatureVector; BaseDistribution; EffectArtifact | Same named contract; preserve exact type, scope and identity between producers/consumers; no code copies. |
| B3 / D1 | artifact registry; FeatureVector; BaseDistribution; EffectArtifact | Use A1 storage/hash/CAS primitives and one DB; immutable references required for backup/rollback. |
| B3 / D2 | artifact registry; manifest CAS; FeatureVector; BaseDistribution; EffectArtifact; approval | Exact effect/population/coverage approval; no legacy certification transfer; base stays visible. |
| B3 / D3 | FeatureVector; BaseDistribution; EffectArtifact; ContextResult; snapshot API; approval | Exact effect/population/coverage approval; no legacy certification transfer; base stays visible. |
| B3 / D4 | artifact registry; manifest CAS; EffectArtifact; ContextResult; snapshot API; approval | Exact effect/population/coverage approval; no legacy certification transfer; base stays visible. |
| B3 / D5 | artifact registry; manifest CAS; snapshot API; approval | Exact effect/population/coverage approval; no legacy certification transfer; base stays visible. |
| B4 / B5 | challenge_engine.py; context_models/football.py; Observation; FeatureVector; BaseDistribution; offset fit; football context | Each sport uses its declared link/head and training-only scale; no coefficient or coverage transfer. |
| B4 / B6 | Observation; FeatureVector; BaseDistribution | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| B4 / B7 | Observation; FeatureVector; BaseDistribution; offset fit | Each sport uses its declared link/head and training-only scale; no coefficient or coverage transfer. |
| B4 / C1 | challenge_15k.py; context_sources/football.py; context_models/football.py; Observation; FeatureVector; BaseDistribution; offset fit; football context | Each sport uses its declared link/head and training-only scale; no coefficient or coverage transfer. |
| B4 / C2 | Observation; FeatureVector; BaseDistribution; offset fit | Each sport uses its declared link/head and training-only scale; no coefficient or coverage transfer. |
| B4 / C3 | Observation; FeatureVector; BaseDistribution; offset fit | Each sport uses its declared link/head and training-only scale; no coefficient or coverage transfer. |
| B4 / C4 | Observation; FeatureVector; BaseDistribution; offset fit | Each sport uses its declared link/head and training-only scale; no coefficient or coverage transfer. |
| B4 / D1 | Observation; FeatureVector; BaseDistribution; offset fit; football context | Each sport uses its declared link/head and training-only scale; no coefficient or coverage transfer. |
| B4 / D2 | challenge_engine.py; FeatureVector; BaseDistribution | Same named contract; preserve exact type, scope and identity between producers/consumers; no code copies. |
| B4 / D3 | Observation; FeatureVector; BaseDistribution; football context | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| B4 / D4 | Observation | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| B5 / B6 | Observation; FeatureVector; BaseDistribution | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| B5 / B7 | Observation; FeatureVector; BaseDistribution; EffectArtifact; TrainingRow; offset fit; snapshot API | Explicit family/head routing required; keep one event per split and retain evidence times. |
| B5 / C1 | context_models/football.py; Observation; FeatureVector; BaseDistribution; EffectArtifact; offset fit; football context | Each sport uses its declared link/head and training-only scale; no coefficient or coverage transfer. |
| B5 / C2 | Observation; FeatureVector; BaseDistribution; EffectArtifact; TrainingRow; offset fit | Explicit family/head routing required; keep one event per split and retain evidence times. |
| B5 / C3 | Observation; FeatureVector; BaseDistribution; EffectArtifact; TrainingRow; offset fit | Explicit family/head routing required; keep one event per split and retain evidence times. |
| B5 / C4 | Observation; FeatureVector; BaseDistribution; EffectArtifact; TrainingRow; offset fit | Explicit family/head routing required; keep one event per split and retain evidence times. |
| B5 / D1 | Observation; FeatureVector; BaseDistribution; EffectArtifact; TrainingRow; offset fit; football context | Explicit family/head routing required; keep one event per split and retain evidence times. |
| B5 / D2 | challenge_engine.py; FeatureVector; BaseDistribution; EffectArtifact; TrainingRow | Explicit family/head routing required; keep one event per split and retain evidence times. |
| B5 / D3 | Observation; FeatureVector; BaseDistribution; EffectArtifact; football context; snapshot API | One immutable revision; pure computation; later reads/price changes do not recompute. |
| B5 / D4 | Observation; EffectArtifact; snapshot API | One immutable revision; pure computation; later reads/price changes do not recompute. |
| B5 / D5 | snapshot API | One immutable revision; pure computation; later reads/price changes do not recompute. |
| B6 / B7 | context_models/tennis.py; Observation; FeatureVector; BaseDistribution; tennis context | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| B6 / C1 | Observation; FeatureVector; BaseDistribution | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| B6 / C2 | Observation; FeatureVector; BaseDistribution | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| B6 / C3 | Observation; FeatureVector; BaseDistribution | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| B6 / C4 | Observation; FeatureVector; BaseDistribution | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| B6 / D1 | Observation; FeatureVector; BaseDistribution; tennis context | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| B6 / D2 | FeatureVector; BaseDistribution | Same named contract; preserve exact type, scope and identity between producers/consumers; no code copies. |
| B6 / D3 | Observation; FeatureVector; BaseDistribution; tennis context | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| B6 / D4 | Observation | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| B7 / C1 | Observation; FeatureVector; BaseDistribution; EffectArtifact; offset fit | Each sport uses its declared link/head and training-only scale; no coefficient or coverage transfer. |
| B7 / C2 | Observation; FeatureVector; BaseDistribution; EffectArtifact; TrainingRow; offset fit | Explicit family/head routing required; keep one event per split and retain evidence times. |
| B7 / C3 | Observation; FeatureVector; BaseDistribution; EffectArtifact; TrainingRow; offset fit | Explicit family/head routing required; keep one event per split and retain evidence times. |
| B7 / C4 | Observation; FeatureVector; BaseDistribution; EffectArtifact; TrainingRow; offset fit | Explicit family/head routing required; keep one event per split and retain evidence times. |
| B7 / D1 | ModelState; Observation; FeatureVector; BaseDistribution; EffectArtifact; TrainingRow; offset fit; tennis context | Explicit family/head routing required; keep one event per split and retain evidence times. |
| B7 / D2 | FeatureVector; BaseDistribution; EffectArtifact; TrainingRow | Explicit family/head routing required; keep one event per split and retain evidence times. |
| B7 / D3 | Observation; FeatureVector; BaseDistribution; EffectArtifact; tennis context; snapshot API | One immutable revision; pure computation; later reads/price changes do not recompute. |
| B7 / D4 | Observation; EffectArtifact; snapshot API | One immutable revision; pure computation; later reads/price changes do not recompute. |
| B7 / D5 | snapshot API | One immutable revision; pure computation; later reads/price changes do not recompute. |
| C1 / C2 | Observation; FeatureVector; BaseDistribution; EffectArtifact; offset fit | Each sport uses its declared link/head and training-only scale; no coefficient or coverage transfer. |
| C1 / C3 | Observation; FeatureVector; BaseDistribution; EffectArtifact; offset fit | Each sport uses its declared link/head and training-only scale; no coefficient or coverage transfer. |
| C1 / C4 | Observation; FeatureVector; BaseDistribution; EffectArtifact; offset fit | Each sport uses its declared link/head and training-only scale; no coefficient or coverage transfer. |
| C1 / D1 | Observation; FeatureVector; BaseDistribution; EffectArtifact; offset fit; football context | Each sport uses its declared link/head and training-only scale; no coefficient or coverage transfer. |
| C1 / D2 | FeatureVector; BaseDistribution; EffectArtifact | Same named contract; preserve exact type, scope and identity between producers/consumers; no code copies. |
| C1 / D3 | Observation; FeatureVector; BaseDistribution; EffectArtifact; football context | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| C1 / D4 | Observation; EffectArtifact | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| C2 / C3 | sports_prematch.py; context_models/team_sports.py; Observation; FeatureVector; BaseDistribution; EffectArtifact; TrainingRow; offset fit; team context | Explicit family/head routing required; keep one event per split and retain evidence times. |
| C2 / C4 | Observation; FeatureVector; BaseDistribution; EffectArtifact; TrainingRow; offset fit | Explicit family/head routing required; keep one event per split and retain evidence times. |
| C2 / D1 | Observation; FeatureVector; BaseDistribution; EffectArtifact; TrainingRow; offset fit; team context | Explicit family/head routing required; keep one event per split and retain evidence times. |
| C2 / D2 | FeatureVector; BaseDistribution; EffectArtifact; TrainingRow | Explicit family/head routing required; keep one event per split and retain evidence times. |
| C2 / D3 | Observation; FeatureVector; BaseDistribution; EffectArtifact; team context | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| C2 / D4 | Observation; EffectArtifact | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| C3 / C4 | Observation; FeatureVector; BaseDistribution; EffectArtifact; TrainingRow; offset fit | Explicit family/head routing required; keep one event per split and retain evidence times. |
| C3 / D1 | Observation; FeatureVector; BaseDistribution; EffectArtifact; TrainingRow; offset fit; team context | Explicit family/head routing required; keep one event per split and retain evidence times. |
| C3 / D2 | FeatureVector; BaseDistribution; EffectArtifact; TrainingRow | Explicit family/head routing required; keep one event per split and retain evidence times. |
| C3 / D3 | Observation; FeatureVector; BaseDistribution; EffectArtifact; team context | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| C3 / D4 | Observation; EffectArtifact | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| C4 / D1 | Observation; FeatureVector; BaseDistribution; EffectArtifact; TrainingRow; offset fit; esports context | Explicit family/head routing required; keep one event per split and retain evidence times. |
| C4 / D2 | FeatureVector; BaseDistribution; EffectArtifact; TrainingRow | Explicit family/head routing required; keep one event per split and retain evidence times. |
| C4 / D3 | Observation; FeatureVector; BaseDistribution; EffectArtifact; esports context | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| C4 / D4 | Observation; EffectArtifact | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| D1 / D2 | artifact registry; FeatureVector; BaseDistribution; EffectArtifact; TrainingRow; experiment | Explicit family/head routing required; keep one event per split and retain evidence times. |
| D1 / D3 | Observation; FeatureVector; BaseDistribution; EffectArtifact; football context; tennis context; team context; esports context | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| D1 / D4 | artifact registry; Observation; EffectArtifact | Native IDs and actual observation time retained; unavailable data is not healthy or zero. |
| D1 / D5 | artifact registry; experiment | Use A1 storage/hash/CAS primitives and one DB; immutable references required for backup/rollback. |
| D2 / D3 | FeatureVector; BaseDistribution; EffectArtifact; approval | Exact effect/population/coverage approval; no legacy certification transfer; base stays visible. |
| D2 / D4 | artifact registry; manifest CAS; EffectArtifact; approval | Exact effect/population/coverage approval; no legacy certification transfer; base stays visible. |
| D2 / D5 | artifact registry; manifest CAS; experiment; approval | Exact effect/population/coverage approval; no legacy certification transfer; base stays visible. |
| D3 / D4 | Observation; EffectArtifact; ContextResult; snapshot API; approval | Exact effect/population/coverage approval; no legacy certification transfer; base stays visible. |
| D3 / D5 | snapshot API; approval | Exact effect/population/coverage approval; no legacy certification transfer; base stays visible. |
| D4 / D5 | artifact registry; manifest CAS; snapshot API; approval | Exact effect/population/coverage approval; no legacy certification transfer; base stays visible. |


## Execution continuation

- Final B2 integration checkpoint: source d7294de65fcb71bbf011378a808316405929d185 includes the independently approved a71d095 and evidence b1bafce; no merge conflicts. Root command: quality Python -B -m pytest -q -rs -p no:cacheprovider --basetemp=.pytest_tmp/context-b2-final-integrated-20260908-01; exit 0, 2324 passed / 15 skipped / 97 subtests in 88.56s. Skips are the same unchanged Windows POSIX/symlink cases listed in task-6-report.md. Exact comparison of 361 tracked files outside SDD/handoff with the reviewed B2 tree passed; raw pinned helper unchanged. The independent standalone repro intentionally requires historical a71d095 HEAD: rerun in an isolated exact-source checkout, not against an arbitrary later docs/merge HEAD. UI main and VPS remain 4f3ecd5db75cb6f4f466270a568ede742d4ed767 (fresh read-only confirmation after QA; clean production tree, app/Caddy running). No B3 task was dispatched; next work begins there. Numeric injury/fatigue effects, real causal data, empirical acceptance, remaining B3-D5 and separate known worker errors are not completed by this checkpoint.

- Reviewed integration closure: task-5-integration-review-20260908.md confirms no findings on0c50c5a, exact5B1/10UI/30Tennis-runtime blobs and unchanged pinned helper; independent9passed. Controller read the complete report. Documentation-only preservationb4f7cdc additionally fixes the handoff gap:20task briefs now tracked with exact A1-D5 mapping, all5plan Git blobs still equal54314ca, validation decisions/source map/reports and sanitized public-source metadata included. No database, model binary or archive added; original report/plan content retained apart from disclosed Markdown end-whitespace hygiene. B1 remote exact250c8c0 verified. Current source remains the2169/15/97-tested0c50c5a; B2 stays isolated and in progress.

- Integration checkpoint: reviewed B1 branch250c8c0 merged as c8f2270, released main4f3ecd5 merged as0c50c5ab2a30c1ffd1f35a62be9c4dda863306b3, both conflict-free. Controller stable complete suite on exact0c50c5a:2169passed/15skipped/97subtests in72.36s, exit0, pinned helper rawSHA1441158 unchanged. Independent bounded integration review assigned; no source edits in this worktree during review. B2 implementation started separately from0c50c5a with own index/source/test tree; preserve that work on any interruption. Full spec/worktree/execution approvals remain in force.

- Current controller checkpoint2026-09-08 17:48Z supersedes older current-status entries below. UI main/GitHub/VPS exact4f3ecd5db75cb6f4f466270a568ede742d4ed767; fresh normal17:07football refresh and rendered Porto desktop/mobile proof completed. Porto's stored1.527/1.133goal rates explain the46.4%home-win forecast directly, with53.6%combinedcounterrisk and explicit provisional cross-league limitation; no probability/rank/price changes. Real1440/390/320render shows23cards/23analyses on page1, no old expander/checklist or horizontal overflow,0errors/9existingwarnings. Final updater backups each87DB verified. App/Caddy and seven timers remain healthy; the known tennis rebuild and settlement-ambiguity worker errors are not falsely cleared by this UI release.
- Context A3 repair2436dd4 independently approved after real-data diagnosis: strict atomic bilateral serve admission in the new separated build/calibration path only, no clipping or codec relaxation, valid Elo preserved. Windows full1904/15/97; reviewer239/3 plus62invalid/fourvalid parity cases. Fresh real cached builds pass both tours on Windows and VPS. Isolated Linux static restore passed17:47Z with exact registry/state/full-output equality; no local model DB was transferred, no production DB/key/env read, existing40seedfiles unchanged. Full commands, hashes, permissions and limitations in tour-restore-proof-20260908.md. B1 final independent rereview accepted22164f, reports preserved250c8c0. Next: combine reviewed source in the context feature worktree, run stable full regression, push scoped branches and preserve handoff; B2-D5 and current-source/empirical activation remain unfinished.

- Current controller checkpoint2026-09-08 16:52Z supersedes historical entries below. A4 software reviewed and feature-pushed c8935c2; B1 separate implementation underway with explicit coverage/parameter/payload/historical-class decisions appended to context-contract-decisions.md. No repeated spec/worktree approval required.
- UI-only release independently reviewed f82d7ae, full1795passed/11skipped/97subtests, independent313/26; published with docs as ce7b98ccf365a5db4303ed04291058acdbe6067b. Root-owned trusted updater exited0; preupdate and normal backups each87DB, exact VPS hash and clean tracked checkout verified; app/Caddy enabled/active, public/internal healthok, seven timers enabled/active. Fresh public browser23cards/23analyses, no old expander/checklist, no desktop overflow,0errors/9preexistingwarnings. Local actual render1440/761/390/320 passed before release. Normal16:37contextrefresh yielded17numerical envelopes with original modelclocks retained; Porto lastchecked16:07 and not yet due, so exact Porto postrefresh/mobile evidence pending. Known settlement ambiguity and old combined tennis rebuild errors remain, no successful UI health relabels them.
- Real offline tour build on c8935c2, isolated .pytest_tmp/tour-offline-realdata-20260908-01, real packaged source files only and requests forbidden: WTA published9baf129c88fb6f5ac5b546a64f6b919d9d3a1bc124f6f2b30216a0379e3430cd with result_date2026-07-26; ATP failed ValueError. Public CLI resultREFRESH_PARTIAL/exit1. A targeted direct ATP diagnostic pinpoints serve_model._validate_serve_payload: breaks exceed return games. /root/a4_tour_readers assigned diagnosis-only before any math/source/validation change. This demonstrates independent continuation but not a complete two-tour build or freshness. No VPS runtime writes. /root/card_analysis_review prepares read-only tour-backup preflight against unchanged trusted helper APIs; full D4 still depends on B3.

- A4 software commit870224c122beb4763c8eef31268212fe85deb14d: focused385passed/7skipped, full1878passed/15skipped. Controller read complete task-4-report.md. Independent reviewer /root/a4_independent_review independently ran267passed/4skipped, then reproduced two blockers through actual public paths: initial write after real kickoff uses stale decisionclock (R1/P1), and pending stalekickoff rewinds a newer native reschedule (R2/P2). Controller read complete task-4-independent-review.md and returned both to original implementer for RED/GREEN repair and full re-review. NOT APPROVED, not pushed or deployed. Prior forecast/status/price history must remain intact; no source or acceptance-rule relaxation.

- Account continuation2026-09-08: current source/Git/agent status reconstructed; A4 WIP preserved and original implementer resumed. Superpowers6.3.0 is no longer available in this account's installed skills; the user-approved task briefs, TDD, scoped commits and independent reviews remain the execution contract. No skill reinstallation, new spec approval or restart from A1. Fresh A4 checkpoint385passed/7skipped; full suite/report/commit/review still pending.

- Additive user UX request2026-09-08: replace the useless public context checklist with a short directly visible reason for the selected outcome and a truthful counterargument. Controller created an independent8c3df7978bf074cfe6ce3d9a794f6f60139327ea-based worktree/branch codex/erklaerbare-karten-20260908 and dispatched /root/card_analysis. Scope is pure evidence projection/card rendering, not model/price/ranking/Cricket/15K changes. No overlapping files or shared index with A4; neither implementer may merge the sibling branch. UI review/test/render/main/push/VPS release is separate from A1-D5. Root-owned tracked source audit and handoff changes stay outside A4's code commit.

- Bounded fresh production diagnosis2026-09-08T15:56Z: VPS clean b3afc478a07fa0d67509e2bef0a9c05766bdb077; app/Caddy active, seven timers scheduled, internalhealthok. Two failed worker units are real, not dismissed by healthcheck. Tennis journal reports ATP197212matches with tournament-start proxy2026-09-07 then combined REFRESH_FAILED HTTPError, old state retained. Wettfinder artifact generated15:37:17.822519Z has footballcompleted/error0 and overallerror1 from riskobet settlement (ambiguous_settlement_revisions / event_snapshot_ambiguous); additional unresolved-result/event-limit diagnostics and existing Cricket source failure remain explicit. Read-only stdlib journal/status probes, no model/data mutations, manual restart or deployment.

- A4 interim implementer checkpoint (not completion/review): cross-tour guard1/1; manifest/state decision cutoff, exact legacy pickle digest and context model identity80 passed/4 skipped including A1/A2 modules; observed-status/atomic cancellation guard2/2 passed. Initial/pending per-tour selection, already-fetched source status wiring, changed-artifact refresh/diagnostics, pipeline regression and final focused/full runs remained open at that checkpoint. No blocker reported; controller has not yet received the final report or committed A4 bytes.

- Controller source-only backup check: existing stage_runtime_databases.discover_databases recursively discovers .db/.sqlite/.sqlite3 beneath the fixed application root, and does not exclude runtime_state. Thus the default in-root context_models.db matches current discovery; this is not proof for an externally configured runtime root or a restored tour manifest. The D4 actual-root, linked-artifact, concurrent snapshot and restoration tests remain required. No pinned helper bytes were changed and no backup/deployment was run by this check.

- Controller bounded read-only corpus checks17:58Z and18:05Z: common forecast archive football5878rows/19storedkeys, tennis116/5, esports84/8; four result-bearing keys across those sports. Legacy tennis1239rows has match_duration_minutes present but zero nonnull, 104result receipts, no exact end/native participant columns;375revisions/63predictionIDs. Older shadow_clv contains one result-bearing fixture and no context archive. Three databases only, not exhaustive legacy discovery or canonical D2 evidence. Sanitized counts/schema limits persisted in corpus-inventory-20260907.md and tracked source audit; B6/D1 briefs route the evidence. No odds/account values, provider fetches or database mutations.

- A4 controller source proof: exactly three memory-only VPS GETs17:27Z. ATP current tournaments/matches both200, validators accept2026 coverage229/11680 respectively, last tournament-start proxy2026-09-07. WTA existing2026w/2026.xlsx503, no parse/retry. No cache/model/DB writes or publication. Raw metadata/hash evidence in training-source-probe-20260907.json and tracked source audit; source availability is not a completed build or A4 activation. A4 implementer notified to keep controller-owned audit changes out of its code commit.

- A2 review package review-e41458d..e08b48c.diff (1commit/36903bytes) was generated by the helper; controller read complete implementer report and both reviewer verdicts. A2 full suite includes A1 R1/R2 fixes. Branch push was exact immutable e08b48c, not uncommitted state. A3 is next; no A2 fix round needed.
- A non-blocking user question about private/non-commercial versus commercial BetBoy use is awaiting response for possible weather archive selection. No new archive data was fetched, subscription accepted or tariff changed. Other work continues. Controller also read primary Open-Meteo terms and OpenWeather/Single-Runs documentation; archive entitlement and applicable use remain unresolved.

- A2 explicit decision_cutoff proposal was approved and recorded in the ruling above; A3/A4 briefs bind downstream decision checks. Additional A3 clock/calibration-cutoff/training-metadata integration questions are in task-3-brief.md and require controller clarification before that part. A2 initial focused47 passed; self-review added RED for non-JSON rating tuples and schema1.0 (2failed/47passed), fix/regression still pending at this checkpoint.
- Auxiliary weather source preparation completed; controller read source-readiness-weather.md completely. Current OpenWeather forecast_at is valid time, not issue/receipt time, and later enters an observed_at slot; city-centroid lookup drops exact venue identity and no saved forecast history exists. Published Open-Meteo archive documentation/terms were inspected by the agent, but no weather-data probe, credentials, account entitlement, paid subscription, or C1 implementation is implied. C1 remains open. Sanitized team-sport probe results and the non-retryable NHL web-documentation lookup are recorded separately in source-probe-evidence-team-sports.md.

- All twenty task briefs exist in the active SDD workspace; helper generation completed with exit0. Existing amended briefs were not overwritten. Auxiliary /root/remaining_sports_sources (gpt-5.6-sol, medium) completed bounded read-only C2-C4 source readiness; controller read source-readiness-basketball-hockey-esports.md completely. Current feeds retain event/team/result identity but no normalized player/rotation/TOI/goalie/roster/patch/map observations; PandaScore lacks a persistent account daily governor. No probes or production/Git changes by that agent. These are source gaps to implement/verify, not C2-C4 completion. A2 is now the sole implementation worker.
- R2 Linux RED used exactly the implementer's test-only file SHA256 adb784a385ad57de3f29bed42ccf85d0a94ae8ca7d19f2993281338a5c7354e7 on unchanged d292c21 production source in a1-r2-red: 4 failed,25 passed in0.66s, all creation failures, corrected owner fixture passing. Then exact e41458d archive SHA256 a9b7c5f7cf054bd097b23d33bed059a5b9824fbe581314aed4e81306135e9876 was verified both hosts and extracted separately to a1-e41458d. Command env PYTHONPATH=/tmp/betboy-context-qa.9xr68INa/a1-e41458d /tmp/betboy-context-qa.9xr68INa/venv/bin/python -m pytest /tmp/betboy-context-qa.9xr68INa/a1-e41458d/tests/test_model_artifacts.py -q -p no:cacheprovider --basetemp=/tmp/betboy-context-qa.9xr68INa/a1-tests-r2-green: 29 passed in0.55s, exit0, no skips or warnings. Non-root ubuntu, actual umask0002. This closes platform execution gap if code re-review also passes; it is not a deployment.
- Additional B4 preparation (not a ruling or completed implementation): football_data_history.py uses negative pseudo fixture IDs for CSV results and merge_api_tail keeps CSV rows when a date/name key already exists; tail team IDs can also be remapped. challenge_engine._team_observations currently drops source fixture identity. B4 must preserve exact baseline contribution/provenance and require a verified native appearance join, never pass a negative CSV identifier as an API fixture ID or silently use a different roster window. _hybrid_strength includes separate goal/xG samples and league-prior shrinkage; do not invent recency decay absent from current math.

- Auxiliary read-only preparation agent /root/context_source_readiness (gpt-5.6-sol, medium) completed source-readiness-football-tennis.md; controller read it completely and independently checked injury status collapse, missing appearance minutes, parser set-only output, and observation time versus match end in the named functions. No provider calls, production edits or Git writes by that agent. These are preparations, not B4/B6 completion; A1 remains the sole implementation worker.
- Bounded WTA HEAD probes at 2026-09-07T14:14:03Z: existing 2026 XLSX HTTPS source raised SSLError, existing HTTP source returned 503. Two HEAD requests only, no TLS bypass, cache replacement or model publication. Current WTA source freshness is not verified.
- Official API-Football documentation was followed by real bounded source checks. Actual probes and sanitized artifacts are recorded in source-probe-evidence.md: exactly two governed football requests (batched fixtures, batched injuries), three local tennis requests including one corrected diagnostic. Completed football sample contains minutes/roles/native IDs; ESPN samples contain set-games/native IDs but no measured duration or match end. No model/ticket changes; football budget reservations are the only production data writes.
- Isolated Linux QA exists only under /tmp/betboy-context-qa.9xr68INa (ubuntu-owned 0700), with its own pytest 9.1.1 venv; app venv unchanged. Real app directories verified betboy-owned 0750. d292c21 archive SHA256 882bbd2d7fb8f5da3b620211de7563e2c43ce21017d14a6bd875442b34b3d745 matches local/remote; extracted only three A1 files to a1-d292c21. Command: env PYTHONPATH=/tmp/betboy-context-qa.9xr68INa/a1-d292c21 /tmp/betboy-context-qa.9xr68INa/venv/bin/python -m pytest /tmp/betboy-context-qa.9xr68INa/a1-d292c21/tests/test_model_artifacts.py -q -p no:cacheprovider --basetemp=/tmp/betboy-context-qa.9xr68INa/a1-tests-01. Result 3 failed, 24 passed in 0.60s. Default umask 0002 produces new parents mode0775; positive sticky fixture also omits root UID from trusted-owner set. Tests and source isolation are preserved for diagnosis, not deployed.
- Fresh read-only VPS check: b3afc478a07fa0d67509e2bef0a9c05766bdb077, clean checkout, app and Caddy active, seven timers planned, internal health ok. VPS runtime Python 3.12.3 / SQLite 3.45.1 has numpy/scipy but no pytest. WSL is not installed locally; Linux-only tests need isolated later verification, never an install into the app venv.
- Setup-only commit 8c3df7978bf074cfe6ce3d9a794f6f60139327ea was pushed to main and confirmed with ls-remote. It contains only the .worktrees/ ignore rule; no functional VPS deployment was performed.

- Clean worktree baseline finally verified with `.pytest_tmp/wb04`: 1724 passed, 11 skipped, 97 subtests passed in 58.58s; exit 0. Only setup directory/line-ending normalization was required, with no application diff. Task A1 dispatched after this result.

- This is the active ledger after explicit worktree consent. Parent checkout scratch is a pre-execution record only; do not dispatch there.
- New worktree baseline initially failed because pytest basetemp parent `.pytest_tmp` did not yet exist (WinError 3 at pathlib.mkdir). Focused `-x` reproduction: 4 passed, 1 setup error. Only local directory setup is being corrected; no production fix or relaxed test criterion.
- The next focused full-suite run reached 1134 passed and 4 skipped before the deploy-helper raw-byte pin failed. Evidence: Git core.autocrlf=true changed scripts/stage_runtime_databases.py from LF to CRLF; worktree SHA-256 4e480c5f421565dbaf267141bce899ec36b17226b05652816f97e4d28bb9084a versus original 1441158c542e97a19b193fa0cd091b645ec6442d6d8157f1d4fceabbba72b026. The existing dos2unix formatter restored the original bytes; git diff confirms no tracked change. No hash-pin or test was weakened.
- The same line-ending conversion changed raw plan-file SHA-256 values. All five source files were independently verified identical after CRLF-to-LF normalization AND by Git blob identity against the main checkout. Original raw hashes remain in source-map.json as source identity; do not misclassify checkout line endings as changed task requirements.
