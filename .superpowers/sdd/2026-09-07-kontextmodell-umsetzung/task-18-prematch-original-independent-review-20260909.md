# P3 independent same-call original review — 2026-09-09

## Verdict and boundary

**PASS for this narrowly scoped producer seam. No actionable findings.**

Reviewed the clean, unchanged commit
`ccefe858d5f2d5913bc1c39142e27fb6f52248ae` in the separate worktree
`C:/Projekt/BetBoy/betboy-app/.worktrees/review-sports-original-20260909`.
The complete five-file diff and owning audit, full prematch normalization,
fitting, prediction and prequential path, actual Risk adapter, changed frozen
default assertion, and D3 controller rulings were personally read. Existing
functions/numerics are unchanged; the diff adds only the explicitly optional
collector, detached record, callback pass-through and their tests.

This is not completed Basketball/Hockey A1/B1/D2/B3 production wiring, independent
calibration, real-source completeness, an approval, deployment, or a native-join
certificate. Existing C2/C3 replay exports may refit and are not implicitly the
new same-call capture. Root must still build and verify the owning live envelopes.
No provider, production, database publication or server action was performed.

## Independent executed evidence

- **39 new independent tests passed, zero skipped, 1.64 seconds**, first run.
- **464 passed, zero skipped, 57.92 seconds** in the broader affected run,
  including the same 39 independent cases plus 425 existing tests. Exit 0.
- No RED was observed on the frozen target; no source correction or expectation
  weakening was made. The probe source was written once and remains unchanged.
- All tests use synthetic local inputs. This review made no new real-source
  observations and asserts no empirical effect value. Owner-reported prior
  727/1 results are not counted as independent execution here.
- No P3 whole-repository run was repeated. The separate Tennis full suite is not
  evidence for this worktree and is deliberately not included in these counts.

The new probes cover:

1. Twenty-four sport/state combinations against a SHA-verified frozen original
   prematch module and the actual prior Risk adapter Git blob: Basketball,
   Hockey, Cricket; available, absent, neutral, freshly received archive,
   ongoing, cancelled correction, moved competition and duplicate histories.
   Complete prediction, Risk snapshot/candidate serialization and IDs compare
   exactly on this CPU; no numerical tolerance. All sixteen non-Cricket cases
   also compare the real optional Risk callback path against the default.
2. Explicit cold caches and actual fit/predict spies: each target fit and target
   prediction occurs exactly once, while causal earlier prequential folds still
   execute and produce evidence. Unrounded parameters/probabilities compare by
   exact float hex to that very target invocation, not a separately rounded fit.
3. Nested mutations inside the callback cannot alter caller event/history or the
   adapter output. Frozen normalized matches, fit coefficients and read-only
   values reject assignment. Reentrant callbacks do not share mutable raw state.
4. A single-use history iterator is traversed exactly once. UTC-offset-equivalent
   decision instants produce the same native-normalized original and Risk IDs.
5. Missing, ongoing and withdrawn histories remain missing; no collector turns
   `None` into a probability. Missing neutral-Hockey full-match basis is retained
   in the parity matrix, not replaced by a 50% value.
6. RuntimeError and ValueError from callbacks propagate through the actual Risk
   adapter. Explicit Cricket collection is rejected; absent collection remains
   byte-exact on all eight independent Cricket cases.

The changed owning AST test is consistent with the approved narrow exception:
only the optional keyword, the exact two no-else optional branches and assignment
followed by return of the otherwise unchanged result are removed for comparison.
Every original top-level function is compared. This was additionally checked
against the actual source diff; AST equality alone is not claimed to cover an
arbitrary future mutation of imports, constants or classes.

The raw source dictionaries intentionally remain mutable detached copies, and
may include source price metadata. They are not a price-free persistent artifact;
the later owning publisher must project/qualify them. Only the normalized original
model input hash and values are price-independent. This is documented, not a defect.

## Reproducible commands

Python: `C:/Projekt/BetBoy/betboy-app/.codex_test_venv/quality/Scripts/python.exe`.
All commands ran with this review worktree as cwd, `-B -m pytest -q -p no:cacheprovider`.

Independent run:

```text
.pytest_tmp/p3-original-independent-20260909/test_p3_review.py
--basetemp=.pytest_tmp/p3-review-first-01
--junitxml=.pytest_tmp/p3-original-independent-20260909/independent-01.xml
```

Affected run:

```text
tests/test_sports_prematch.py
tests/test_sports_prematch_original_capture.py
tests/test_basketball_context.py
tests/test_ice_hockey_context.py
tests/test_ice_hockey_numerics.py
tests/test_ice_hockey_features.py
tests/test_ice_hockey_effects.py
tests/test_ice_hockey_correction_boundaries.py
tests/test_riskobet_candidates.py
tests/test_riskobet_domain.py
.pytest_tmp/p3-original-independent-20260909/test_p3_review.py
--basetemp=.pytest_tmp/p3-review-focused-02
--junitxml=.pytest_tmp/p3-original-independent-20260909/focused-02.xml
```

## Immutable source and probe identities

Raw local SHA-256; not a portable CRLF/LF equivalence claim:

```text
sports_prematch.py 1d908a32d4decdd508477526af9802f3a81bd9054f93636e4df8c63675b9a07d
riskobet_candidates.py f5bb84e23eca72c79ed28c567efea1706ced947e07d8fb9bb898fec7b530bf28
tests/test_basketball_context.py d142f94c0263baa293cf122e585884ce0000fb1865d665cd53e7c2d96ac1ea4a
tests/test_sports_prematch_original_capture.py a49ee7e9b3a32fd60ca7e4ff319c97fc258f2c6191b42365b9129717a6bcc44b
docs/audits/2026-09-09-d3-prematch-original-capture.md 1a296f885152095f4df586be78f0ac8887a015c86dcf06dc16f175fc711d15bc
test_p3_review.py 3eb52b9cbbe78fd9ef6c61ea79ec4dd763e67bfbb4dd67bc2ba3a6d389ce1166
independent-01.xml e5033bd8048f305657bd79a8fcdea25b1fa8043a4e0890d09976ec15cc3152f5
focused-02.xml 19c77a918faa1f55fd17c5d340d7497b85cb3aa2de14ebb4dd56eb1fa18b3ef1
```

Frozen original fixture SHA-256:
`b4e44d03d09a6c8f22ce52568ea36f9af1c7e563d094af98c135703a0a204677`.
Prior adapter source resolved from exact parent
`6a0e05a3c9a95124d838bdfc21d9eddec80317e7`, Git blob
`c9ba77d82800e0d0dc8ca1561fd25ebdca0d88e9`, independently hash-checked before execution.

All five target-file hashes were checked again after both runs and remain exact.
Tracked worktree remains clean. Only this ignored independent probe/report/JUnit
directory and its ignored test temporaries were created.
