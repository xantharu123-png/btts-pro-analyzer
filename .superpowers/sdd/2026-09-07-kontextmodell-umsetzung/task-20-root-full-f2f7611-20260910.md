# Root full regression at frozen technical B — 10 September 2026

Exact revision: `f2f7611823c8e0b23b102bafba9e880adab87f9f`, separate LF worktree
`C:/Projekt/BetBoy/betboy-app/.worktrees/kontext-release-b-20260910`.
The tracked source/index remained unchanged throughout the test run. The new
untracked local `output/playwright/` contains isolated browser QA data, not
production observations or edits to the tested code.

Actual command used the existing quality Python:

```text
-B -m pytest -q -rs -p no:cacheprovider
--basetemp=.pytest_tmp/context-release-b-full-20260910-01
--junitxml=.pytest_tmp/context-release-b-full-20260910-01.xml
```

Result: **6255 passed, 20 skipped, 97 subtests passed, 1354.13 seconds, exit 0**.
JUnit independently parsed: 6372 recorded cases including subtests/skips, zero
failures, zero errors, 20 skips. JUnit SHA256:
`3217c046dbd24102e76fc4bd07f7f7d6c1d1fe9f495ac48e46fa15f80345d841`.

All skips were inspected. They are actual Windows symlink privilege or POSIX
mode/umask limitations in backup stage, reader trust, runtime backup, Hockey
revisions, model artifacts, seven server-job cases and three Tennis seed cases.
They are not counted as Linux checks. The independently preserved real Linux
785-case run and actual root/betboy DAC report provide their separate scope.

This full run adds P5a+metadata correction to accepted Root6d03; it does not
cover later P4b2/F1, P6a or the new manual-price UI correction. Its exact source
tree equals `19a8f53`; the separate A ancestry merge changed no application byte.

## Actual browser hold, not hidden by a green suite

The real local Streamlit app on port8507 uses exclusively synthetic fixture
data in this new worktree, no provider credentials. Five normal forecast cards
render from six raw rows. The intentionally opposed home/away Porto rows yield
one displayed home selection; every short analysis is visible without expanding.
All cards/ordering survived the empty Tennis filter roundtrip and low/high quote
checks. At 1440,761,760 and320 widths measured so far, no horizontal card overflow.

However, a genuine manual-price display defect was reproduced: submit confirmed
1.12, then edit to4.00 and blur. The old unqualified low-price message remains
because the form batches inputs until its next submit. Its number-free wording
appears to evaluate the edited quote. This is a **release hold**, not a passed
browser result. A separate LF fix worktree is authorized; no frozen B source was
patched during this run. Explicit keyboard confirmation works; the automated
pointer attempts at the React-Aria hidden checkbox were not counted as passed.

The local quote check only executed non-BET synthetic candidates. Its existing
TipStore path is inside the new QA worktree, not the user's real data. No real
bet, financial action, provider request or production artifact was created.

The A/B updater has a separately accepted bounded review. Main/VPS remain on
the earlier revision pending the new final target, complete QA and the actual
installed A-then-B transition. This green run is not empirical effect evidence,
the final all-sport release, or a claim that the user's overall work is done.
