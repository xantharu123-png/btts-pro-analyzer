# C2 implementation rulings — 9 September 2026

Root read the complete bounded C2 preflight (SHA256
`7f1f45a12b8adbb266d562cd95ad6068a01ea351242d70831befd51cc788cff3`),
the C2 plan, source-readiness and current actual Ridge/variance/prediction code.
Approve its four requested boundaries, with the following exact restrictions.

- Add only `basketball:margin:including_ot`, original unrounded
  `expected_margin` / positive `residual_scale`, one B2 identity `margin` head,
  and inclusive `home_win` / `away_win`. No generic margin, spread, total,
  regulation or hockey inference. The owning reference/parameter validators
  may be added to B1; old football/tennis validation remains unchanged.
- The new base export is additive and basketball-only. Preserve the default
  dataclass/call result, actual selected inventory, fit, variance and original
  probability bytes. Capture old Cricket parity before editing; no default
  context hook, provider call, timer or changed existing selection.
- Use the actual signed Ridge influence recipe and replay-check its complete
  ordered design/penalty/target/fit inventory. Do not replace it by a convex
  mean or normalize away negative influences. Do not reconstruct parameters
  from rounded factor text, accept free coefficients as proof, or rewrite the
  original mean to force a secondary recomposition equality.
- `basketball-context-reference-v1` binds full unchanged B1 Event, full base,
  and exact sorted preprocessing references. Add explicit keyword-only Event
  binding at effect/feature consumers. Season/rule/native joins live in the
  owning provenance transport, not guessed new B1 Event fields.
- Initial formats `nba_reg48_including_ot` and
  `euroleague_reg40_including_ot` require explicit source-resolved rule scope.
  The internal closed transport is not a verified ESPN/EuroLeague player feed.
  Use complete measured regulation-only rotation exposure for the first roster
  difference variant. Inclusive workload is a distinct observed factor; no
  guessed OT subtraction, expected-lineup-as-whole-rotation or questionable=.5.
- Unknown native/season/roster/rules retain an available original base with
  unavailable context provenance. No missing player/column becomes zero;
  persistent absence may be zero only when both measured references support it.
  Joint event and full team content revisions must not borrow old projections.
- Prove the real B2 fitting/application mechanism on explicitly synthetic
  complete examples; preserve scale and exact neutral-result base bytes.
  Actual D1 dataset/case/outcome assembly and empirical activation remain later
  owning integrations, not a parallel C2 training or approval shortcut.
- No new source request, subscription, secret, Hockey/Cricket behavior, money,
  deployment helper or production change in this packet. Record the existing
  dated 403 as an unsuccessful probe, not a complete live provider capability.

The scoped packet requires RED/GREEN, complete old/new default Cricket parity,
focused/full regression and independent source/numerical review before merge.
Neither new family support nor a passing synthetic fit completes C2 empirically.
