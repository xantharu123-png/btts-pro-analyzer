# Independent P4b2 review — one bounded P3 finding

10 September 2026. Read-only review of the clean, frozen worktree
`C:/Projekt/BetBoy/betboy-app/.worktrees/kontext-team-sports-binding-20260909`,
HEAD `ba73dc9ef0da65b97f5d581e38ebf34a55c18292`, parent
`4f49a8c86e98d56127f5419d0b63b629997e0712`.

## Disposition

**One P3 diagnostic/contract correction is required; no qualified P1/P2 or
false-model/false-match finding was reproduced.** The original six failing
assertions remain red on the unchanged frozen source. The remaining bounded
source-to-original, alias, chronology, integrity and no-extra-model controls
pass. This is not an empirical, live-feed, worker, consumer, D4, deployment or
betting-quality approval. It does not complete P4.

Independent execution: **505 existing/owning focused cases passed**. The final
additional probe run has **46 passed / 6 failed / one explicitly deselected
reviewer setup case**, zero skips. None of the author's prior 804+19 counts or
Root's integrated full suite is counted as this review's execution. No full
repository suite was run.

The reviewer changed no source, owning test, historical evidence, Git reference,
dependency, provider endpoint, production database or server. Only new ignored
probe/report files and unique temporary test databases/XML were written.
Target HEAD, the three packet files and relevant original sources remained
byte-exact throughout this review.

## Material read and prepared scope

Before the resolver freeze, the entire approved context-model specification,
P3/P4a/P4a-F1/P4b1 owning audits and their unchanged original independent reports
and Root acceptance were read from exact Root
`6d03ca00c487295a9a336e5917c314762e8ec637`. The entire team-sport live preflight,
actual P3 identity/history normalization and source capture/alias/reader/cache
boundaries were read too. The resulting unchanged 22-group preparation plan is:

`.worktrees/kontext-football-original-20260909/.pytest_tmp/p4b2-review-preparation-20260910/PLAN.md`

SHA256 `cf712e27a32a0a745e4051826db1f2c209d3770679f0fcec14ab2c7ebb702bb3`.
It was a preparation document, not a premature source review or findings list.

On the final freeze, the complete 192-line owning audit, all 388 source lines,
all 698 owning test lines and the exact three-file diff were personally read.
The final closed return semantics were applied: historical `match_state` is
documentary raw-row correspondence, while `current_state` independently controls
current source status. An old documentary match is not itself a current claim.
No probe demands a new rules/format/freshness gate or treats an unknown context
as permission to erase the original prediction.

## F1 — P3: a partial native identity drops a separately known contradiction

Location: `context_sources/team_sports_binding.py:298`, `_resolve_input`.
A receipt with `projection.issues` is skipped before `_compare`. One missing
native team therefore prevents comparison of the other team's independently
present, deliberately wrong native ID against the actual original target.

The actual P3 model is computed and captured first. A provider-shaped response
then has one participant set to null and the other set to 99, or Club99 for
EuroLeague. Real B1 normalization and SQLite append/read preserve the unknown
side and independently known opponent. The setup asserts those actual stored
projection values before calling the resolver. It does not use free coefficient
or verified flags, fabricate a success row, or call a stub resolver.

Expected: `current_state='unknown'` and no Event remain correct, but the known
raw/native contradiction gives `match_state='conflicting'` with the participant
conflict reason. Actual: `match_state='unknown'` without that known reason.
Six cases cover all three providers and both unknown sides. Six unknown-only
controls are green and must not be converted to contradictions by the fix.

**Product consequence is limited:** a caller cannot distinguish incomplete
native identity from incomplete identity containing a known impossible opponent
association. No Event, false matched receipt, changed probability, applied
effect, bet or approval is created by these six cases. This is not the stronger
old P4a F1 accepted-Event bug and is not a claimed money or betting defect.

The separate early `F1-PARTIAL-IDENTITY.md` addendum, original probe and first
RED XML were frozen and sent before the rest of the review concluded, allowing
a different worktree to prepare a fix. They remain byte-identical; this report
does not retrospectively change the original result.

Smallest repair: accumulate independently comparable known contradictions
from partial native records without making those records matchable/currently
usable. Keep aliases, source schema, rules, unknown-only fallback and original
probabilities unchanged. Independently rerun the original six REDs unchanged.

## Independently reached positive/negative controls

1. **Actual existing-response chain for ESPN, EuroLeague and NHL.** Tests enter
   the real schedule method and real `completed_history.fetch_page`, replacing
   only their existing HTTP calls with local provider-shaped responses. Exactly
   two existing GETs per provider feed actual response capture, native SQLite,
   actual completed-cache parsing/receipt time, one P3 capture and P4a build.
   Each complete 84-row synthetic history and target is correctly indexed.
   After that original capture, provider/file/SQLite/target-fit/predict access
   is forbidden during resolve and P4a construction. Unrounded probabilities
   stay exact; no A1 or empirical qualification is invented. The EuroLeague
   target uses its **actually requested E2026** season, separately from the
   E2025 historical request; the harness does not falsely label that request.

2. **Outer-only withdrawal and genuine restoration.** Target and history cases
   use the true competition/outer-alias distinction with outer ID123. Before,
   exactly at and after a partial withdrawal, the entire causal lineage survives.
   Old documentary matching references remain, but current state is unknown and
   a withdrawn target has no Event. Only a fresh full same-identity tuple restores
   its own current availability; reversing the whole pool gives the same result.
   The focused owning cases additionally execute cross-competition ambiguity,
   equal-time conflicting revisions and corrected participant/schedule/status
   cases. This does not reinterpret the old exact-key selector as an alias proof.

3. **Actual EuroLeague UUID/identifier/code and request season.** Explicit
   co-occurrence of the real-shaped UUID and literal E2025_406 with synthetic
   gameCode406 supports each actual raw spelling. Same gameCode under a real
   E2024 request does not join the UUIDs. Wrong raw IDs with identical names,
   times, participants and scores do not become aliases for any provider.
   Focused cases retain composite-only, case/NFKC/whitespace and shared-alias
   ambiguities. `played=false` remains not-completed, not a made-up scheduled
   status: a documentary match does not generate a EuroLeague Event.

4. **Full raw index, not selected model index.** Two ignored raw slots shift an
   actual history observation to raw85; its duplicate is raw86. Both independently
   refer to the same actual native receipt, and raw84 remains unbound. Ignored
   slots remain not-applicable rather than disappearing. The actual P3 input
   and output stay unchanged. Focused cases cover changed/future-cache-time raw
   revisions independently from their original model inclusion rules.

5. **Honest receipt/cache timing.** Actual capture precedes parser/cache by a
   separate recorded clock in the full chain. A matching source receipt exactly
   at the new decision may be documentary evidence at that time; one microsecond
   after it may not be backdated by the older cached result clock. Neither clock
   is rewritten. Three genuine warm-cache tests preserve the available original,
   invoke no new GET/receipt clock, create no B1 file and return missing bindings.
   Existing same-payload refresh and UTC-offset controls were also executed.

6. **No native-default or scope promotion.** The actual NHL upcoming parser
   converts absent neutralSite to legacy False. Independent tests prove the new
   binding still reports native neutral unavailable; actual false and true are
   separate controls. The inherited neutral-Hockey missing model stays missing,
   not 50%. Conflicting ESPN response/event/competition season years are rejected
   as correspondence conflicts. Focused cases retain absent rules/season,
   unsupported NHL 2026/27/preseason and boolean/float game types without changing
   the original prediction or borrowing old C2/C3/D2 approval.

7. **Actual integrity, not public-hash authority.** Real SQLite TEXT-to-BLOB
   corruption of six existing index fields fails before an unrelated earlier
   cutoff can turn it into missing data. Recomputed public content/receipt/source
   digests around an inconsistent native projection still fail the owning
   reconstruction. The focused cases check malformed/future/unrelated selected
   rows and closed metadata before filtering. This proves the declared
   inventory's internal integrity, not authentication of an arbitrary caller's
   fake native body, complete-pool capability, HMAC or whole-file rollback.

The existing 505-case focus includes P4a F1, old original/default/Cricket parity
and source-cache regressions. No fresh 15K financial, C2/C3 empirical, live UI or
provider claim follows. Only the new resolver module is implementation source;
none of those established model or financial source files changed.

## Exact run ledger, including reviewer setup error

All calls used the existing quality Python with `-B -m pytest -q -p
no:cacheprovider`, unique basetemp/XML and explicit `$LASTEXITCODE` propagation.

| Run | Own result | Meaning |
| --- | --- | --- |
| `p4b2-review-focus-01` | 505 passed, 0 skips, 15.90s | Selected existing/owning binding, P4a, P4b1, prematch/original and cache files; no new independent probes in this count. |
| `p4b2-review-native-partial-02` | 6 failed / 6 passed, 1.77s | Six genuine F1 reached assertions, with six unknown-only controls. |
| `p4b2-review-controls-03` | 30 passed / one failed, 4.64s | The failure was **reviewer setup**, using nonexistent physical SQL column `source_schema`. No source reader/resolver was reached by that failed statement. |
| `p4b2-review-qualified-04` | 10 passed, 1.81s | Separate actual-column corruption controls plus actual NHL neutral/default and genuine warm-cache tests. |
| `p4b2-review-closing-05` | **6 failed / 46 passed / one deselected, 5.24s** | All three original unchanged probe files; only the precisely named invalid SQL-column setup case deselected. Same original six F1 assertions remain RED. Zero skips/errors. |

`context_observations` physically stores source, subject_id, kind and schedule
revision, but **not source_schema**. The latter is part of the content payload.
The mistaken original test and its failed XML remain untouched. The separate
replacement first asserts real `PRAGMA table_info` membership before changing
only a known real index column. This is no waived source failure, hidden Windows
skip or weakened assertion. It is the only reviewer harness correction needed.

Closing command:

```text
python -B -m pytest -q -p no:cacheprovider
  -o "pythonpath=. tests .pytest_tmp/p4b2-independent-20260910"
  .pytest_tmp/p4b2-independent-20260910/test_native_partial_conflicts.py
  .pytest_tmp/p4b2-independent-20260910/test_independent_binding_controls.py
  .pytest_tmp/p4b2-independent-20260910/test_qualified_extra_controls.py
  --deselect=.pytest_tmp/p4b2-independent-20260910/test_independent_binding_controls.py::test_real_sqlite_index_corruption_is_not_a_missing_resolution[source_schema]
  --tb=short --basetemp=.pytest_tmp/p4b2-review-closing-05
  --junitxml=.pytest_tmp/p4b2-review-closing-05.xml
```

Use a different output/base directory when reproducing. Never overwrite these
first-run REDs, mistaken setup evidence or frozen probes with an owner fix run.

## Frozen raw SHA256

```text
context_sources/team_sports_binding.py 8cce9d9e4b333ecc48bbf289264c5f7e1e5fc305c725b8728a0c025b3eaeee97
tests/test_team_sports_binding.py 951bee6afeaca9013bf7f9d2b4702f2aca80ba7dca857d1eba937ebfdd022a78
docs/audits/2026-09-10-p4b2-team-sports-binding.md 164c974bbf402f066768fc4686dc16478b735d439a36b1e2a9abdc6f164d4b42
sports_prematch.py 1d908a32d4decdd508477526af9802f3a81bd9054f93636e4df8c63675b9a07d
context_models/team_sports_live.py dcb571b6aaef03a1e18638a8aedfd47841c1317d324f5c0561eab67da8646b8d
context_sources/team_sports_status.py 1a11dab252f7a028171f0f85504b00ffce2d42f581f38920e6d7bfa11a4cbb2c
context_sources/team_sports_capture.py 7201d9d2596a7830485bb744407af9c8a35c5ecc422b0dd2a98c5acb1995c199
```

Independent files in `.pytest_tmp/p4b2-independent-20260910`:

```text
test_native_partial_conflicts.py 97f67983baf822a63465ce9a8729fec10838414ce9fc61827a90a759d71e3818
test_independent_binding_controls.py 163a31dac0f4d9c451b8a071e1852a4bbc71bd961a706b88b52a1e351abdd681
test_qualified_extra_controls.py 6a85ee4dd59b326505915dad0394fd6741d47835b5b83dc9d7fc5c2ab208bbb0
F1-PARTIAL-IDENTITY.md 48ef0eb3f076af34f3fd56ac9e23654c9afc720b172e50bcff60ee286767b9eb
```

Original XML in `.pytest_tmp`:

```text
p4b2-review-focus-01.xml d9fab100b6fba589fc63682e6659c2e973416a1412e49618974584eb59860879
p4b2-review-native-partial-02.xml 858b7c061208bbd8aaf639dd60a960ea12ed096994c30139bb1ac50f396055a9
p4b2-review-controls-03.xml 86263ce89267f69687c0429554fe8c71e389d2225417c691ec2fee50ea8c7185
p4b2-review-qualified-04.xml 8af61d3531fa5cadc8ecfc09fb5c28c1951cedbadc9af06f9a1270cc934dc8cb
p4b2-review-closing-05.xml 0729bf4369d7223f47925e3a441fbf903223fc005d73fa178d92aa91f729c9e5
```

All source/test/audit/probe identities were rechecked after the final run;
the target checkout remained clean. These are actual local byte identities,
not portable CRLF/LF equivalence. Report hash is supplied separately.
The original F1 addendum remains an independent immutable record of its first
disposition. Source repair, Root integration and every live/empirical boundary
are separate from this completed original review.
