# Root browser continuation on frozen C0

10 September 2026, approximately 10:43–10:57 CEST. Frozen source
`c0a83f191e200b5305acbf06cf23ddfb359a0a91`, separate LF worktree
`kontext-manual-price-ui-20260910`. This is local UI evidence, not VPS,
source coverage, empirical approval or a completed release.

## Environment and isolation

The previous account's browser and local listener had stopped. Created one
hidden CUA in-app tab at `http://127.0.0.1:8507`; title became `BetBoy`.
Started the existing quality Python/Streamlit with the exact C0 source, no
file watcher, no telemetry, explicit empty secrets file and removed provider
credentials in that child environment. No package/browser dependency installed.
The preserved seed was hash-identically copied into the new local QA directory
`output/playwright/context-resume-20260910` and run with isolated runtime paths.
The existing normal Finder reader uses its module-local runtime path, so only
this QA worktree's own synthetic `runtime_state/wettfinder_latest.json` was
updated. Its SHA256 is
`a26b52be52dbba2335c8ff3889272f6deeb72d2cac5c4ca54a807a59fd9fe2dd`.
Visible source time was today10:42, not the previous account's00:37 fixture.

Six raw football rows deliberately contain two opposing Porto outcomes; six
Risk scenarios exercise all existing sport filters, including unchanged Cricket.
These are synthetic QA data, not real match recommendations or observed effects.
The Risk seed deliberately includes legacy technical strings/missing model
bases; its prose is not evidence that current producers emit those strings.

## Checks and findings

- PASS: meaningful Finder and Risk pages, no framework error overlay;
  final warn/error browser log list empty. Local only, one browser engine.
- PASS: five normal cards from six raw rows; only the home direction appears
  for QA Porto/QA City. Main reasoning and counter-risk visible without opening
  an analysis disclosure. No claim of real evidence quality from this fixture.
- PASS: explicit1.12/100/confirmed returns below-price. Edit4.00+Tab:
  after the reactive rerun the old caption/status disappears and the changed-
  input message appears. Immediate pre-rerun DOM still shows the caption for
  the explicitly named old1.12 result; that transient is not a new finding.
- PASS: explicit4.00/100/confirmed gives pending evidence, no stake. All five
  model-card texts and order remain byte-for-byte equal to the original UI text.
- PASS: actual Clear value button empties the balance and invalidates the old
  decision; the popover closes and reopening shows the changed-input message.
  Explicit check with empty balance gives invalid-input text without exception.
- IMPORTANT F1: first opening two fresh cards gives blank balance, even after
  waiting about a minute. Default100 claim is false in the actual UI. Submitting
  a quote/confirmation without touching balance safely rejects it; there is no
  hidden100 calculation. Independent review requires initialization correction.
- IMPORTANT F2 from independent review: invalid explicit results lack a
  checked-input caption. Actual empty-balance browser case confirms generic text.
- PASS: empty Tennis normal filter has no misleading Top heading; All returns
  the exact same five card texts/order. All six Risk filters each show the
  matching one scenario; All returns the exact six original texts/order.
- PASS geometry: Finder and Risk at1440,1024,761,760,390,320 pixels each have
  document scrollWidth exactly viewport width. The five-item mobile navigation
  switches Finder→Risk; desktop/tablet column changes and mobile wrapping were
  observed. Inline screenshots captured desktop Finder and320-pixel Finder/Risk.

Actual screenshot bytes were displayed through CUA, not saved to a claimed local
PNG path. They are evidence in the tool transcript. The final corrected target
still needs its own source verification and interaction rerun. Two concurrently
cached manual decisions were not tested here; existing AppTests cover that case,
and Root will run it in the final browser pass. Real mobile devices and other
engines remain untested.

## Qualified tool limitations

The first number-input `.fill('')` followed by Tab restores the previous100 in
the browser; it did not exercise an actual clear. The available Clear value
button was then used successfully. Immediate dialog lookup after that click
timed out because the popover closed; fresh DOM and reopening verified its
state. An Escape request against an already closed popover also found no match;
no product fault or successful interaction is inferred from that attempt.

No BET result or money transaction was submitted. The unchanged explicit manual
check helper can archive matching NO_BET/SHADOW records in this isolated
worktree's synthetic tip store; tests are not falsely described as no database
operation. No production data/history was touched.

## Release and next step

C0 remains unmerged and release held for the two findings. Original author owns
one narrow F1 worktree; the independent reviewer will review its delta, Root
will repeat real browser interactions, then freeze the final integrated target
for its single full regression. The other implementation writer was explicitly
paused during this correction; its WIP remains preserved.

Fresh read-only network checks: origin feature898652b/main2ba3931. Actual VPS
HEAD2ba3931, clean tracked state; app/Caddy active, local/public health`ok`.
All seven timers have scheduled next times. Tennis07:17–07:18 CEST failed exit1.
Installed updater SHAa07ad24c, protected stage helper SHA1441158 unchanged.
The deploy login cannot directly read the app path; repeated with the owning
app user via existing sudo authorization. No permissions were changed.
