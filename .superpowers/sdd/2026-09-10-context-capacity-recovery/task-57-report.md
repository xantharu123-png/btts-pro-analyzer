# Task57 — actual guarded small ATP/WTA chain harness

Status: **DONE_WITH_CONCERNS; implementation frozen for independent review.**
This is a new small diagnostic harness, not a native run or completed C/B gate.
Root supplied BASE `ca663ec16d9df53b5e90efd3fafdb324be4a1362` and sole-writer
identity `/root/c_native_chain_harness`. No Git/index operation, server/network
command, dependency installation, subagent, cleanup, product edit, Task54 edit,
or existing guard/supervisor/budget/probe edit was performed by this writer.

## Exact responsibilities and frozen bytes

Only these five files were written:

- `tests/native_context_chain_catalogue.py`: versioned closed manifest, fixed
  package selection, archive decoding, no-follow held input readers, bounded
  copies, complete file/directory and allocated/logical accounting, read-only
  inventory and exact stdin-launcher generation.
- `tests/native_context_chain.py`: fresh stdlib parent, in-memory bootstrap
  admission, complete reservation, own copying/sealing, fixed supervisor calls,
  full output retention, initial-input/copy/previous-child rechecks and custody.
- `tests/native_context_chain_worker.py`: guard readback before imports; actual
  callable invocation, real ATP rollback test, observation-only quiescence
  instrumentation and complete bounded evidence output.
- `tests/test_native_context_chain.py`: focused portable protocol tests and
  actual local Task54 callable/rollback integration.
- This report.

```text
tests/native_context_chain.py
6d02567951b0b7612e620960eb4754e7793c11df289cb0826187ef7d081d6c2a
tests/native_context_chain_catalogue.py
946cabc787dc1b57904c6931fde3b191278b072525122e5e01a40156f832227c
tests/native_context_chain_worker.py
ee08091b3aec6e9a579e9fe3811392854198fe676bacdcae786dcb1217f5f302
tests/test_native_context_chain.py
d5f5643177c1bee89bf048292fe7da6d8ce4b53fed1538a2fa41ba1f4f967527
```

The parent contains the catalogue's exact pin. Its launcher is generated only
from the same reviewed archive/manifest's fixed parent/catalogue/three helpers;
there is no arbitrary module/path fallback. Fresh unchanged owner checks:

```text
tests/test_context_storage_corpus_consumer.py
5a59f75d0a3093238031159a813ce81b6c633c63105cab0338ed376a7bac7649
context_preparation_process_guard.py
62fcfcacaa7e658eefa8a2f8ceced9dc465846a8ca61dc38c2691dc9984685e4
context_preparation_budget.py
fb12aa3b2170dc2dbed7d70d2aa5846d5928b4fda42de00d4828f9f411482478
context_preparation_supervisor.py
c6c1c8e82107d48b66d7714577004e7d9e7e400525620b23070d4258ab554dc8
```

## Actual implemented chain and retained ownership

The fixed worker argv is exactly `ATP` or `WTA`. Native launch is always the
unchanged `run_single_process`, UID/GID65534, from the fixed copied worker path;
there is no pytest.main, collection, plugin startup, alternate executable,
subprocess producer or replacement validator. The worker rejects optimized
Python before assert-bearing Task54 code and observes the actual guard again
after computation. Product/pytest/numerical imports are below this guard check,
and the parent never imports them. Its catalogue executes held reviewed bytes
in a non-registered namespace, preserving the supervisor's unchanged whitelist.

`invoke_cases` uses actual `pytest.MonkeyPatch.context()` owners. It directly
calls `run_small_corpus_consumer_acceptance(positive, monkeypatch, tour,
record_property=...)`; ATP additionally calls the existing actual
`test_real_late_prepared_cleanup_failure_rolls_back_consumer_before_commit`
inside its separately reserved `late-cleanup` directory. All setup/oracle,
Corpus, genuine Source/D2/receipt ownership, old-parts reopening, History,
prediction/features, full canonical Original/snapshot/ref comparisons, required
pre-commit cleanup and output reopening remain owned by unchanged Task54.

Per Root ruling, an observation-only wrapper calls the real
`WorkspaceBudget.check_quiescent` first, samples after that successful boundary,
returns the exact original observation, and propagates observation failures.
Each sample includes full existing-file hashes, logical and allocated file and
directory sizes, allowed namespace, actual native free space and conservative
unspent whole-diagnostic reservation. Sampling cost belongs to the same child.
The late failure's private files remain and its cold consumer reopen has no
tables; no accepted result is reported for that case.

The parent holds initial archive, manifest and dependency-root descriptors and
their identities, checks all actual dependency member identities and hashes,
and checks full copied code/dependency trees before and after each child.
Completed ATP output bytes are also checked unchanged across WTA. Only fresh
private attempt directories are child-writable; code/dependency files are0444,
their directories root-owned with no group/world write. Existing host paths
and permissions are untouched. This DAC arrangement is not a whole-host sandbox.

Complete stdout/stderr are retained as exact hex in fixed `ATP-output.json`
and `WTA-output.json` before parsing/rechecking can reject a returned result.
Each file also retains the actual native result, stopped-child kernel readback,
wait4 usage, output digest, RSS/free-space/sample-gap observations. The parent
report references these complete records and records final charged accounting.
The output parser itself is not an authentication/Source permission primitive.

## Fixed resource and namespace contract

- One permanently retained300CPU-s PreparationBudget ticket; no settlement or
  refund. Parent hardCPU90 plus two child hardCPU90 =270, leaving30 charged.
- Parent and children AS2GiB; unchanged supervisor requires observed/terminal
  child RSS strictly below1GiB and combined child output at most1MiB.
- Child FSIZE4MiB matches Task54's main/journal caps and exceeds its1MiB ledger.
  Parent hardFSIZE128MiB accommodates the largest admitted dependency copy and
  is not lower than the child's hard limit. No setter follows child guard.
- Whole-parent deadline600s uses the actual kernel start tick, rounded down;
  whole-life parent process CPU includes interpreter/import startup. Both
  children have90s wall limits. Copies, hashing, observations, reports and
  accounting run in this same measured parent or its guarded children.
- Exactly57 file slots across `ATP/positive`, `ATP/late-cleanup`, `WTA/positive`:
  nine main/journal pairs and one ledger per attempt. File caps total229638144;
  six1MiB Task54 metadata allowances give235929600 reserved attempt bytes.
- Extra predeclared controls comprise two8MiB catalogue/plan slots, report,
  failure, custody and journal slots, two3MiB full hex-output slots and a2MiB
  stdin/launcher allowance.128MiB additionally covers directory/allocation
  slack. This fixed base is399507456 bytes, plus exact archive/code/dependency
  copy totals; no wildcard retry/directory reuse exists.
- Archive<=64MiB, each selected package member<=128MiB, dependencies<=1GiB,
  aggregate files+directories<30000. All exact members are listed and hashed.
  Total new reservation<=8GiB; active input<=4GiB includes original archive and
  dependency files AND their copies, code, attempts and conservative controls.
- Native free-space sampling requires4GiB plus unspent reservation. Logical,
  allocated, metadata and complete allowed directory names are checked. These
  sampled/accounted bounds are not a native filesystem quota or a growth proof.

System interpreter hash/version, kernel and stdlib search locations are bound
to Root's native read-only inventory. The worker records bounded proc maps and
Python file-access observations; training CSV/XLSX/pickle fallback attempts
are rejected. Python audit events omit the dir_fd for relative copier reads,
so those observations are labelled by event/path and custody belongs to the
no-follow reader. Native ELF opens are represented by mappings, not falsely
equated with the package-tree hash. Full transitive B runtime closure remains
explicitly unproved.

## Root rulings applied

1. Catalogue code may execute in a non-registered stdlib namespace; its actual
   executed bytes must still be pinned. The real quiescence owner may be wrapped
   for observation only, never replaced or silently bypassed.
2. An actual UnreapedChild suspends only the parent wall alarm BEFORE fallible
   reporting/accounting, retains the full durable charge and same PID/pidfd,
   and SIGSTOPs until an external operator resumes it. Only that same child may
   then be signalled/reaped. No WTA/retry/success follows. This exception is
   **operator-custody-required, NOT a completed600s diagnostic**; hard CPU/AS/
   FSIZE remain. Report failure cannot discard custody. Tests simulate this
   control flow; no real VPS process was stranded.
3. Initial protected-sibling startup would require an unmeasured new root
   install, so Root approved a fixed reviewed stdin bootstrap. No prior new
   root sealer/install exists or is assumed. Ordinary-user-owned initial files
   are only pinned DATA, read through held no-follow descriptor chains. The
   same fresh parent creates its own new job directory, binds its inode,
   reserves, copies, seals and supervises. Root independently records/verifies
   the exact launcher/stdin SHA, not merely caller assertions.

## Exact local QA and retained RED/GREEN evidence

Workdir for every command:
`C:/Projekt/BetBoy/betboy-app/.worktrees/context-capacity-recovery-20260910`.
Actual runtime: Python3.12.14 MSCv1944/AMD64, SQLite3.53.1, pytest9.1.1.
All test commands used this complete prefix:

```powershell
$env:PYTEST_DISABLE_PLUGIN_AUTOLOAD='1'
$env:PYTHONDONTWRITEBYTECODE='1'
& '.pytest_tmp/qa-python312-c-01/Scripts/python.exe' -B -m pytest -q -p no:cacheprovider -o junit_family=xunit1 tests/test_native_context_chain.py
```

For each row append the exact filter if present, then
`--basetemp=.pytest_tmp/<basetemp> --junitxml=.pytest_tmp/<XML>`.
Every artifact remains retained; none was cleaned/reused.

| XML | basetemp | Exact optional filter | Observed result |
| --- | --- | --- | --- |
| task57-red-01.xml | task57-red-bt-01 | none |13 expected failures /0.31s: Task57 implementation missing |
| task57-green-02.xml | task57-green-bt-02 | none |13 passed /0.25s |
| task57-observation-red-03.xml | task57-observation-red-bt-03 | `-k 'unknown_empty or actual_fixed or kernel_start'` |2 expected missing-interface failures,1 passed,13 deselected /0.24s |
| task57-observation-green-04.xml | task57-observation-green-bt-04 | none |1 failed,15 passed /9.44s; Windows FD epoch issue below |
| task57-protocol-red-05.xml | task57-protocol-red-bt-05 | `-k 'broken_child or orchestration_unknown or manifest_requires'` |2 failed,1 passed,16 deselected /0.26s |
| task57-full-green-06.xml | task57-full-green-bt-06 | none |22 passed /9.70s |
| task57-bootstrap-red-07.xml | task57-bootstrap-red-bt-07 | `-k 'stdin_bootstrap or data_read_holds'` |2 expected missing-bootstrap/reader failures,22 deselected /0.26s |
| task57-freeze-green-08.xml | task57-freeze-green-bt-08 | none |26 passed /9.33s; preceded final test refinements |
| task57-final-protocol-09.xml | task57-final-protocol-bt-09 | `-k 'data_read_holds or actual_bootstrap_catalogue'` |2 passed,24 deselected /0.21s |
| task57-parser-roundtrip-10.xml | task57-parser-roundtrip-bt-10 | `-k actual_fixed_driver` |1 passed,25 deselected /8.81s |
| **task57-final-owned-11.xml** | **task57-final-owned-bt-11** | none |**26 passed,0 failures/errors/skips /12.05s on FINAL pinned bytes** |

Run05 specifically demonstrated that incomplete fake child evidence was accepted
before the parser fix (`DID NOT RAISE ChainError`), plus the missing fixed
orchestration seam. Additional baseline tests were green on their first run;
they are not mislabelled as semantic RED evidence. Final parser tests first
accept a complete parser-only native wrapper around REAL Task54 callable output,
then independently reject CPU/RSS/output/unknown/readback/exit mutations.
Those native counters are synthetic protocol fixtures, not kernel evidence.

Run04's issue was confined to portable reader QA: Windows `lstat` and CRT
`fstat` returned different st_ctime_ns for the same dev/inode/mode/size/mtime.
The portable branch compares the shared FD fields and separately retains full
path-epoch checks. Linux retains full strict FD/path epochs with no relaxation.
The namespace-replacement test supplies another real inode's stat at final
portable path readback; it is not an actual Linux rename/race test.

```text
task57-red-01.xml 28b12cc3ea23ee3485e072ec8d9e677a45e738a6ffc9f5d53bf53d8a9f5b3fac
task57-green-02.xml 4ebdc98aa5bde968cd7fc81eacfbced89bebb261c4355d4b1fdf86281dcb7d80
task57-observation-red-03.xml dfea5209369df4c2022f7f552c39e70332254c28ae2df750b0e537eda432119a
task57-observation-green-04.xml a8af06c84063046e380bf3cc12dc11db39bd78b6b087cd34ce53cee10a3216df
task57-protocol-red-05.xml 10a436161bb9b8d30898f198f2bd61d675368e7cd9552d7a6202f82e35de853d
task57-full-green-06.xml 77781f16daaa32e439f3e5c49d64e265d4f2fa281d6cfb3a48a5a8cf02b138fe
task57-bootstrap-red-07.xml 27b4d9a02c4448993ad6d0be0c49726da306d40c96a2799414900713329f9b1e
task57-freeze-green-08.xml fbedc6139b8fecfb9d96d46ee3847e1339cafa6d4de0446116fbd7621b0ea88c
task57-final-protocol-09.xml 0166ee1864e12fd5447ccafca86ce37442a6aacb1ccbf2a96a9c1067f2fbc771
task57-parser-roundtrip-10.xml e412fada68a97baa5cf80950d9dbebb2b21ae9df10102f0541d77e3bec0c2e18
task57-final-owned-11.xml 27d18ae6671de17200843423567765749bed4e8a5df686f034f3d01b85326559
```

Final local actual-chain artifacts live beneath
`.pytest_tmp/task57-final-owned-bt-11/test_actual_fixed_driver_runs_0/`:

```text
ATP/positive/legacy-setup/context.db
594464c51f0e251a5cb64d4ed959c361a299090961271f5b722d5e757b2dbf4e
ATP/positive/whole-job/corpus/legacy-copy.sqlite
30506a6be59405535cedf540b76f605948374da3d6b11958d9e6a880bc68b9eb
ATP/positive/whole-job/new-consumers/consumers.sqlite
bf11b7134c436f6b910a095773932a2c81de85f792fa1fe8f41439f31227efc8
WTA/positive/whole-job/corpus/legacy-copy.sqlite
d9d4cbff75aa73de8258f08ff2ebbcde31f1621908ec49d34a40a6e3f9a1766d
WTA/positive/whole-job/new-consumers/consumers.sqlite
f359dd89ef61198e8c04398b5938c5a952e6c91fa3315c4f4cd660ca8bc773d7
ATP/late-cleanup/whole-job/new-consumers/consumers.sqlite (retained empty rollback file)
e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
```

These are fresh run-local SQLite hashes, not copied historical Task54 physical
file hashes. Real canonical payload/ref/snapshot parity assertions passed.
The existing Task54-M1 direct new-Original-created_at assertion remains deferred;
this task neither asserted that timestamp nor demonstrated a timestamp defect.

## Actual later packaging and first native invocation — Root only

No commands in this section were executed by this writer. Independent harness
review precedes ALL native tooling/execution. Root chooses and records actual
paths/hashes; placeholders below are not existing resources or claimed pins.

1. Root commits reviewed bytes and exports the **closed set of non-hidden
   tracked Python sources** from that immutable reviewed commit, preserving
   project-root layout. Enumerate the commit's tracked names first; select only
   lowercase `.py` suffixes and reject every path with a dot-leading component
   (including the two tracked `.pytest_tmp` Task55 diagnostics):

   ```powershell
   $task57Commit = 'REVIEWED_TASK57_COMMIT'
   $task57Members = @(& git ls-tree -r --name-only $task57Commit |
       Where-Object { $_ -clike '*.py' -and $_ -notmatch '(^|/)\.' } |
       Sort-Object)
   if ($LASTEXITCODE -ne 0 -or $task57Members.Count -eq 0) { throw 'Tracked source enumeration failed' }
   & git archive --format=tar --output=TASK57_ARCHIVE.tar $task57Commit -- @task57Members
   if ($LASTEXITCODE -ne 0) { throw 'Exact source archive failed' }
   ```

   Inspect the exact exported list and exclude no required route module; no
   secrets/untracked QA/database/training files belong in this archive. Record
   archive SHA256. Root alone handles the ordinary-account upload to a new
   retained file in the already observed QA location. This is not a root sealer.

2. After review, run the reviewed catalogue script as the ordinary native user
   for read-only inventory, with no dependency imports/installations:

   ```text
   env -i PATH=/usr/bin:/bin LANG=C.UTF-8 /usr/bin/python3 -I -S -B REVIEWED_CATALOGUE.py inventory /ABS/UPLOADED_ARCHIVE.tar REVIEWED_TASK57_COMMIT
   ```

   Retain exact stdout as `TASK57_CATALOGUE.json`, inspect its exact selected
   members/bytes, runtime and resource plan, and pin its SHA. It selects the
   actual fixed `/tmp/betboy-context-qa.9xr68INa/venv/lib/python3.12/site-packages`
   entries including pytest/pandas/requests/numerical lazy-support metadata.
   A mismatch stops; do not install or widen the guard to make it fit.

3. Locally generate the exact stdin launcher from reviewed inputs, not by
   concatenating arbitrary remote executable files:

   ```text
   .pytest_tmp/qa-python312-c-01/Scripts/python.exe -I -S -B tests/native_context_chain_catalogue.py launcher TASK57_ARCHIVE.tar ARCHIVE_SHA TASK57_CATALOGUE.json CATALOGUE_SHA
   ```

   Retain the generated stdout bytes unchanged as `TASK57_STDIN.py`; Root
   independently checks its five fixed embedded sources and records SHA256.
   No new root file has been installed. Record preparation/upload/launcher
   diagnostic costs in the existing evidence, without a global-C cost claim.

4. Select an UNUSED exact job path immediately below an existing root-owned,
   non-group/world-writable and nobody-searchable directory. `/tmp` is not an
   acceptable protected job parent; do not chmod an existing path to force it.
   The new Python parent, not a preceding shell mkdir/sealer, creates the job.
   Root sends the exact recorded launcher bytes to stdin of this NEW process:

   ```text
   sudo -n env -i PATH=/usr/bin:/bin LANG=C.UTF-8 /usr/bin/python3 -I -S -B - --manifest /ABS/UPLOADED_CATALOGUE.json --manifest-sha256 CATALOGUE_SHA --archive /ABS/UPLOADED_ARCHIVE.tar --directory /EXISTING_PROTECTED_PARENT/EXACT_NEW_TASK57_JOB --commit REVIEWED_TASK57_COMMIT --launcher-sha256 STDIN_SHA
   ```

   Use a byte-preserving transport (for example subprocess.run with `input=`
   equal to the saved launcher's read_bytes), not a PowerShell text pipeline
   that may re-encode or add a newline. The two uploaded initial paths are
   DATA only and need not be root-owned. No source from them is executed.
   Normal `native_context_chain.py` file execution intentionally fails without
   `_REVIEWED_BOOTSTRAP`; there is no protected-sibling fallback.

5. Root additionally owns the independent external terminal/custody observer.
   Correlate the actual parent PID/kernel start, both fixed pidfd/wait4 results,
   total elapsed and CPU through parent exit, final I/O/report/journal closes,
   every retained namespace/hash/allocation and the external exit status.
   Retain observer output without hidden server files outside the slot plan.
   For example, an ALREADY AVAILABLE and independently checked external timing
   command may wrap the env-i Python start and return its report over the same
   transport; do not install a timer or infer complete custody from timing alone.
   A live parent's `report.json` always says native_pass=false and external
   terminal observation required. Only that later reviewed external evidence
   can close the small native diagnostic. Any `failure.json`/custody stop,
   unknown process, changed input, truncated output or deadline failure is STOP,
   remains fully charged and retains all artifacts; no automatic retry/reset.

## Concerns and handback

- The package/member catalogue and stdin/archive SHA cannot be finalized before
  Root's reviewed commit and real native read-only inventory. Package-name
  observations alone are not version authentication or runtime closure.
- All26 final local tests pass, including the real three-attempt chain, but
  Linux parent admission/copy/kernel/guard/resource/custody execution was NOT
  performed. Protocol tests and local byte parity do not predict native import
  success or certify RSS, filesystem quotas, timing or complete runtime closure.
- Python-level file observation is supplemental, not a native I/O sandbox.
  Filesystem sampling is not a physical quota. Native ELF/stdlib/transitive
  runtime closure, protected whole-C/B all-cost ownership and terminal external
  observations remain explicit separate gates.
- Full590553-receipt/199-Original/199-snapshot/114-key profiles, Task54-M1,
  global1800CPU/3600wall, B authenticity/reuse, restore, whole suite, main/VPS
  deployment and release remain later gates. Cricket/A0/P4b3/daily-job unchanged.

The five owned files are frozen. This writer explicitly relinquishes sole-writer
authority to Root for metadata/Git packaging and independent review. There is
no pending native execution, dependency operation, cleanup or out-of-scope edit
from this writer.
