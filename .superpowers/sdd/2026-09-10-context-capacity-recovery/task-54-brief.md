# Task54 — actual small Corpus / Source / Tennis producer integration

Prepared by Root while Task53 is under independent review. NOT activated.
Start only after the actual Task51 consumer is independently approved and its
exact final API/bytes have been rebound here. This is an integration acceptance
task inside approved C, not a new design decision or native/global certificate.

## Ownership

New `tests/test_context_storage_corpus_consumer.py` and this directory's
`task-54-report.md` only. Existing product owners must be called directly;
do not add a duplicate producer, monkeypatched success validator, fake prediction
or a test-only production permission. A concrete missing product seam is a
reported finding for Root before any out-of-scope edit. No other agent, Git,
server, dependency installation, source-module edit or cleanup operations.

Read task-54-integration-preflight.md, Task48 and Task51 final reports, and the
actual APIs named below. Failed attempts and all source files remain retained.

## Binding scope and authority

Approved C spec SHA256
08f296bfd0b7c43e4934367ec2533f29464b28f9dc7d2ada218ea194ae4645ba.
Same source/order/cutoff/physical/D2/original/feature/model semantics. No new
per-value SQL16MiB or64MiB cap. Full raw inputs4GiB, history1GiB/tour,
encoded reference/processing blocks16MiB, new workspace/retries8GiB and actual
free reserve4GiB remain unchanged; local fixtures cannot establish native
CPU300/AS2GiB/RSS1GiB/output1MiB or global1800CPU/3600wall admission.

## Genuine fixture and closed small chain

1. Build a small legacy baseline with real `configure`/`run_batch` and owning
   artifact/capture APIs, actual stored tour state and at least one complete
   Original/snapshot. Include foreign-tour physical rows and run ATP/WTA cases.
   The fixture provider and clocks may be controlled; prediction, normalization,
   persistence, artifact and complete semantic validators may not be replaced.
2. Preserve its complete raw inventory and original file hash. Create a separate
   small legacy oracle from identical baseline bytes. Two ordinary normalized
   receptions are appended by the actual old owner; one new native competition
   is then captured/predicted/persisted/finished by the actual old live worker.
   Exactly THREE new receipts result, not three plus a hidden fourth capture.
   Use distinct real normalized IDs and consistent later reception/cutoff/
   future schedule clocks. Bind old and new paths to the same actual values.
3. New path calls `build_receipt_corpus` with exactly those three actual
   `(normalized_record, observed_at)` pairs. Its result is observed copy data,
   not Source permission. Writer closes before a fresh profiled RO connection
   opens and holds one real TrackedConnection transaction. Recompute complete
   RawInventory and compare exact full typed membership with the real oracle
   at its post-reception/pre-new-Original point, not just counts or fake SHA.
4. Establish actual D2/artifact semantics on that held source: genuine
   VerifiedArtifactMapping, validated actual created-at mapping and
   `_verify_artifact_types` produce `semantics['protected_receipts']`. Then use
   the actual VerifiedReceiptMapping constructor and validate_all. No public
   descriptor, hard-coded empty protected set or caller boolean substitutes
   for the unchanged owner. Small old loaders remain honest small oracles;
   this does not declare them fully streaming at native growth size.
5. Adapt complete OLD snapshot coverage into its own fresh parts store. Commit
   and close that writer; independently reopen/check all original coverage.
   Do not append new consumer products to it: source-adapter coverage is exact
   membership of the source's old snapshots, not a generic growing output.
6. Build actual History on the fresh Mapping, then Task51's real
   prepare_tennis_consumer and put_tennis_consumer in a SEPARATE fresh consumer
   store. Retain the live source/history/features for their required scopes;
   no dataclass resurrection or post-exit reopen assumed. Keep model/effect/
   approval outcome and unresolved native-state identity genuine.
7. Compare full same-call original canonical bytes/hash, prediction values,
   full reference memberships, complete features, snapshot key, raw canonical
   payload bytes/digest and selection reason to the old actual producer.
   Reopen the completed outputs read-only and check again through actual
   parts/ref readers. Full materialization is allowed ONLY for the explicitly
   small independent test oracle, not smuggled into a new production loop.

## Single-job local reservations and completion boundary

Predeclare one actual WorkspaceBudget before new writers: fixed corpus main,
journal and two-bank ledger, separate old-parts main/journal, history
main/journal, feature main/journal, new-consumer main/journal. Use explicit
small page-aligned ceilings sufficient for measured fixture sizes, count all
main+journal ceilings and the source ExternalInput, include directory metadata.
Do not widen constants after a failure or free slots by deleting artifacts.
Exactly one writable main is active at a time; all owned writer capabilities
must be closed before quiescent reconciliation and final output reopening.
Report actual reserved/observed sizes and free space, plus unchanged input hash.
WorkspaceBudget is a local accounting component, not native filesystem quota.

There is no global production manifest or B authority in this task. A private
completed phase is not whole-generation publication. After a late failure,
verify that no successful result escaped and that partial new-consumer rows
were rolled back; preserve earlier completed private phase files for diagnosis.

## Cross-phase negative checks

Test actual new seams: source transaction/generation drift after the corpus
phase; altered full membership with the same count; missing/extra native member;
abort after a completed private corpus before consumer completion; failure
after genuine prediction before full Original/snapshot insertion completes.
Reuse already exact lower-level coverage rather than repeat unrelated broad
failure matrices. If a named integration failure reveals a real product defect,
retain its RED and report it; do not change tests merely to produce GREEN.

Use `.pytest_tmp/qa-python312-c-01/Scripts/python.exe`, -B, disabled external
pytest plugins, no cacheprovider, junit_family=xunit1, fresh retained basetemp
and XML paths. Run the new module and only genuinely coupled existing files.
No full product suite at this intermediate task. Record exact commands/results,
source/output/candidate hashes and remaining cross-worker/native/global/B gaps.

The full three590553-receipt/199-Original/199-snapshot/114-key profiles, native
closure/reopening, all-costs owner, B, restore and release remain later gates.
