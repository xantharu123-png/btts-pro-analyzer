# Task54 integration preflight — not activated or implemented

This records the next already approved small chain while Task53 is the sole
writer. It is not a native plan, final API brief or another design gate.
Task51 must first supply and independently validate its real consumer bridge.

## Actual boundary to exercise next

`build_receipt_corpus` creates/commits/closes its own private copy and returns
observed data, not Source permission. A new held profiled RO connection must
recompute full RawInventory and actual artifact/D2 ownership before creating
VerifiedReceiptMapping and calling validate_all. `_verify_artifact_types` is
the existing actual D2 owner and returns `protected_receipts`; neither a
hard-coded empty set nor the CorpusResult inventory grants that authority.
The old owner can run as an independent small fixture oracle, not the large
unbounded implementation. Actual snapshot source coverage stays in its own
fresh parts store; appending new consumer products to it invalidates its exact
source membership, so new consumer products have a separate writer/store.

Use one small baseline produced by real legacy fixture owners, with actual
stored ATP/WTA state and at least one original/snapshot. Copy/add exactly two
ordinary normalized statuses and one genuine new scheduled consumer reception
(three new receipts total). Do not append another capture on preparation.
Use a complete old writer/capture/predict/finish on a separate small oracle to
compare exact selected order, full refs/features, same-call original bytes,
snapshot key/raw bytes/payload digest and genuine effect-selection reason.

Known fixed slots: corpus/legacy-copy.sqlite + -journal + receipt-additions.bin;
old-parts/snapshot-parts.sqlite + -journal; history/history.sqlite + -journal;
feature/features.sqlite + -journal; new-consumers/consumers.sqlite + -journal.
Every directory is an explicit existing empty slot; no random allocation or
cleanup. Choose small explicit caps from real fixture sizes and account main
plus journal, actual source and ledger and all failures before running.
Close each writable main before the next writer. Source/History/Features stay
live only as required for actual consumption. A local sequential multi-main
check is not automatically a native closed-catalogue or cross-worker reopening
permission. Native ownership must be implemented and measured separately.

Negative integrations required by Task39: source change between phases,
missing/extra/same-count other member, abort after corpus commit with no final
generation, source/native collision, and failure after actual prediction that
leaves no partial published consumer. Reuse existing narrow tests where they
already prove the same unchanged edge; add genuine cross-phase tests where
that is the new risk. Retain all generated files and old source bytes.

## Fresh baseline observations for later native planning

The actual sealed VPS baseline was refreshed read-only at2026-09-12T14:23:54Z:
270233600 bytes, SHA73ec16911c7d66e894a0f92c6600e3486e34b57c4176b30938638d3669146ffa.
100553 contents/receipts,31 snapshots,31 originals and2 tour states.
Receipts:42099ATP,54819WTA,3635football. Latest observed clock is
2026-09-11T23:07:34.262393Z. No runtime DB/secret copied locally; these are
bounded counters, not a fresh full raw/source/D2 verification.

Current positive native diagnostic imports only NumPy/SciPy. Real predictor
and tour-state imports include pandas and data-loader/backtest dependencies;
the new actual chain needs its own complete sealed import catalogue and native
measurement, not a claim that Task50 already covered those imports.
Existing QA disk use remains retained. Full C still requires590553 receipts,
199 originals and199 snapshots,114 cutoff/tour keys in each of the three
unchanged profiles, full aggregate accounting and later B/restore/release.
