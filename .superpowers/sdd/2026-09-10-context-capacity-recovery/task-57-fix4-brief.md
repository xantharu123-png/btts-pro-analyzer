# Task57 FIX4 — fresh-owner full import diagnosis and bounded remedy

Read task-57-brief.md first: it remains the binding exact requirements.
This is fix round4/5 under the approved SDD process. The original implementer
has completed three scoped fix rounds. You are the fresh implementation owner;
Root alone owns index, Git, native transfers/execution and controller evidence.

Current HEAD9007150fde783c9ef8d0c453146f8a78a05f3ba0, previous reviewed
executable FIX_BASE97e3bb64cfc4c0e81f4d55262db2de1b613760b6. The current worker
SHA584f90ba46e3c2895eb5ea6bbca67956c407da45e37473d5314dcb4e0f83573d denies
only exact admitted-source cache probes as FileNotFoundError and otherwise
retains closed-file admission. FIX3 was independently approved with32 targeted
tests; it is not the completed native chain. Do not undo the reviewed cache
non-permission regression to make a package import succeed.

## New actual finding

The exact FIX3 archive was inventoried on the real VPS and actually launched
under the unchanged reviewed stdin parent/supervisor/guard. Its isolated job
`/var/lib/betboy-context-chain-task57-97e3bb6-03` failed terminally before any
Task54 setup directory or DB was created. All three private attempt directories
are still empty. WTA was not launched.

- Actual ATP exit125/nonzero_exit, childCPU1716124000ns, elapsed1757707859ns,
  peakRSS91897856bytes. Actual guard UID/GID65534, PID222235, CPU90s,
  FSIZE4194304bytes, one seccomp filter; initialRSS60301312bytes.
- Exact retained stderr167bytes again says `ChainError: unplanned Python file
  read`, then `betboy-native-launch-failed`. The unknown path is NOT captured.
- Parent externally terminal: exit1/wall40.32/user28.02/system7.36/maxRSS89744KiB.
  Full300CPU reservation retained/stopped/unmeasured, no remaining UID65534 PID.
- Native result890bytes SHA
  b46674f277806eea7e6593b3a59f8e5fdbec72dad221a3730b1b8974c79a4e72.
- Native manifest `.pytest_tmp/task57-native-inventory-97e3bb6-03.json`:
  679530bytes SHA7692f06a247196eeae16d122685e9aab73d6c4b1e0e730a66ff9f0179dc8d0b8.
  It contains exact440 code files/8882016bytes and4540 existing dependency
  files/217788209bytes, fixed34 package roots and the actual Linux runtime.
- Code archive `.pytest_tmp/task57-candidate-97e3bb6-python.tar`:9236480bytes
  SHA1aca4741b3dc21767da361784c21445694a38f3b87c75da3bd05ec7a08b0f465.
- Parent/catalogue/helpers/stdin are unchanged from FIX2, stdin SHA
  c6f3d93e0911b60a07e5e851717ec4c8f6e9155b76500ff7b8a7c3f5d0811a9a.

The earlier full local-route diagnostic hashed the entire unrelated local
site-packages installation and was manually stopped before it produced a
catalogue or workspace. Its two PIDs were confirmed absent. Do not repeat that
unbounded discovery. Actual local real pytest import is already proven; the
new failing phase is a later import, not a completed data-flow call.

## Concrete work and boundaries

1. First identify the specific missing read/contract by bounded read-only code
   inspection and an appropriate actual import diagnostic. The entrypoint
   `import test_context_storage_corpus_consumer` already reaches all imports
   before the first real setup, so no whole fixture build is required to
   diagnose this observed phase. Keep portable versus native claims distinct.
2. Use exact observed local dependency roots or existing native manifest file
   names; no unrelated all-environment hash sweep, guessed package install or
   whole-directory allowance. Any local diagnostic must have a real bounded
   subprocess timeout and retain its actual output/failure. The existing hook
   consumes exact file names; a local name inventory is a diagnostic, not a
   native sealed/hash-proven catalogue. Do not claim otherwise.
3. If needed, ask Root for narrowly named read-only native file inspection.
   No direct server action. Do not execute dependencies outside the prescribed
   native guard to bypass this failure, or change production settings/data.
4. If the precise read cannot be established portably, propose the smallest
   bounded native error-path observation change first. An unknown path is not
   permission to allow it. Root can review and run that diagnostic within the
   same retained fixed envelope; no silent automatic retries.
5. Report the concrete mechanism BEFORE choosing any input-authority change.
   Missing required runtime data needs explicit closed binding, not a wildcard.
   Every permitted source must still be exact, and unplanned bytecode/data
   must remain denied. No guard, supervisor, budget or limit relaxation.
6. Add genuine focused RED/GREEN around the actual mechanism and the failure
   admission boundary. Re-run only changed harness coverage; not whole product
   or repeated unchanged native guards. Keep all previous failed outputs.

Own only the same four harness/test files plus append to task-57-report.md.
No product, Task54, frozen specs, helpers, Git, server, installation, deletion,
other-agent dispatch or main/VPS deployment. Declare concrete changed files
before editing. If a required interface exceeds that ownership, report it to
Root before expanding. Use approved Python3.12 QA, disabled external plugins,
fresh retained paths/XML and exact file hashes. Final report must name actual
commands/results, unresolved limits and return sole writer to Root.

Global C590553/199/199/114 profiles, persistent all-cost owner, B/closure,
restore, whole-suite and application rollout remain subsequent open gates.
Cricket/A0/P4b3 and the failed separate daily Tennis service stay out of scope.
