# Task8 native release evidence — not yet executed

Controller record, 11 September 2026. Task8 implementation is in progress
from06db3be; there is no accepted Task8 product SHA or native result yet.
Task7's actual-current failure remains binding. This file is a test schedule,
not a passing report.

## Fix1 exact candidate —07d975f, rereview pending

Final product/report commit07d975fb54a62ecb976c8040823e16fe45d9f16f,
118final owner-coverage tests passed in85.15s. Scoped Fix1 rereview is running.
Exact Git archive `.pytest_tmp/capacity-task6-07d975f.tar.gz`:
33863927compressed bytes,826safe members,102786203expanded bytes,
SHAbc19bce5fbf9bf14a83642b246c044fb0bee342509ac99c2b312205d7bdf75d9.
Local structural/PAX verification passed; approved SCP completed exit0 to
`/tmp/betboy-context-qa.9xr68INa/capacity-task6-07d975f.tar.gz`.
Measurement/archive/Linux helper pins now name07d975f. Native DAC source
path remains deliberately unset until exact root staging. No source staging,
native model execution, full suite, main push or deployment yet.

Subsequent scoped Fix1 review APPROVED all three reference findings with
no new findings; full verdict in task-8-review.md. Unchanged pinned stdlib
stager created `/var/lib/betboy-capacity-code-b3xcmdb8/source` and sibling
fixtures from the exact07d975f archive,826members. No app import ran as root.
Actual-current measurement started asUID997, child201336, source07d975f,
inputced192e7...d2f05. Fixed supervisor/result/input checks are unchanged.
Final detached QA checkout advanced tracked-clean from899 to07d975f, preserving
all earlier outputs. The whole suite is not started before current-data PASS.

## Reviewed-candidate preparation (review still pending)

Subsequent scoped verdict:7bab03c NEEDS FIXES for a retained, already uncharged
query loop object during sealing. See task-8-review.md. Its uploaded archive
is retained as a rejected review baseline and has NOT been staged or executed.
Fix round1is in progress; prepare new exact pins/archive after its commit and
scoped rereview, do not execute the currently7bab-pinned helpers as acceptance.

Task8 product/report candidate7bab03c481a7d6053ef53ad796527bad259adfd4:
962focusedpassed/12platformskips,556.58s; independent scoped review in progress.
Exact local Git archive `.pytest_tmp/capacity-task6-7bab03c.tar.gz` (legacy
shared-stager filename) contains826safe members,102770637expanded bytes and
33858223compressed bytes. SHA256:
`2055c1f4eafa85e08d7829ec6c4f5733249ed2da564adeed3ad4f955a97644a4`.
Git PAX commit and all member paths/types were verified locally. Only versioned
source/tests/reports were archived; no additional local secrets or runtime DBs.
Approved SCP transfer completed exit0 to
`/tmp/betboy-context-qa.9xr68INa/capacity-task6-7bab03c.tar.gz`.
No root source staging or native execution has occurred at this checkpoint.
The exact prepared stager remains0c0427f286169241db854cc1328c5c987d51499eeb89120479a0c1a3a63f9359.

## Exact inputs retained on the VPS

| Profile | Root-sealed path | Bytes | Receipts / artifacts / snapshots | SHA256 |
| --- | --- | ---: | --- | --- |
| Actual-current | `/var/lib/betboy-live-backup-_04ttiwe/context-current.db` | 253333504 | 99521 / 28 / 26 | `ced192e70a7240606906087df08b8e82be227189f06728e7c62dfb3b186d2f05` |
| G1 | `/var/lib/betboy-task4-growth-mxqhafsc/generation-1/context.db` | 212172800 | 95406 / 13 / 11 | `01186c29cb6e31aef5815ae3f937d81401b9e72ddaaa30d934864bcf78daa966` |
| G2 | `/var/lib/betboy-task4-growth-mxqhafsc/generation-2/context.db` | 215121920 | 95407 / 14 / 12 | `12157b36f8b9e53677dd5d4f9c2c9f1ddc7a56b1bf9000f1434c95afc363eed4` |
| G3 | `/var/lib/betboy-task4-growth-mxqhafsc/generation-3/context.db` | 218079232 | 95408 / 15 / 13 | `79954e45984d3ee4aafd64025c69679517e4a2fbbdec990a5ff342017053d0ec` |
| Historical largest | `/var/lib/betboy-capacity-growth-t8512i71/generation-3/context.db` | 387739648 | 188791 / 6 / 4 | `3628dee7b82bcfadd320bfc9f28df8d770fc7e31692de690cdcb0ae0919cc953` |

All have2manifests and0rollbacks; the optional rollback table may be absent.
The older114929664-byte current copy is not a substitute for actual-current.
Rehash and check complete filesystem identity/no companions for each run.
The historical largest is additional regression evidence, not a replacement
for G1-G3 receipt-plus-consumer growth. It preserves the earlier supported
188791-receipt/4-snapshot profile that passedc4e20e0 at298.846wall seconds.
Its artificial future rows still require complete physical verification.

## Execution order after immutable code and independent scoped review

1. Archive only the exact committed candidate with `git archive`; inspect
   member types/paths, compute SHA256 and record the Git PAX commit. Use the
   already approved QA upload and unchanged root stdlib stager
   `.pytest_tmp/prepare_task6_candidate_source.py` (legacy filename only).
2. Pin a new copy of `.pytest_tmp/measure_task7_profiles.py` to the exact
   candidate. Do not alter supervision, inputs, counts or result criteria.
   Run actual-current first, uninstrumented. A failed actual profile ends
   release acceptance; do not launch an obsolete complete local suite.
3. On actual-current success, run G1/G2/G3 plus the historical largest serially and the fresh complete
   local suite in the separate detached QA worktree. Preserve old XML/bases.
   Model output remains `incomplete` / `transport_only` with exactly existing
   D1/D3 limitations and `empirical_approval_verified=false`; exit2 with that
   complete report is expected, not an empirical model approval.
4. Run Linux projection/witness/shared/capacity/live-Tennis focused tests with
   warnings as errors on the exact archived source as ordinary QA UID1000.
   The number/platform meaning of skips must be recorded, not inferred.
   The nine `linux_seal` tests require `CONTEXT_CAPACITY_TEST_ROOT` and may
   also skip in ordinary UID1000 QA without app-readable root-sealed fixtures.
   Their real3DAC-denial/8unsafe-input controls are explicitly executed by
   the separate exact-source UID997 native helper against the staged fixtures;
   never call the skipped ordinary-QA cases passed.
5. Run exact source reader DAC controls as UID997 and unchanged seven native
   sealed-input races. No root application imports. Retain D2 named-binding
   and actual unopened-final evidence; rerun cross-task coverage where needed.
6. Obtain the one final whole-branch review against current main, including
   deferred Task5 unordered-scan comparison. Fresh full backup with actual
   isolated restore/HMAC is separate evidence; if its current input has grown,
   it requires current-data verification, not the earlier copy's result.
7. Only all passing evidence permits the previously authorized main push,
   narrowly pinned updater-only transition, and separate ordinary exact app
   deployment. Recheck actual source/services/health and keep the pre-existing
   Tennis job failure visible until a successful job independently proves it
   repaired. No marker/key/data rewrite or silent limit change.

## Fixed performance/admission contracts

Exact CLI child UID997, AS2GiB, CPU300, wall600, output1MiB. Acceptance also
requires measured CPU and wall below300seconds and RSS below1GiB. Sealed
input1GiB, individual history256MiB, retained plus pending cache64MiB and
32aggregate entry/query metadata slots remain unchanged.

## Results

### Actual-current07d975f — FAILED

Uninstrumented exactCLI UID997 child201336 ended at300.046CPU/300.135wall,
peak486572KiBRSS,returncode-9. Supervisor exit1,child_exit,null report,
measured_profile_accepted=false. Input253333504bytes SHAced192e7...d2f05
and complete filesystem identity unchanged; no companions. No current-data
PASS, empirical approval, main push, updater exchange or app deployment.
Local final suite and final whole-branch review were not started because
this decisive profile failed. Exact reviewed code07d975f was already pushed
normally to the existing repair branch, priorupstream06db3be; main2dd1116.

Bounded same-source observation helper
`.pytest_tmp/trace_task8_fresh_07d975f_phases.py` is now running asUID997,
unchangedCPU300/AS2GiB with diagnosticCPU275/wall295stop and1MiBoutputbound.
It times preparation/cold/seal, actual projection hit/miss, scalarfold cost,
owned-full fallback and unchanged snapshot/outerdecoder work. No source
patch, skipped owner check or successful verification is claimed.

### Bounded07d975f diagnosis — complete diagnostic, NOT verification

Helper SHAc87e62f084e3bbab9a3078d46102edeecb8cfabe5396ffca6f0c602a0e11f337.
Execution ended cleanly with deliberateCPU275stop,277.100CPU/276.115wall total,
486528KiBRSS,input full identity/SHA unchanged,no companions. No diagnostic
stderr/unwind noise. It did not return a complete D4 report.

- Complete physical checks45.653CPU/45.707wall.
- One complete cold history87.302CPU/87.350wall.
- Independent complete seal26.135CPU/26.141wall;42099scalarfold calls used
  0.733CPU total. Single58390805-byte entry plus7403charged metadata,27slots,
  no eviction/bypass. All26planned queries remain present.
- All26original lookups HIT:0.0083CPU total,zero owned-full-fallback calls.
  All original/native/predictor checks including preparation126.407CPU,
 126.467wall; snapshots start at178.186wall. The projection optimization
  is actually used and saves original materialization; it is not sufficient.
- First18 complete snapshots cost4.018–4.747CPU each. The19th at the larger
  06:37cutoff costs9.148CPU, including3.035history materialization and
  4.233full owning features. The20th is deliberately interrupted in history
  materialization. The remaining6snapshots are not reached in this diagnostic.
- Outer snapshot phase96.930CPU includes19complete and one partial replay.
  Twenty actual snapshot decodes consume1.210CPU. This excludes no checks;
  aggregate call timings are nested, not additive independent measurements.

Root assigned a read-only bounded follow-up design, task-9-design-analysis.md,
and launched a separate20CPU cold-owner profile after the full physical pass.
No Task9 implementation/file scope or changed invariant is silently approved.
Full suite/whole-branch review/deployment remain HOLD pending viable exact
current-data evidence, not another run against a smaller historical copy.

### Cold-owner20CPU attribution — diagnostic only

Helper0701533b65f6592dd0579ea83a39d284ed289570f3dac4cd9bd0584c52f88542
`.pytest_tmp/profile_task8_cold_window.py` ran on exact07d975f asUID997 after
the complete physical pass. Only the cold owner was profiled, then deliberately
interrupted after20CPU:19.994profiled seconds,73.355totalCPU/72.292wall,
193264KiBRSS. Full identity/SHA/no companions unchanged. No validation bypass,
complete report or acceptance claim. cProfile overhead and the first12977rows
mean these proportions are diagnosis, not extrapolated full-input timings.

| Function | Calls | Cumulative seconds (nested) |
| --- | ---: | ---: |
| values_at_or_before |12978|10.2499|
| _decode_receipt |12978|7.7967|
| select_tennis_observations |12977|9.4301|
| _validate_selected_tennis_receipt_cold |12977|8.9996|
| normalize_observation |25954|8.4124|
| _sport_json |633110|6.3574|
| _is_price_name |502284|4.2729|
| canonical_bytes |150695|4.5565|
| _inventory_stamp |38933|2.0018|
| canonical_timestamp |123952|1.5093|

The receiving design agent has this evidence and the exact call counts.
It must distinguish removing a repeated physical decode from removing source
validation: the latter remains required, as does the independent complete seal.
Generic receipt-decoder or inventory integration changes require an explicitly
recorded scope decision; none is implemented by this diagnostic.

### Exact07d975f native role/reader boundaries — PASS

Against sourcebetboy-capacity-code-b3xcmdb8 and its synthetic fixtures,
UID997 received all3actual write/rename/SQLite denials. All8unsafe classes
were rejected:hardlink,symlink,owner,mode,ancestor_mode,sidecar,WAL,budget.
Small sealed-file and in-memory complete reports match exactly. Exit0.

Unchanged root-stdlib race controller created only a fresh synthetic tree
`/var/lib/betboy-capacity-races-8qj70gbs`; all app imports ranUID997.
All7actual boundary changes rejected:file replacement,ancestor replacement,
bytes mutation,sidecar,file mode,ancestor mode,and before-SQLite-open
replacement. Exit0,production_changed=false. These are reader/DAC results,
not a waiver of the failed actual-current capacity profile.

Exact archived07d975f ordinaryUID1000 Linux projection/witness/shared/capacity/
live-Tennis regressions launched separately with warnings as errors and a
newQA extraction. No result yet. Whole repository suite still not started.

Pending. No Task8 source was uploaded, tested, pushed to main or deployed at
this record's creation. Earlier reviewed repair checkpoints are published
only on `codex/context-capacity-recovery-20260910` through06db3be (Task8 plan,
not Task8 product code).

Read-only preflight during implementation reconfirmed the known worker state:
`betboy-tennis.service` is failed, Result=exit-code, ExecMainCode=1,
ExecMainStatus=1. Its last start/end were11Sep07:17:02/07:32:19CEST. No reset,
restart or new job execution was performed; timer health does not clear it.
The separate local final-QA checkout remains tracked-clean at8992386, with
its old completed test evidence preserved and sufficient free local space.
Task8-only measurement/Linux/DAC helpers have been prepared with deliberately
unset revision/archive/source pins; none has been executed prematurely.
