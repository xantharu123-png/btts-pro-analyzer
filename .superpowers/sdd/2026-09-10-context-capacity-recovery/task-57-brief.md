# Task57 — actual guarded native small-chain harness

READY FOR ROOT ACTIVATION after the documentation checkpoint. Task54 code/test
commitcac8db725b4c9ffac6bbcb4f81817d90da2823c9 is independently approved:
test SHA5a59f75d0a3093238031159a813ce81b6c633c63105cab0338ed376a7bac7649;
review SHAd387a88691c2454febf1cced49289f5fb461f0cf77919be291de19e56c417583.
The report-only M1 precision update is493c4d4cacc198c6bed4d69bd0ea542eed678553be502ab88c91ca9988b92f26;
test/product bytes and final5/34-pass runs are unchanged. M1 is a deferred
direct assertion of the new Original timestamp, not a demonstrated product
defect; do not silently treat it as tested. Root records actual dispatch BASE
and sole-writer identity in the ledger. This continues approved C, not a new
model/source/odds decision or B approval.

## Scope and ownership

Build the smallest dedicated native harness for the finalized Task54 real
ATP/WTA chain. Own new `tests/native_context_chain*.py`, their focused
`tests/test_native_context_chain*.py`, and `task-57-report.md` here. Define the
concrete file list before editing. Do not edit product owners, the existing
native guard/supervisor/budget, previous probes, Task54, services or deployment.
No Git, server command, dependency installation, cleanup or subagents. A real
missing required interface is a finding for Root before expanding ownership.

Read task-56-native-chain-report.md, task-57-native-catalogue-observation.md,
Task54's final brief/report and only the existing native owner couplings needed
for this task. Historical native diagnostic Task50 is not this chain's evidence.

## Actual execution, not a new substitute producer

The entrypoint is the finalized Task54 callable in
`tests/test_context_storage_corpus_consumer.py`. Call it directly with an
explicit private path, actual pytest MonkeyPatch owner and the fixed tour;
do not invoke pytest.main/collection/plugins. Reject optimized Python
(`sys.flags.optimize != 0`) before this assert-bearing acceptance code runs;
assertions must remain active. Provider/clocks remain the
existing controlled fixture. No replaced prediction, validator, semantics,
Source permission, constant result or hand-built consumer object.

Two fixed guarded children, in order: ATP then WTA. ATP also exercises the
existing actual late-cleanup rollback case in a separately reserved directory;
keep those failed private files and record that nothing was accepted. Each
case includes its real legacy setup/oracle and C phases. Record their union,
not three separate reset allowances. Positive cases must retain old/new full
byte comparisons, reopened outputs and genuine D2 admission, not just JSON
counts. Native setup and every comparison are charged to the child.

The root parent is a newly launched `env -i PATH=/usr/bin:/bin LANG=C.UTF-8`
Linux/x86_64 `python3 -I -S -B` interpreter that has never held application
secrets or imported product/dependency code. It may execute only the reviewed
stdlib harness and exact pinned existing stdlib helpers. All product/pytest/
numerical imports occur only after the existing supervisor's UID/GID drop,
guard installation and stopped-child readback. Reuse `run_single_process`
unchanged, with actual pidfd/wait4 custody. Do not replace or loosen seccomp,
identity, inheritance, output, RSS, AS or failure rules to make imports work.

## Fixed, truthful diagnostic envelope

Ruling prepared by Root: use a stricter small diagnostic envelope, not the full
growth profile. Parent hard CPU90s, each of the two child hard CPU90s; AS2GiB,
RSS strictly below1GiB, combined observed child stdout/stderr at most1MiB,
and child per-file FSIZE compatible with the exact Task54 main/ledger slots.
The parent hard FSIZE must allow its largest predeclared copy and be at least
the child's limit; similarly parent hard AS/CPU must permit the dropped child
to set the requested lower/equal limits. No limit setter is permitted after
the child's guard. These stricter90s bounds do not change C's maximum300s.

One conservatively retained300CPU-s PreparationBudget reservation covers the
whole native diagnostic. Parent90 plus two children90 each gives270s hard
CPU upper bound; leave the remaining30s as charged margin, not a refund.
Start accounting before copy/sealing and before any child; bind the initial
archive/code/runtime/profile and job directory identity. Include interpreter
startup in parent CPU/boot-clock observations. The same trusted stdlib parent
performs admission/copy/sealing, then both child calls: do not hide an earlier
unmeasured sealer subprocess. Fixed total diagnostic wall deadline600s from
the parent's actual kernel start; each child wall90s. Observe all final I/O,
cleanup, report and terminal exit externally before declaring a native pass.
No settlement/refund: close stopped with the complete durable charge retained,
because the live parent cannot terminally measure its own exit. An unknown or
unreaped child is a stopped custody failure, never success or a fresh retry.

This is a new bounded diagnostic identity, not the missing protected whole-C/B
cost registry. Explicitly keep old Task50 attempts and developer diagnostic
costs in their existing evidence. No global1800CPU/3600wall certificate follows
from this small envelope. No retry reset or deletion is introduced.

## Catalogue and workspace admission

Create a closed, versioned archive/manifest contract from the reviewed commit,
never a caller-selected executable/module or arbitrary unversioned path.
Task54/fixture code, route-live local modules and the six actual code-hash files
must be present with their proper project-root layout. A bounded over-inclusive
set of tracked Python sources is allowed if each exact member is predeclared
and hashed: current read-only scale was445 tracked Python files,8866614 bytes,
largest214856 bytes before Task54 was committed. Those observations are NOT
final catalogue constants. No local secrets, untracked QA outputs, runtime
databases or training fallback data in the code archive.

Select and bind the actual existing native package files needed by the route,
including lazy imports and relevant package metadata. The package-name note
is not admission. No root import for discovery and no installation. Copy into
one fresh root-owned read-only namespace; reject linked, duplicate, unknown,
changed, missing or oversized catalogue entries and file/directory races.
Recheck held input identities and full copies before running a worker and
after it. Within this new seal, only fresh child workspaces are writable by
the child; do not alter any existing host path permission or imply that this
DAC arrangement is a whole-host filesystem sandbox. System interpreter,
stdlib, native ELF and runtime-data observations must be explicit; do not call
a Python package-tree hash a complete B runtime-closure proof.

Before the first copy/writer, predeclare the aggregate code/dependency copies,
archive, all three Task54 setup+C attempts, main/journal/ledger slots, metadata,
accounting journal and bounded reports. Bind exact positive and retained-failure
directories, no wildcard slot or directory reuse. Use Task54's final slot plan,
not guessed filenames/caps. Total new files/retries<=8GiB and active inputs
including indexes/blocks/manifests<=4GiB. Actual free reserve4GiB plus unspent
reservation; sample logical AND allocated sizes and source/candidate hashes at
each real quiescent boundary. A local accounting object or final diskwalk is
not a native filesystem quota. Do not claim the full growth writer envelope.

## Verification and handoff

Add focused RED/GREEN protocol/manifest tests: exact catalogue completion and
rejection, bounded copy failures/retention, fixed cases/CLI, resource-plan sums,
guard-before-product ordering, output parsing, changed inputs, late-close
no-acceptance and stopped/unknown/over-budget handling. Reuse existing exact
owner evidence, no repeated unrelated guard suite or whole product suite.
Tests of parsed proc strings are not actual kernel observations.

Use the approved Python3.12 QA runtime, disabled external pytest plugins,
no cacheprovider, fresh retained basetemp/XML for each run. The report includes
exact file responsibilities and pins, actual commands/RED/GREEN/output, final
archive/catalogue preparation command and reviewed native invocation procedure.
Root conducts the real read-only dependency inventory and any subsequent
authorized native upload/execution only AFTER independent harness review.

Full590553-receipt/199-Original/199-snapshot/114-key growth profiles, transitive
runtime closure, protected all-cost owner, B, restore, whole suite, main and VPS
deployment remain explicit later gates. Cricket/A0/P4b3/daily-job unchanged.
