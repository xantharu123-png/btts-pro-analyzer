# Task57 FIX5 — bind the two actual required timezone data inputs

Read task-57-brief.md first: it remains the binding exact requirements.
This is fix round5/5. A prior implementer attempted the task three times;
the fresh FIX4 owner corrected the optional metadata handling, now independently
approved. You are a fresh most-capable implementation owner for this new actual
runtime-data gap. Read the existing report's FIX4 appendix for what was tried.
Root owns Git, native operations, preparation evidence and the progress ledger.
Previous reviewed executable FIX_BASE isdf874ba7a29ee41ef27d48eb7a726520bd6fdcc4.
Root supplies the actual dispatch HEAD separately. No reapproval or worktree setup.

## Actual finding, no guessed pathname

The real closed native df874ba-04 child now reports this exact rejected open:

```text
ChainError: unplanned Python file read
event=open
path=/usr/share/zoneinfo/UTC
path_truncated=false
path_sha256=c53fc1bf542fbbb1244e33b6ce647c4c05e4cdf1d08542c7c898c852d3f2b223
flags=524288
```

Actual ATP exit125/nonzero_exit; childCPU3863069000ns, elapsed3964996145ns,
RSS135450624bytes; parent supervision38746209ns. Guard UID/GID65534,
PID223356, CPU90s, FSIZE4194304bytes, one seccomp filter. Output362bytes,
SHAa659e278b54e124bcd61cc912e9b9aab90a081b7330e4c528d67c5663f2ee094.
No Task54 setup/data file was created; all three reserved attempt directories
remain empty, no WTA execution. External terminal exit1,40.94wall,29.14user,
7.17system seconds,132128KiB maxRSS. Full300CPU charge retained, no live test PID.

Local raw actual readback `.pytest_tmp/task57-native-run-df874ba-04-readback.json`
2201988bytes SHA0ef1d803bd6399a54d5edbef81a38f5d58f2f6d3125f6d76e938ed4c35d7cf65.
Exact manifest `.pytest_tmp/task57-native-inventory-df874ba-04.json`
679530bytes SHAc1753bd765bcc219911674a1f41f7bb370aca20fdbbe06e66765e5b0141b09b2.
The complete raw bundle is also preserved as gzip in this plan's evidence.

Root's read-only source lookup finds `scripts/tennis_daily.py:31,50` imports
ZoneInfo and constructs `ZoneInfo("Europe/Zurich")` during the actual Task54
import route (`tests/test_tennis_live_worker.py` -> daily). This second required
key is source evidence, not another observed native denied path. Other unused
sport modules are not authority to add further timezone keys.

Root's actual native no-import observation20:04:29UTC:

| Path | Actual type and owner | Bytes / allocated | SHA256 or alias |
|---|---|---|---|
| /usr/share/zoneinfo/UTC | root:root symlink0777 | 7 / 0 | exact relative target `Etc/UTC` |
| /usr/share/zoneinfo/Etc/UTC | root:root regular0644 | 114 / 4096 | 8b85846791ab2c8a5463c83a5be3c043e2570d7448434d41398969ed47e3e6f2 |
| /usr/share/zoneinfo/Europe/Zurich | root:root regular0644 | 1909 / 4096 | 2b9418ed48e3d9551c84a4786e185bd2181d009866c040fbd729170d038629ef |

Both actual regular files start with TZif2. Read through exact no-follow FDs,
bounded65536bytes, with before/after metadata equality. The UTC symlink was only
lstat/readlink observed; do not introduce generic link following into custody.

## Root ruling — exact closed runtime data, no filesystem allowance

Extend the existing closed catalogue and same-parent copy/seal lifecycle with
only the two fixed timezone keys `UTC` and `Europe/Zurich`, sourced from the
two exact regular canonical files above. Do not grant reads of the original
system paths, all of /usr/share/zoneinfo, arbitrary symlinks or the local tzdata
installation. The observed UTC alias explains the canonical source choice;
the copy operation opens the fixed regular Etc/UTC source no-follow.

Each exact source size/hash/path and target key must be predeclared, validated
and bound to the immutable manifest before writing. Reject missing/extra/
renamed/duplicate/changed entries. Copy them under a distinct root-owned
read-only data subtree in the same new seal, not into code or executable search
paths; no runtime package install. Account both original logical/allocated/
directory metadata and new copies/metadata in the existing pre-copy active/
new-file/reserve calculation. Preserve held-input and full-copy rechecks at
the existing actual boundaries. No shared source or workspace is mutated.

After the unchanged guard/readback and before product/dependency imports,
bind stdlib zoneinfo to precisely that sealed data directory. Keep the Python
file audit's exact-member admission: only those copied data files may be read,
no parent-directory allowance. Do not replace ZoneInfo calculations, UTC/DST
semantics, fixture clocks, prediction, validator or actual acceptance callable.
Do not permit fallback to undeclared timezone files/packages. If actual source
inspection reveals a different timezone consumer mechanism, report it before
adding authority; an arbitrary import shim is not authorized.

Why: the real route requires timezone bytes as runtime inputs, and source-only
dependency selection omitted them. Cost if wrong: another retained stopped
diagnostic or localized catalogue rework, not relaxed resource/data authority.
This two-file closure is NOT transitive B runtime closure or full C acceptance.

## Implement and verify within owned harness only

Own the same four files `tests/native_context_chain_catalogue.py`,
`tests/native_context_chain.py`, `tests/native_context_chain_worker.py`,
`tests/test_native_context_chain.py`, plus append to task-57-report.md.
Declare the exact edit surface first. If the catalogue source changes, update
the parent catalogue pin and real launcher reconstruction tests coherently.
Existing helper pins, limits, guards, supervisor, budget owners, Task54 and all
product/spec files stay unchanged. No Git/server/network/install/delete or
subagent actions. One implementation writer only; return it explicitly to Root.

Use focused real RED/GREEN: missing/extra/changed/aliased timezone catalogue
rejection; pre-admission aggregate source/copy accounting; actual fixed copies
and changed/replaced source/copy behavior; real ZoneInfo loading from only the
sealed two-key tree with UTC and Zurich summer/winter behavior; denied original
system/unlisted/writable targets; and guard-before-data/product ordering. Use
existing local public timezone data only if its exact source is first inspected;
otherwise a deterministic legitimate TZif fixture is portable test input, not
the native data proof. No replacing the actual ZoneInfo reader to force a pass.

Re-run only the amended harness tests once final; do not repeat full product or
unchanged native guard suites. Every subprocess diagnostic has a real timeout,
fresh retained paths and captured output. An extra local PyArrow installation
still makes full local import non-equivalent; do not reopen that closed issue
by admitting/copying it. Real sealed native execution after review is Root's job.

Append report with actual commands/RED/GREEN/results, final exact source pins,
scope and unresolved concerns. The original task's deferred test minors and
full590553/199/199/114 profiles, protected global cost owner, B/restore/fullsuite/
main/VPS rollout remain separate open gates. Cricket/A0/P4b3/daily job unchanged.
