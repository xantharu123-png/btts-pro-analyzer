# Task61 native operation order — not executed

Task61 review of eaa60ac..fec316f requires I1-I4 fixes. No native prefix or
receipt diagnostic may be inferred from this document or the local dry run.
The Root prefix instrument also awaits its separate held-template correction.
After the sole author returns, freeze exact code/test/report hashes, commit
the scoped fix, obtain a scoped independent rereview, then repin instruments.

## Exact namespaces and permitted contents

- `/var/lib/betboy-receipt-prefix-task61-01`: new fixed root0755, reviewed
  1203-byte probe.py root0444, new empty success/failure directories65534:65534
  mode0700. Preparation refuses an existing root. One fresh clean stdlib-only
  interpreter per test; preserve actual parent/child/SSH exits and retained logs.
- `/var/lib/betboy-receipt-input-task61-01`: planned new root-owned input/control
  namespace, separate from every retained root. Only the final versioned
  non-hidden Python archive, exact bounded retained-control and canonical
  catalogue/control bytes. No local secret, venv or runtime DB upload.
- `/var/lib/betboy-receipt-registry-task61-01`: planned new empty protected
  root0700 registry. Actual Task60 admission alone creates durable history.
- `/var/lib/betboy-receipt-task61-01`: planned new empty root0755 job. Actual
  reviewed parent alone creates its exact reserved copies/output slots after
  admission. Never relabel a partial or failed job as empty/new.

These are reservations of names, not evidence that any path has been created.
Root checks absence/protected ancestors before creating only these paths.
Existing source/QA/backup permissions and contents remain unchanged. The old
baseline is never exported to PC; the authorized diagnostic creates its one
root0444 seal on the VPS and accounts both originals and copies.

## Complete controls and evidence

Refresh the complete retained union of the85 preflight roots, the additional
Task60 protocol root and the completed fixed prefix root. Discover unexpected
additional roots through the same explicitly scoped parent selectors; no
exclusion inside a retained root. The three exact new input/registry/job roots
are separately explicit new slots, not self-hashing retained input. Complete
typed content/identity/allocation hashes, inert links and all16 declared actual
budget-journal descriptors are validated by the reviewed catalogue. Unknown
historical total CPU remains null. Occupied hardlink totals are upper bounds,
not future allocations or releasable credit.

Retained-control preparation calls only held reviewed stdlib namespaces and
the three pinned registered helpers; no root product/pytest/venv imports. Use
the final fixed public inventory/launcher APIs after the I1-I4 fixes. Record
actual preparation CPU/wall separately; do not invent a durable ticket or
settle engineering history into the full-C1800CPU allowance.

Require actual original/copy/input metadata and physical allocation, fixed
additional4GiB backup reserve, free4GiB, complete new8GiB/active4GiB envelopes,
fresh stable baseline270233600bytes/SHA73ec16911c7d66e894a0f92c6600e3486e34b57c4176b30938638d3669146ffa,
all reviewed code/runtime/dependency/timezone identities and actual fresh
registry/job emptiness. Match archive/catalogue/launcher hashes again before
the one authorized real1024-row call. No automatic retry or changed profile.

Retain external GNU time/SSH/native child outcomes plus the exact progress
prefix, failed outputs and accounting state. A timeout/stop is measured failure,
not committed corpus evidence. Confirm actual terminal process/custody/output
readback before deciding the next C seam. Task58 small native acceptance is
already closed and is not repeated. Main/deployment, full490000 growth/global
accounting, B, restore/full regression/final review remain later separate gates.
